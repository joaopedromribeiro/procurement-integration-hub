# API Management

Planned for Phase 10. No API proxy or policy is deployed.

`docs/` will describe proxy boundaries, client authentication, quotas, rate limiting, correlation propagation and policy test results. Add real exported configuration when available, without credentials.

See [security progression](../ARCHITECTURE.md#security-progression). Backend authorization remains necessary even when a proxy validates tokens.
