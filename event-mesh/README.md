# Event Mesh extension

**PHASE 9 — CLOSED WITH ENVIRONMENT LIMITATION. Phase 9.6 — CLOSED WITH ENVIRONMENT LIMITATION.** The owner found no SAP Event Mesh or SAP Integration Suite, advanced event mesh entitlement/plan or marketplace offering in the inspected DEV subaccount. The sole `cf marketplace` match for `event|mesh` was `alert-notification` / `standard` (SAP Alert Notification Service), not an Event Mesh broker. It was not substituted. See the [Phase 9.6 closure record](docs/phase-9-6-environment-closure.md).

Phase 9.1 is **closed with an environment limitation**: owner read-only checks found SAP Event Binding and `/IWXBE/CONFIG` AMQP tooling and a Cloud Integration AMQP adapter, but no assignable Event Mesh entitlement in the currently inspected BTP environment. Phase 9.2 is a **local-only** contract/consumer-semantics proof. Phase 9.3's RAP DeliveryIntent business-event producer is **implemented and locally verified in A4H**. Phase 9.4's PIH-owned Event Binding `ZJPEBPODLVREQ` is **active**, with generated external type `zjp.PurchaseOrder.DeliveryRequested.v*` (version `0001 / 0 / 0`), but external publication is **environment-blocked**. No PIH-owned broker, service key, AMQP channel, outbound binding, queue, Cloud Integration consumer, or broker-to-SAP callback exists yet; the separate SAP HTTP trigger exists from 9.5B.

See the [9.1 offering and architecture record](docs/phase-9-1-foundation.md), [9.2 event contract/test record](docs/phase-9-2-event-contract.md), [9.3 SAP producer evidence](docs/phase-9-3-rap-producer.md), and [9.4 binding evidence](docs/phase-9-4-event-binding.md). `payloads/` contains a versioned **logical** schema and synthetic fixtures; `src/` and `test/` contain an isolated TypeScript decision simulator. Run its tests with `node --experimental-strip-types --test event-mesh/test/consumer.test.mts` from the repository root. The real broker/publication path remains environment blocked.

Phase 9.5A's [SAP trigger adapter](docs/phase-9-5-consumer-trigger.md) is **locally verified in A4H**: five production objects and the Test Classes include activated; all eight network-free ABAP Unit methods passed with zero failures/errors. The dispatcher double proves validation and same-DeliveryUUID delegation, not actual coordinator execution or HTTP delivery. Phase 9.5B then published the narrow authenticated `ZJP_PO_EVENT_DISPATCH` HTTP Service with dedicated `DISPATCH` authorization: 13/13 network-free handler tests passed, and the owner verified a real DEV path through the existing coordinator and `PIH_OrderDelivery_v1` to CAP/HANA `RECEIVED`, with SAP `DELIVERED` and one attempt. The owner-supplied final HTTP handler, Test Classes, projection BDEF, and metadata extension are mirrored. There is no Event Mesh consumer/broker runtime proof, no cutover or exactly-once claim; the concurrent revision-change race remains open.

The active A4H producer uses DeliveryIntent CREATE in managed additional save; the event key is DeliveryUUID and its parameter contains PurchaseOrderUUID, OrderRevision, and PayloadHash. The broader [event architecture](../ARCHITECTURE.md#event-driven-extension-phase-9) predates the current lifecycle finding: `PurchaseOrderApproved` must not itself trigger delivery. The Phase 9.2 logical contract is `PurchaseOrderDeliveryRequested.v1`, referring to the committed DeliveryIntent; the authoritative generated external type is `zjp.PurchaseOrder.DeliveryRequested.v*`. The three green ABAP Unit methods prove saver emission/count/payload and CREATE-vs-UPDATE gating only, not external publication or rollback semantics.
