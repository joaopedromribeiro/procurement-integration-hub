{
  "name": "cap-supplier-portal",
  "version": "0.1.0",
  "private": true,
  "description": "CAP Supplier Portal for the Procurement Integration Hub. Phase 4.1 foundation only.",
  "license": "UNLICENSED",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "cds-tsx serve",
    "watch": "cds-tsx watch",
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx --import ./test/cds-typescript.ts --test test/health.test.ts"
  },
  "dependencies": {
    "@cap-js/sqlite": "^3.1.1",
    "@sap/cds": "^10.1.0"
  },
  "devDependencies": {
    "@cap-js/cds-test": "^1.0.2",
    "@cap-js/cds-types": "^0.19.0",
    "@sap/cds-dk": "^10.1.0",
    "@types/node": "^22.20.3",
    "tsx": "^4.23.13",
    "typescript": "^5.9.3"
  }
}
