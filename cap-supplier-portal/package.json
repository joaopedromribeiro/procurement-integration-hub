{
  "name": "cap-supplier-portal",
  "version": "0.1.0",
  "private": true,
  "description": "CAP Supplier Portal for the Procurement Integration Hub. Phase 4.2 domain model.",
  "license": "UNLICENSED",
  "engines": {
    "node": ">=22"
  },
  "imports": {
    "#cds-models/*": "./@cds-models/*/index.js"
  },
  "scripts": {
    "start": "cds-tsx serve",
    "watch": "cds-tsx watch",
    "generate-types": "cds-typer \"*\" --outputDirectory @cds-models",
    "pretypecheck": "npm run generate-types",
    "typecheck": "tsc --noEmit",
    "pretest": "npm run generate-types",
    "test": "node --import tsx --import ./test/cds-typescript.ts --test test/health.test.ts test/domain-model.test.ts"
  },
  "dependencies": {
    "@cap-js/sqlite": "^3.1.1",
    "@sap/cds": "^10.1.0"
  },
  "devDependencies": {
    "@cap-js/cds-test": "^1.0.2",
    "@cap-js/cds-typer": "^0.41.1",
    "@cap-js/cds-types": "^0.19.0",
    "@sap/cds-dk": "^10.1.0",
    "@types/node": "^22.20.3",
    "tsx": "^4.23.13",
    "typescript": "^5.9.3"
  }
}
