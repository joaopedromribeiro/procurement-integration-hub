# Phase 8.6 — end-to-end security acceptance

Status: **PHASE 8.6 CLOSED; PHASE 8 CLOSED** on owner-supplied deployed acceptance evidence (2026-09-25 UTC). Phase 8.1–8.5 remain closed within their recorded scopes. Phase 9 Event Mesh has **not started**. This record relies on owner-supplied runtime output; Codex did not execute the CF tasks or browser request.

## Acceptance matrix

`VERIFIED WITH ENVIRONMENT LIMITATION` means the specified security check ran but a broader identity, audit, or scan claim is deliberately excluded. Local tests alone are never counted as deployed runtime proof.

| # | Boundary | Classification | Exact existing project evidence and limit |
| --- | --- | --- | --- |
| 1 | Anonymous → protected application | VERIFIED | [Project status, OI-14(2)](../PROJECT_STATUS.md#completed): deployed CAP routes answered 401 without a token. |
| 2 | Human supplier → SupplierService | VERIFIED | [CAP Phase 8.2 acceptance](../cap-supplier-portal/README.md#phase-82-interactive-runtime-acceptance): named `sap.default` user authenticated through approuter/XSUAA; `RTTEST001` Orders GET and detail succeeded. No human action mutation is claimed. |
| 3 | Human supplier → IntegrationService | VERIFIED | [CAP Phase 8.2 acceptance](../cap-supplier-portal/README.md#phase-82-interactive-runtime-acceptance): authenticated browser `/rest/integration/v1/Orders` returned Not Found at the router; [router allowlist](../cap-supplier-portal/app/router/xs-app.json) proxies only `/rest/supplier/v1/*`. This is browser-boundary evidence, not a direct CAP human-token 403 test. |
| 4 | IntegrationClient → IntegrationService | VERIFIED | [Project status, OI-14(2)](../PROJECT_STATUS.md#completed): deployed client-credentials token entered ingestion handler and received application `400 SCHEMA_VERSION_UNSUPPORTED`, not an XSUAA rejection. |
| 5 | IntegrationClient → SupplierService | VERIFIED | [Project status, OI-14(2)](../PROJECT_STATUS.md#completed): same technical token received 403 for missing `SupplierPortalUser`. |
| 6 | Supplier A → Supplier A data | VERIFIED | [CAP Phase 8.2 acceptance](../cap-supplier-portal/README.md#phase-82-interactive-runtime-acceptance): authenticated `RTTEST001` user saw only the current supplier order census and opened `PO00000122` detail. Only one supplier existed in that deployed census. |
| 7 | Supplier A → Supplier B data | VERIFIED | [Phase 8.6 owner-supplied runtime proof](#final-deployed-cross-supplier-proof): authenticated `RTTEST001` human directly requested portal order `b1c03e13-1b02-463a-8d1d-c5bb9de506ec`, independently verified as owned by active `RTSEC002`; SupplierService answered scoped HTTP 404 with no foreign fields, and the order remained unchanged. |
| 8 | SAP Buyer → Buyer actions | VERIFIED | [Phase 8.3 SAP runtime record](../PROJECT_STATUS.md#phase-8--current-security-checkpoint): on active SUBMITTED orders Buyer had update/delete/item association and Buyer actions enabled. |
| 9 | SAP Buyer → Approver actions | VERIFIED | [Phase 8.3 SAP runtime record](../PROJECT_STATUS.md#phase-8--current-security-checkpoint): Buyer approve/reject were false; UI OData action annotations agreed. |
| 10 | SAP Approver → Approver actions | VERIFIED | [Phase 8.3 SAP runtime record](../PROJECT_STATUS.md#phase-8--current-security-checkpoint): Approver approve/reject were true; UI OData action annotations agreed. |
| 11 | SAP Approver → Buyer actions | VERIFIED | [Phase 8.3 SAP runtime record](../PROJECT_STATUS.md#phase-8--current-security-checkpoint): Approver update/delete/item association and cancel/Edit/removeItem/submit were false. |
| 12 | CI integration identity → `applySupplierResponse` | VERIFIED WITH ENVIRONMENT LIMITATION | [Phase 8.4 B2](phase-8-4-technical-boundaries.md#b2-connectivity-incident-and-final-replay): after `ZJP_ROLE=INTEGRATOR` correction, SAP answered HTTP 204 on an already-applied response replay, including after 10,000 ms CSRF timeout restoration. `PIH_SAP_BASIC` still authenticates shared `ZHUB.USER`; not dedicated SAP-user least privilege. |
| 13 | Operator → `retryDelivery` authorization | VERIFIED WITH ENVIRONMENT LIMITATION | [Phase 8.5 SAP trace](phase-8-5-operator-secrets-audit.md): ACTIVE handler `ZJP_PIH`/`ZJP_ROLE=OPERATOR` returned RC 0. No action, negative cross-role test, or dedicated Operator identity was exercised. `GET PERMISSIONS` yielded no instance result and is not counted. |
| 14 | Worker → `recordDeliveryResult` authorization | VERIFIED WITH ENVIRONMENT LIMITATION | [Phase 8.5 SAP trace](phase-8-5-operator-secrets-audit.md): ACTIVE handler `ZJP_PIH`/`ZJP_ROLE=WORKER` returned RC 0. No action, negative cross-role test, or dedicated Worker identity was exercised. |
| 15 | SAP → CI OrderDelivery dedicated sender credential | VERIFIED | [Phase 8.4 A1](phase-8-4-technical-boundaries.md): dedicated client reached the iFlow/MPL; old shared client received 403 with no MPL. Final deployed export carries `ESBMessaging.send_OrderDelivery_RTCustomRole`. No new successful order delivery was claimed for that hardening test. |
| 16 | CAP → CI SupplierResponse dedicated sender credential | VERIFIED | [Phase 8.4 B1](phase-8-4-technical-boundaries.md): dedicated client reached the iFlow/MPL; old shared client received 403 with no MPL. Final deployed export carries `ESBMessaging.send_SupplierResponse_RTCustomRole`. |
| 17 | CI → SAP authenticated/authorized inbound path | VERIFIED WITH ENVIRONMENT LIMITATION | [Phase 8.4 B2](phase-8-4-technical-boundaries.md#b2-connectivity-incident-and-final-replay): `PIH_SAP_BASIC`, CSRF GET, bound POST, and final 204 confirmed the path. Shared Dialog user `ZHUB.USER` retains unrelated roles; full SAP-user least privilege and individual attribution are not claimed. |
| 18 | Obsolete shared technical credential retired | VERIFIED | [Phase 8.5 key-retirement record](phase-8-5-operator-secrets-audit.md): owner removed the old shared service key; the `pih-integration-runtime` instance was retained, while both dedicated instances remain. This is key retirement, not rotation of active credentials. |
| 19 | Secrets not stored in repository | VERIFIED WITH ENVIRONMENT LIMITATION | [Phase 8.5 targeted scan](phase-8-5-operator-secrets-audit.md): no confirmed real secret in targeted tracked source, documentation, ZIP members, and available history; `gitleaks` was unavailable, so no full forensic-history assurance is claimed. |
| 20 | Audit/correlation evidence | VERIFIED WITH ENVIRONMENT LIMITATION | [Phase 8.5 audit record](phase-8-5-operator-secrets-audit.md#accepted-audit-evidence) and [Phase 7 operational evidence](phase-7-operational-resilience.md): SAP/CAP durable delivery identity, attempts/history, and matching `X-Correlation-ID` across SAP and CAP. `STAUTHTRACE`/MPL are retention-limited; this is not an immutable compliance audit or durable per-denial record. |

## Supplier decisions: three distinct claims

1. **Deployed SupplierService mutations are proven.** [Phase 6.5f](../integration-suite/docs/phase-6-cloud-integration.md#8c-phase-65f-closed--every-decision-type-proven-on-the-real-path) records `SupplierService.accept`, `updateEstimatedDeliveryDate`, and `reject` committing against CAP/HANA. [Phase 7.3](phase-7-operational-resilience.md) also records a fresh `SupplierService.accept` through the existing business path, not a direct order update.
2. **End-to-end response delivery is proven.** Phase 6.5f records CAP outbox deliveries through Cloud Integration to SAP, with HTTP 204 for date update and SAP's terminal REJECTED outcome for reject; Phase 7 records further persisted delivery/attempt evidence.
3. **Human/XSUAA mutation is not proven.** Phase 8.2's production browser UI was intentionally read-only. This remains an accepted *trial-scope limitation*, not a new 8.6 closure blocker: the security contract being accepted is human identity/attribute propagation, server-side supplier scoping, role separation, and the already-proven deployed action/transport path. The actions use the same server-side `supplierOf(req)` and ownership-qualified order lookup as the read boundary; [local action-isolation tests](../cap-supplier-portal/test/supplier-service.test.ts) support the code path but are not runtime proof. Do not describe the human mutation path as runtime-verified or create a decision merely to remove this wording.

## Final deployed cross-supplier proof

The owner supplied the following runtime results. The pre-run CAP/HANA task `pih-p86-foreign-order-census` **SUCCEEDED** and returned `{"suppliers":[{"supplierCode":"RTTEST001","active":true}],"otherActiveSupplierOrders":[]}`. A second supplier/order was therefore necessary; no historical Phase 7 row was reused.

The CAP-aware, app-bound task `pih-p86-create-rtsec002` **SUCCEEDED** and returned `{"supplierCode":"RTSEC002","name":"Phase 8.6 Security Test Supplier","active":true,"created":true}`. The existing `IntegrationClient` OAuth identity then called the deployed `IntegrationService` ingestion path, which returned **HTTP 201** for one synthetic `PIH_SECURITY_TEST` order: external number `POSEC00001`, portal order ID `b1c03e13-1b02-463a-8d1d-c5bb9de506ec`, supplier `RTSEC002`. The Order was **not** inserted directly into HANA. No supplier decision or response transport was invoked.

The already accepted human BTP/XSUAA identity had `SupplierPortalUser` and effective `supplier=RTTEST001`. It directly requested `/rest/supplier/v1/Orders/b1c03e13-1b02-463a-8d1d-c5bb9de506ec`. SupplierService answered **HTTP 404**, body `{"error":{"message":"Not Found","code":"404"}}`. No `RTSEC002` data, `POSEC00001`, amount, items, or other foreign-order fields were disclosed. A 403 was **not** treated as equivalent: scoped 404 is the [ADR-029](../ARCHITECTURE.md) contract.

After the denial, the read-only CAP/HANA task `pih-p86-ownership-check` **SUCCEEDED**. It confirmed `RTSEC002` active, `ownedByRTSEC002=true` for the exact tested portal ID, `externalOrderNumber=POSEC00001`, `sourceSystem=PIH_SECURITY_TEST`, `status=RECEIVED`, `responseVersion=0`, `responseRows=0`, and `receivedAt=modifiedAt=2026-09-25T00:10:41.237Z`. Thus the foreign order still existed and the denied read had not changed its business/response state. This is **deployed** cross-supplier isolation evidence, not a promotion of the earlier local tests.

The labelled synthetic supplier `RTSEC002`, its `POSEC00001` order, and its ingestion receipt are **retained** to preserve ingestion/idempotency/audit evidence; they are not SAP business fixtures. The owner temporarily started `cap-supplier-portal-srv` and `cap-supplier-portal-router` for acceptance and was instructed to stop both afterward. **No final stopped-state confirmation was supplied**, so this record does not claim either app is now stopped.

## Closure limits

Every required matrix boundary is verified within the classifications above; none remains UNVERIFIED. The following are accepted limitations or follow-up debt, not Phase 8 closure blockers: shared SAP Dialog identity `ZHUB.USER` with unrelated roles; no separate OPERATOR/WORKER identities, negative cross-role isolation, or individual SAP attribution proof; targeted repository secret inspection rather than a forensic full-history scan because `gitleaks` was unavailable; retention-limited STAUTHTRACE/MPL rather than permanent security-event audit storage; and the unresolved moderate trial/approuter availability advisory, accepted only for a time-bounded test window. Phase 8.2 did not execute a human/XSUAA browser mutation; deployed SupplierService mutations and end-to-end response handling were separately proven by Phases 6/7. No Phase 7 lifecycle, retry, idempotency, lease, UNKNOWN, snapshot, history, or correlation behavior changed during Phase 8.6 documentation closure.

**PHASE 8.6 CLOSED. PHASE 8 CLOSED. Phase 9 Event Mesh NOT STARTED.**
