# Project status

Last updated: 2026-09-26 UTC.

## Phase 9 — Event Mesh checkpoint

**PHASE 9.5A — SAP TRIGGER ADAPTER LOCALLY VERIFIED.** The owner compiled and activated all five production objects and the trigger's Test Classes include in A4H. ABAP Unit passed **8 methods, 0 failures, 0 errors**: valid and duplicate identity, unknown delivery, missing order, stale revision, and PurchaseOrderUUID/OrderRevision/PayloadHash mismatches. The network-free tests use a dispatcher double, proving adapter validation and same-DeliveryUUID delegation semantics—not a real coordinator invocation, HTTP delivery, or broker settlement. The committed-state reader remains read-only and does not lock the order against a concurrent revision change. No RAP action, external HTTP endpoint, broker consumer, Event Mesh runtime, or second state machine was added. See the [9.5A adapter record](event-mesh/docs/phase-9-5-consumer-trigger.md).

**PHASE 9.5B — AUTHENTICATED HTTP TRIGGER VERIFIED END-TO-END IN THE INSPECTED DEV ENVIRONMENT.** The owner published A4H HTTP Service `ZJP_PO_EVENT_DISPATCH` with handler `ZCL_JP_PO_EVENT_DISPATCH`; the dedicated `ZJP_PIH` / `ZJP_ROLE=DISPATCH` check passed for the runtime user. Its network-free HTTP-boundary ABAP Unit suite passed **13/13**, and real GET, malformed POST, unknown delivery, identity conflict, and already-delivered `STATE_NOT_CLAIMABLE` probes verified rejection/skip boundaries. An initial blank item number reached CPI XSD validation and returned retryable `HTTP_502`/`PENDING` with one attempt and cleared lease; subsequent blank `product.code` and inactive `SUP033` requests were answered non-retryably and persisted `FAILED`. The controlled `RTTEST001`/`MAT001` fixture returned HTTP 200 `DELEGATED` with coordinator `claimed`, `dispatched`, and `recorded` true and `finalState=DELIVERED`; SAP persisted one attempt and no lease, CPI MPL was `COMPLETED` without intermediate error, and a read-only CAP/HANA task found the exact delivery as `RECEIVED` portal order `82745532-fbd1-4c6f-8024-1c1385bbfda4`. The successful path is **SAP → authorized HTTP trigger → existing coordinator → CPI → CAP/HANA**, not an Event Mesh run or exactly-once guarantee. The owner-supplied full ACTIVE HTTP handler/Test Classes, projection BDEF, and metadata extension are now mirrored. The two-read revision race and broker publication/ACK remain open. See the [complete 9.5 evidence](event-mesh/docs/phase-9-5-consumer-trigger.md). One reviewed commit is intended for all of 9.5, not one per subphase.

**PHASE 9.1 CLOSED WITH ENVIRONMENT LIMITATION.** Owner read-only checks in subaccount `PIH Integration` found no visible Integration Suite Event Mesh capability or assignable Event Mesh/`message-client` plan in the currently inspected BTP environment; this does not generalize to all Trial accounts. A4H `/IWXBE/CONFIG` exposed AMQP channels/outbound bindings; those shared channels/bindings are not PIH-owned. Cloud Integration exposed an AMQP adapter, not a configured consumer. **PHASE 9.2 CLOSED for local contract/consumer semantics only:** 11 deterministic simulator tests passed, including all ten required duplicate/redelivery/ordering cases; TypeScript checking and `git diff --check` passed. **PHASE 9.3 IMPLEMENTED AND LOCALLY VERIFIED in A4H** for RAP producer emission only. **PHASE 9.4 EVENT BINDING ACTIVE / EXTERNAL PUBLICATION ENVIRONMENT-BLOCKED:** owner activated PIH-owned `ZJPEBPODLVREQ`, mapping `ZJP_I_DELIVERYINTENT~PURCHASEORDERDELIVERYREQUESTED` to generated external type `zjp.PurchaseOrder.DeliveryRequested.v*` (version `0001 / 0 / 0`). No PIH-owned broker/service key, channel, outbound binding, publication, queue receipt, or consumer is verified. See the [9.1 foundation](event-mesh/docs/phase-9-1-foundation.md), [9.2 contract proof](event-mesh/docs/phase-9-2-event-contract.md), [9.3 SAP producer evidence](event-mesh/docs/phase-9-3-rap-producer.md), and [9.4 binding evidence](event-mesh/docs/phase-9-4-event-binding.md). Phase 8 remains CLOSED and proven Phase 6–8 HTTP paths remain unchanged.

**PHASE 9.3 RAP PRODUCER IMPLEMENTED AND LOCALLY VERIFIED.** The owner activated `ZJP_A_PODeliveryRequested`, `ZJP_I_DeliveryIntent` managed additional save/event, and the CREATE-only `ZBP_I_DELIVERYINTENT` saver in A4H. The compiler rejected duplicate `DeliveryUUID` in `%param`; the active event uses DeliveryUUID as entity key and PurchaseOrderUUID/OrderRevision/PayloadHash in `%param`. A CREATE+UPDATE-in-one-LUW debugger probe showed the final PayloadHash consolidated in `create-deliveryintent` at `save_modified` on this target. RAP Event TDF ABAP Unit finished **3 methods, 0 failures, 0 errors**: one CREATE event with exact identity payload, zero on empty CREATE, zero on UPDATE-only. This is saver/local evidence, **not** proof of external publication, rollback of external events, or a broker. The repository SAP mirrors reflect the owner-supplied active source shape; no Phase 7 retry/UNKNOWN/lease/coordinator behavior, `/IWXBE/` channel, or transport was changed. The Event Binding was activated separately in 9.4; real Event Mesh runtime remains blocked by the currently inspected account's entitlement.

## Phase 8 — current security checkpoint

- **PHASE 8 CLOSED.** 8.1 identity/access contract: CLOSED. 8.2 interactive supplier login: CLOSED within read-only runtime acceptance. 8.3 RAP Buyer/Approver authorization: CLOSED. 8.4 technical-client/iFlow boundary hardening: CLOSED. 8.5 operator access/secrets/audit: CLOSED. **8.6 end-to-end security acceptance: CLOSED** on owner-supplied deployed cross-supplier scoped-404 and post-denial ownership/state evidence. All 20 required matrix boundaries are verified within their stated limits; no new Phase 7 behavior was introduced. Phase 9 remains Event Mesh; 9.1 discovery is closed with the account limitation and 9.2 is local-only, with no runtime implementation. See the [8.6 final acceptance matrix](docs/phase-8-6-end-to-end-security-acceptance.md), [8.4 evidence and limits](docs/phase-8-4-technical-boundaries.md), and [8.5 closure record](docs/phase-8-5-operator-secrets-audit.md).
- Phase 8.3 SAP authorization object `ZJP_PIH` has field `ZJP_ROLE` with final technical value `INTEGRATOR` alongside `BUYER`, `APPROVER`, `OPERATOR`, and `WORKER`. The PFCG role **name** remains `ZJP_PIH_INTEGRATION`; the other role names are `ZJP_PIH_BUYER`, `ZJP_PIH_APPROVER`, `ZJP_PIH_OPERATOR`, and `ZJP_PIH_WORKER`. The relevant human roles also received UI OData V4 service authorization (`R3TR` / `G4BA` / `ZJP_UI_PURCHASEORDER_O4`). These are SAP runtime/role configuration facts, not repository-managed PFCG objects. The activated base PurchaseOrder BDEF uses global plus instance authorization; global create requires Buyer, and instance authorization independently checks Buyer for update/delete/Edit/removeItem/submit/sendToSupplier/cancel, Approver for approve/reject, `INTEGRATOR` for applySupplierResponse, Operator for retryDelivery, and Worker for recordDeliveryResult. Phase 8.4 `STAUTHTRACE` found the former `INTEGRATION` check failed with return code 4 because the domain fixed value was stored as `INTEGRATIO`; ACTIVE ABAP and the repository mirror now check `INTEGRATOR`. Business lifecycle, prechecks, idempotency, and Phase 7 state transitions remain separate from authorization.
- Real `ZJP_UI_PURCHASEORDER_O4` results on active SUBMITTED orders: Buyer had `_Items` create-by-association, Updatable, and Deletable = true; approve/reject = false; cancel/Edit/removeItem/submit = true. Approver had `_Items` create-by-association, Updatable, and Deletable = false; approve/reject = true; cancel/Edit/removeItem/submit = false. OData action annotations agreed with these controls. This is Buyer/Approver runtime evidence; Phase 8.5 separately proved the ACTIVE Operator and Worker authorization checks through positive `STAUTHTRACE`, not separate technical identities or negative isolation. The technical `INTEGRATOR` action authorization was functionally proven in 8.4, but its shared SAP Dialog identity is not least privilege. Authorization denials are intended to occur before any DeliveryIntent, attempt count/history, retry-window, lease, UNKNOWN, correlation, supplier-response, or HTTP mutation; Phase 7 behavior itself was not changed by this mirror synchronization.
- Phase 8.4 final runtime boundary: A1 uses dedicated `pih-orderdelivery-runtime` with `ESBMessaging.send_OrderDelivery_RTCustomRole`; B1 uses dedicated `pih-supplierresponse-runtime` with `ESBMessaging.send_SupplierResponse_RTCustomRole`. Each new client reached its own iFlow; the old shared client received 403 with no MPL at each hardened sender. A2 remains the verified XSUAA `IntegrationClient`-only client; B2 returned HTTP 204 on an already-applied version-2 response replay after the SAP authorization value correction and again after the CSRF GET timeout was restored to 10,000 ms. The earlier CSRF timeout was caused by SCC host DNS failure on the application tunnel, not application code. B2's `PIH_SAP_BASIC` authenticates shared Dialog user `ZHUB.USER`, which retains unrelated roles: only the PIH-specific authorization is functionally verified, **not end-to-end SAP least privilege**. Phase 8.5 removed the obsolete shared service key under `pih-integration-runtime`; the instance and both active dedicated instances remain. Both sender CSRF settings remain disabled and were accepted for this M2M scope. The owner replaced both tracked ZIPs with final deployed exports; XML inspection verified their dedicated sender roles and the SupplierResponse 10,000 ms CSRF GET timeout.
- Phase 8.5: `STAUTHTRACE` on 2026-09-24 showed `ZHUB.USER`, entry point `SADT_REST_RFC_ENDPOINT`, program `ZBP_I_PURCHASEORDER===========CP`, successful `ZJP_PIH`/`ZJP_ROLE=OPERATOR` at 18:57:44.378 (RC 0) and `ZJP_ROLE=WORKER` at 18:57:44.380 (RC 0). The attempted read-only `GET PERMISSIONS` probe returned no instance result and is **not** the positive proof. Neither business action was executed and no Phase 8.5 business state was mutated. Existing Phase 7/8 durable and correlated audit evidence was accepted; `STAUTHTRACE`/MPL are retention-limited. The targeted repository secret scan found no confirmed real secret, but gitleaks was unavailable. Shared `ZHUB.USER` and unrelated roles prevent a claim of separate identities, negative cross-role isolation, or individual attribution.
- The existing `cap-supplier-portal-auth` XSUAA instance and generated `SupplierPortalUser` collection remained. A named `sap.default` human user has `SupplierPortalUser` and static `supplier=RTTEST001` through `PIH_RTTEST001_SupplierAttribute` (the existing `userattributes` template) in the dedicated `PIH_RTTEST001_Attribute_Test` collection. An unintended human assignment to the generated `IntegrationClient` collection was removed before acceptance; that collection itself was not deleted or structurally changed. Final intended human authority: supplier role and attribute, but no `IntegrationClient`.
- The MTA was built as `mta_archives/cap-supplier-portal_0.1.0.mtar`. Partial deployment selected `cap-supplier-portal-router` and existing `cap-supplier-portal-auth`: XSUAA was updated and bound, and the router reached started, 1/1, ready. CAP srv was not redeployed/restaged; the HDI deployer did not run; no service was deleted. A fresh Chrome session completed browser → approuter → XSUAA/sap.default → CAP login. The read-only UI showed “Authenticated with SAP BTP” and no mock sign-in. Supplier Orders GET returned the current `RTTEST001` order census. Detail navigation to `PO00000122` returned RECEIVED, EUR 1500.00, response version 0, and item 10/MAT001 (2.000 PCE at EUR 750.0000, line EUR 1500.00). The unchanged SupplierService denies missing/unknown supplier identity before returning orders, so this is application-behavior evidence for usable `req.user.attr.supplier=RTTEST001`, not a decoded-token claim. No decision or other data mutation was executed. The authenticated browser received Not Found for `/rest/integration/v1/Orders` through the router, proving it is not a generic CAP proxy. Earlier technical-client 403 at SupplierService remains separate machine-role evidence; no new machine test was run. An initial Edge SAP ID processing error was not reproduced in Chrome and is not diagnosed as a product/configuration defect.
- The owner accepted the unresolved moderate availability advisory `GHSA-vcc3-ghjq-m6fr` / `CVE-2026-45822` (`@sap/approuter@23.0.0` → `query-string@7.1.3` → `decode-uri-component@0.2.2`) **only for this time-bounded trial acceptance**, not as remediation. `cf stop cap-supplier-portal-router` returned OK; follow-up showed requested state stopped and instances 0/1, while the process snapshot still said `stopping`. No later fully terminated snapshot was supplied; intended state is STOPPED.
- Phase 8.6 final runtime proof: the pre-run CAP/HANA census had only active `RTTEST001` and no foreign order. The owner created active, labelled `RTSEC002` through a CAP-aware task and one `POSEC00001` order through the deployed `IntegrationClient`/IntegrationService ingestion path (HTTP 201), portal ID `b1c03e13-1b02-463a-8d1d-c5bb9de506ec`. The authenticated `RTTEST001` human requested that exact foreign order through SupplierService and received **HTTP 404** with body `{"error":{"message":"Not Found","code":"404"}}`, disclosing no foreign fields. A succeeding read-only task confirmed `RTSEC002` ownership, `RECEIVED`/version 0, no response row, and `modifiedAt=receivedAt=2026-09-25T00:10:41.237Z`. The synthetic supplier, order, and ingestion receipt are intentionally retained. The owner temporarily started `cap-supplier-portal-srv` and `cap-supplier-portal-router`; instructions to stop both were given, but **no final stopped-state evidence was supplied**.
- Accepted limits remain: Phase 8.2's **human/XSUAA browser** scope was read-only, so no human browser mutation is claimed; deployed `SupplierService.accept`, `reject`, and `updateEstimatedDeliveryDate` mutations and CAP → Cloud Integration → SAP delivery were separately runtime-proven in Phase 6/7. Shared SAP `ZHUB.USER` does not prove separate OPERATOR/WORKER identities, full least privilege, or individual attribution. The targeted secret scan was not a full `gitleaks` history scan; STAUTHTRACE/MPL are retention-limited; the upstream approuter advisory remains unresolved and was accepted only for a time-bounded trial window. Phase 7 business, retry, delivery, UNKNOWN, lease, history, correlation and recovery behavior was unchanged. The earlier unchanged local suite passed **235/235 tests, 45 suites, zero failed/cancelled/skipped/todo**; typecheck, production CDS build, router build, generated production-resource checks and `git diff --check` passed at that checkpoint.

## Phase 7 CLOSED — repository and runtime-evidence checkpoint

- Phase 7.5 deployed concurrent-drainer and expired-lease reclaim proofs passed: one claim owner, one competing skip, one transport attempt/history entry per fixture, and cleared lease after reclaim.
- Phase 7.6 recovery mechanism passed on the existing UNKNOWN intent `37FC3FA8EB2D1FE1ADD3A6790C9BD871`: same identity, one `retryDelivery` preparation and one coordinator dispatch, attempts 1 → 2, answered HTTP 400 `UNKNOWN_SUPPLIER`, correctly persisted FAILED. The same correlation ID reached CAP. UNKNOWN → DELIVERED was not demonstrated: remaining UNKNOWN snapshots reference SUP044, SUP074A or SUP074H, none active in CAP; neither supplier master nor snapshots were altered.
- Phase 7.7 SAP and CAP read-only reporters are runtime-proven. Final SAP census: PENDING 85, IN_FLIGHT 5, DELIVERED 24, FAILED 32, UNKNOWN 21, live leases 0, stale leases 5. Final CAP census: DELIVERED 5, UNKNOWN 1, PENDING 3, retryWindowBlocked 3, live/stale leases 0/0, historyCount 5. Historical SAP stale leases and the Phase 7.2 CAP UNKNOWN fixture remain untouched.
- ACTIVE SAP sources were manually synchronized into the repository. Both final deployed Integration Suite exports are synchronized and inspected: OrderDelivery is consistent; SupplierResponse final export verification passed with the normal CSRF service root and no OQ-2 fault injection. The recovery runner remains fail-closed when more than one UNKNOWN exists.
- The complete unchanged CAP suite passed outside the Codex host: **231/231 tests, 44 suites, 0 failed/cancelled/skipped/todo**, duration 6865.0322 ms. `npm run typecheck`, `npx cds build --production` and `git diff --check` passed. Windows `uv_os_get_passwd` ENOMEM in the Codex host was an environment-only launcher limitation, not a test failure; the full external pass removed the last validation blocker. See the Phase 7 guide and runbook for the exact runtime evidence and scope limits.

## Completed

- Phase 0 architecture, business ownership and complete phase roadmap.
- RAP header/item and CAP logical domain models, draft distinction and lifecycle rules.
- Proposed API boundaries, mapped payload examples, receipts and supplier-response contract.
- Transaction boundaries, retry/idempotency design and error policy.
- Local-versus-SAP environment matrix and future account/service prerequisites.
- Initial README, architecture, API contracts, learning journal and review exercise.
- Repository folders reserved for later source artifacts; local Git initialization.
- Learner confirmed Phase 0 complete and selected SAP S/4HANA with ABAP Cloud for Phase 1.
- Phase 1 local source preparation: ZJP_PO_H, ZJP_PO_I, ZJP_I_PurchaseOrder, ZJP_I_PurchaseOrderItem, ZJP_C_PurchaseOrder and ZJP_C_PurchaseOrderItem.
- Prepared the [step-by-step ADT guide](abap-rap/docs/phase-1-domain-model.md), including complete source, purpose, object names, explanations and tests for each object.
- Updated the architecture, domain model, API compatibility notes, environment record, README and learning journal.
- Learner manually created and successfully activated all six Phase 1 persistence/CDS objects in SAP S/4HANA using ADT/Eclipse. Phase 1 is complete on that reported activation evidence.
- Reconciled the root currency annotation and child projection provider-contract clause to the two working compiler adjustments; synchronized the Phase 1 guide's full listings.
- Confirmed the configured GitHub remote and that local main tracks origin/main.
- Phase 2.1–2.3: corrected managed BDEF, minimal behavior pool/stub and successful SAP EML CRUD/composition runtime test, supported by the learner's console output.
- Phase 2.7A: root business action `submit`, business `Status` `DRAFT` → `SUBMITTED` on active instances only, runtime-verified on SAP with its draft, status and empty-order rejections and the full Phase 2.5/2.6 regression.
- Phase 2.7B: post-submission commercial immutability through RAP prechecks on root update, item update and `_Items` create-by-association, plus a status guard in `removeItem`, runtime-verified on SAP for active and technical draft instances.
- Phase 2.7C: approver decisions `approve` and `reject` with a mandatory rejection reason, plus the generalized lifecycle invariant that makes every state after `DRAFT` commercially immutable, runtime-verified on SAP.
- Phase 2.7D-1: the `cancel` lifecycle action, carrying `DRAFT`, `SUBMITTED` and `APPROVED` to the terminal `CANCELLED`, runtime-verified on SAP with no new precheck code.
- Phase 2.7D-2: physical root `DELETE` narrowed to business `Status` `DRAFT` through `delete ( precheck )`, with terminal fixtures kept as deliberate test residue, runtime-verified on SAP.
- Phase 2.7E: `PurchaseOrderNumber` allocation on a successful `submit`, from number range object `ZJP_PO` interval `01`, formatted `PO` plus eight digits, runtime-verified on SAP. This closed OI-02 and completed Phase 2.
- Phase 3.1: OData V4 exposure through the projection behavior definition on `ZJP_C_PurchaseOrder`, service definition `ZJP_UI_PURCHASEORDER` and service binding `ZJP_UI_PURCHASEORDER_O4`, runtime-verified end to end over HTTP.
- Phase 3.2: metadata extensions on `ZJP_C_PurchaseOrder` and `ZJP_C_PurchaseOrderItem` at layer `#CUSTOMER`, runtime-verified in Fiori Elements Preview for the List Report, Filter Bar, header info, General Information facet and Items facet.
- Phase 4.1: CAP Supplier Portal foundation — CAP 10 on Node.js 22 with TypeScript, SQLite in memory, pinned lockfile, local scripts and a non-business health service; verified by local execution of install, typecheck, test and server start, not by SAP.
- Phase 4.2: CAP persistence and domain model — namespace `pih.portal` with `Suppliers`, `Orders` and composed `OrderItems`, portable CDS types, database unique constraints, CSV fixtures and generated `#cds-models` types; verified by local execution of a clean install, typecheck and a 9-test suite, not by SAP.
- Phase 4.3: CAP integration-facing order ingestion — `POST /rest/integration/v1/Orders` on an insert-only `IntegrationService` that exposes no persistence, with the nested contract DTO, 20 HTTP-reachable boundary validation codes on exact decimal arithmetic, supplier resolution, and delivery idempotency enforced by a database unique constraint with 201/200/409 semantics; verified by local execution of a clean install, typecheck, a 34-test suite and manual HTTP scenarios, not by SAP.
- Phase 4.4: CAP supplier-facing service and supplier decisions — a `SupplierService` separate from `IntegrationService`, `@readonly` projections narrowed by a row filter, bound `accept`/`reject`/`updateEstimatedDeliveryDate` actions with `responseId` replay detection and a `responseVersion` precondition, and a pending outbound response written in the same transaction as every decision; verified by local execution of a clean install, typecheck, a 63-test suite and eight manual HTTP scenarios, not by SAP.
- Phase 4.5: minimal supplier UI — plain HTML/CSS/ES modules served from `app/` with no framework and no build step, listing a supplier's own orders with bounded `limit`/`offset` pagination, opening an order with its lines, and submitting accept, reject and delivery-date commands through the Phase 4.4 bound actions with a fresh `responseId` and the loaded `responseVersion`; verified by local execution of a clean install, typecheck, an 87-test suite and eleven browser scenarios, not by SAP.
- Phase 4.6: Phase 4 closure — clean-install re-verification (87 tests, 20 suites, 0 fail; 5 tables, 7 unique constraints), a live smoke test across all four surfaces, contract reconciliation that found no implementation defect, and five documentation-drift corrections. No new capability was added; Phase 4 is complete.
- Phase 5.2b: DeliveryIntent persistence and RAP business object — **SAP runtime-verified.** Six objects: `ZJP_PO_DLV` (16 fields exactly as designed in Phase 5.1 §4.2, `payload_snapshot` kept as `abap.string`), the unique Table Index **`ZJP_PO_DLV~REV`** over `CLIENT` + `PURCHASE_ORDER_UUID` + `ORDER_REVISION`, `ZJP_I_DeliveryIntent`, `ZJP_I_DELIVERYINTENT` (managed, `strict ( 2 )`, **no draft**), `ZBP_I_DELIVERYINTENT` (skeleton, `get_instance_authorizations` only) and a `ZJP_CL_PO_EML_TEST` extension. Resolves blocker **B7** and answers probes **P8** and **P9**. Verified by the full EML suite: an intent created through EML with a framework-generated `DeliveryUUID`, readable in the buffer with `DispatchState = PENDING`, committed with `sy-subrc = 0` and empty `FAILED`/`REPORTED`, read back from `ZJP_PO_DLV` with `OrderRevision = 1` and the ADR-015 initial values intact on `PortalOrderUUID`, `LastCorrelationId`, `LeaseOwner` and `LeaseExpiresAt`, and exactly one intent for that order revision. Three compiler corrections were taken on evidence: a **total etag needs `with draft`** so the non-draft BO uses plain `lock master`; the 30-character method-name limit forced `TEST_DLV_INTENT_PERSIST`; and `field ( readonly, numbering : managed ) DeliveryUUID` makes a caller-supplied duplicate key a **compile** error rather than a runtime case. The unique index is verified **manually**, not by the suite — a violation short-dumps rather than returning `FAILED`. **No projection, no OData binding, no draft, no HTTP, no transport, no mapper, no dispatcher, no `sendToSupplier`, no revision increment.**
- Phase 5.2c: outbound transport abstraction — **SAP runtime-verified.** Three objects: `ZJP_IF_OUTBOUND_TRANSPORT`, the seam, implemented exactly as Phase 5.1 §6.2 specifies it and using nothing but `abap_bool` and built-in types, so it depends on no released API; `ZJP_CL_TRANSPORT_FAKE`, a deterministic fake configured by constructor injection with no factory, registry or setter; and `ZJP_CL_TRANSPORT_TEST`, a dedicated console harness added to the object inventory as object 13 by explicit decision. All four scenarios passed through a `REF TO ZJP_IF_OUTBOUND_TRANSPORT` — CREATED `201`, REPLAYED `200` with a byte-identical receipt, CONFLICT `409` carrying `DELIVERY_PAYLOAD_CONFLICT` and still `answered`, and UNANSWERED with `answered = false`, status `0` and diagnostic text — with no exception, no network, no SQL, no EML and no commit. First repository use of the ABAP `CS` operator, which compiled and ran. `ZJP_CL_PO_EML_TEST` untouched; `ZJP_CL_PO_DISPATCH_TEST` still reserved for 5.2g.
- Phase 5.2d: outbound builder and CAP mapper — **SAP runtime-verified.** Three objects: `ZJP_CL_ORDER_DELIVERY_BUILDER`, which reads one **active** PurchaseOrder through EML and returns a canonical snapshot keeping SAP-native values, read-only and proven so by identical row counts either side of the call; `ZJP_CL_CAP_ORDER_MAPPER`, which turns that snapshot into the verified seam's `TY_REQUEST` and owns every wire concern — C36 UUIDs, the three decimal scales, `NUMC 00010` → JSON `10`, header currency propagated onto each line, and `EA` → `PCE` by lookup with rejection; and `ZJP_CL_BUILDER_TEST`, object 14, a dedicated harness added by explicit decision. It produced the golden CAP payload **byte for byte**, twice, and refused unit `BOX` with `UNSUPPORTED_UNIT` and an initial request. Probes **P5**, **P6** and **P10** were answered first — `CL_SYSTEM_UUID=>CONVERT_UUID_X16_STATIC`, `NUMBER = RAW DECIMALS = n`, and the `XCO_CP_JSON` builder with explicit member names. `deliveryId` and `sourceSystem` are **inputs**, so no intent is created and blocker B4 stays open; `OrderRevision` is copied, demonstrated on a pre-5.2a order carrying `0`.
- Phase 5.2e: real HTTP transport — **SAP runtime-verified.** Two objects: `ZJP_CL_OUTBOUND_TRANSPORT`, the only object in the design permitted to touch an HTTP API, implementing the verified seam over the new path-less base destination `ZJP_CAP_BASE` with the route supplied by `TY_REQUEST-PATH` through `SET_URI_PATH`; and `ZJP_CL_HTTP_TRANSPORT_TEST`, object 15, the only test object in this project that makes real network calls. SAP posted to the deployed portal over TLS and OAuth for **201**, an idempotent **200** returning the identical `portalOrderId` and `receivedAt`, and **409 `DELIVERY_PAYLOAD_CONFLICT`** with the receiver body intact — all three `answered = true` — plus an unresolvable destination classified as `answered = false`, status `0`. Probe **P15** was answered first. The adapter forwards headers generically, sends the body unchanged, holds no SQL, EML, `COMMIT` or `ROLLBACK`, and generates no correlation UUID; the seam is unchanged. An earlier HTTP 500 on this path turned out to be a **stopped `pih-hana` database**, not transport logic, and nothing in the code was changed to resolve it.
- Phase 5.2f: `sendToSupplier` and the durable delivery intent — **SAP runtime-verified.** One action on `ZJP_I_PurchaseOrder`, parameterless and active-instance only, plus `ZJP_CL_DLV_SNAPSHOT_JSON` (object 16) for the source snapshot; no new object beyond that. It creates or reuses exactly one intent per `(PurchaseOrderUUID, OrderRevision)` inside the order's own LUW — cross-BO EML create, managed key from `MAPPED`, builder, serializer, SHA-256, then the intent and the header updated together — with **no HTTP, no commit and no rollback** in handler code. Eight branches passed: new intent, `PENDING` replay, `FAILED` retry, non-approved refusal, technical-draft refusal, `IN_FLIGHT` no-op, `DELIVERED` informational no-op with no `SENT` transition, and `UNKNOWN` retry. Business `Status` is never written. Blocker **B4** is resolved for DEV with multi-environment configuration deferred. `sendToSupplier` is deliberately **not** exposed through the projection.
- Phase 5.2a: PurchaseOrder header integration values — **SAP runtime-verified**, the first real Phase 5.2 implementation slice. `submit` now writes `OrderRevision = 1` in the same local-mode `UPDATE FIELDS` clause as `Status` and `PurchaseOrderNumber`, and the existing `initializeStatus` determination now writes `IntegrationStatus = 'NOT_REQUESTED'` alongside `Status = 'DRAFT'`. This resolves **blocker B1** and **blocker B2 for new orders**. Two existing classes changed — `ZBP_I_PURCHASEORDER` and `ZJP_CL_PO_EML_TEST` — with no new object, no behavior-definition change, no table change and no CAP change. Verified by the full EML suite: revision `0` before submit, `1` in the buffer immediately after submit and still `1` after commit, `0` preserved on a submit refused for having no items, `1` preserved on a refused re-submit, `NOT_REQUESTED` on every newly created fixture, the `PO########` format retained, and the whole pre-existing lifecycle suite running through its final summary with no `STOP`. Legacy pre-Phase-5 rows keep their blank `IntegrationStatus` and are intentionally not mass-updated. No revision increment exists; 5.2a establishes only the initial frozen revision. `DeliveryIntent`, `sendToSupplier`, the `DeliveryId` lifecycle and any `IntegrationStatus` transition beyond `NOT_REQUESTED` are **not** implemented.
- First SAP S/4 → CAP authenticated outbound runtime probe — **a technical probe, not `sendToSupplier`.** It proves the pipe end to end: SAP ABAP → HTTP destination → TLS → OAuth2/XSUAA → CAP `IntegrationService` → HANA ingestion. Destinations `ZJP_CAP` (`/health/ping`) and `ZJP_CAP_INGEST` (`/rest/integration/v1/Orders`), OAuth2 client profile `ZJP_CAP_OAUTH` with configuration `ZJP_CAP_XSUAA` on client credentials, and a token carrying `<xsappname>.IntegrationClient`. TLS needed work: the first handshake failed with `SSSLERR_PEER_CERT_UNTRUSTED` and importing **DigiCert TLS RSA4096 Root G5** into the ANONYM SSL client PSE resolved it. Evidence: connection test **401 before OAuth** — decisive, because only CAP can produce a 401, so DNS, routing, TLS and the application were proven by a failure — then **200** after; ABAP code reading `/health/ping` for **200** `status UP`; a `{}` POST returning the portal's own **400 `SCHEMA_VERSION_UNSUPPORTED`**, proving the request entered the handler; and a valid hand-built delivery returning **201 RECEIVED**, an exact repeat returning **200** with the same `portalOrderId` and `receivedAt`, and a same-`deliveryId` changed payload returning **409 `DELIVERY_PAYLOAD_CONFLICT`** with the stored order unchanged. This resolves **Phase 5.1 blocker B3** and answers probes **P1, P2/P3, P4 and P12**. **It does not start Phase 5.2**: the payload was hand-built, so no RAP order is mapped, `OrderRevision` is unpopulated, the `DeliveryId` and `IntegrationStatus` lifecycles do not exist, `sendToSupplier` does not exist, no HTTP outcome is persisted back into RAP, and there is no retry or reconciliation.
- First SAP HANA runtime verification, ingestion path — one valid order ingested through the deployed application with a real XSUAA token and then proven present in the `pih-hdi` container, the first time this project exercised HANA rather than inspecting its generated DDL. The deployed production profile resolves to `HANAService`; the container was found **completely empty** (0 suppliers, 0 orders, 0 items, 0 receipts), which runtime-proves the fixture exclusion of item 3; `POST /rest/integration/v1/Orders` returned **201** and HANA then held exactly one Order, one OrderItem and one DeliveryReceipt, with the supplier association resolving and decimal scale and Timestamp precision surviving the round trip; an exact replay and a byte-different but business-equivalent replay both returned **200** with the same `portalOrderId` and the original `receivedAt`; and a changed payload on the same `deliveryId` returned **409 `DELIVERY_PAYLOAD_CONFLICT`** leaving the stored rows byte-identical. The HANA catalog contains no `IntegrationService.Orders` table, which runtime-confirms ADR-034. No HANA-specific defect was found. **The supplier decision/update path remains unverified**, because every write exercised here was an INSERT.
- First SAP BTP Cloud Foundry deployment and authenticated runtime verification — `cap-supplier-portal-srv` is started and 1/1 running on Trial `us10-003` space `dev` behind a stable route, on SAP HANA through the reused `pih-hdi` container with no duplicate database or container created, and bound to a real XSUAA instance `cap-supplier-portal-auth`. One startup defect was found and fixed: `npm start` ran the dev-only `cds-tsx`, so production crashed with exit 127 until `start` became `cds-serve`. Verified against the live application: anonymous 401 everywhere, an authenticated technical token reaching `/health/ping` with 200 and the ingestion handler with its own 400, and 403 on the supplier surface for lack of `SupplierPortalUser`. **No business row had touched HANA at that point**, so HANA runtime behaviour was still unverified; the HANA runtime verification recorded above has since closed that for the ingestion path only.
- XSUAA production security preparation and a real BTP client-credentials probe — mocked authentication eliminated from the production profile, named `SupplierPortalUser` and `IntegrationClient` roles replacing `authenticated-user`, the previously unauthenticated ingestion endpoint closed, and `xs-security.json` generated from the annotations. A temporary XSUAA instance in Trial `us10-003` space `dev` proved at the token level that a scope and role-template alone do **not** reach the application's own technical client and that top-level `authorities` is required; supplier and integration scopes stay isolated in a technical token. Deployed enforcement, the supplier attribute mapping and SAP's own OAuth configuration remain unverified, so OI-14 item 2 is **partially resolved and still open**.
- OI-14 deployment-readiness cleanup, items 1 and 3 — the empty persistence artifact generated for the `IntegrationService.Orders` wire contract is suppressed with `@cds.persistence.skip`, and the three fixture CSVs moved to CAP's development-only `test/data` folder so no synthetic data reaches a production build. Together these take the HANA build from 7 tables and 3 `.hdbtabledata` files to 6 tables and none, with the OData contract byte-identical, local fixture seeding unchanged and 87 tests passing throughout. Build-output evidence only; at that point items 2 and 4 stayed open and no HANA runtime was verified. *Both have since moved: item 2 is resolved, and item 4's ingestion path is verified while its supplier decision/update path stays open.*
- CAP HANA production preparation — the first CAP cloud-deployment preparation step. The portal now selects its database by CAP profile: SQLite `:memory:` for local development, SAP HANA through `@cap-js/hana` for production, and `cds build --production` generates the HANA DB build task and an HDI deployer module. Verified by local execution — 87 tests in 20 suites with 0 failures, a clean typecheck and a passing production build — **not** by any HANA runtime, because nothing has been deployed or bound.
- Phase 5.1: SAP ↔ CAP direct integration prerequisites and outbound foundation — **design and evidence review only, not runtime-verified and not implemented.** Reviewed network reachability and compared the two permitted development options without choosing one; inventoried the nine existing RAP integration fields and found none missing but two carrying unusable values; designed the `DeliveryIntent` persistence, the outbound DTO mapping trace and a transport abstraction; analysed the `sendToSupplier` transaction boundary; and recorded seven blockers and twelve required ADT probes. No ABAP object was created, no source changed and no HTTP request was made.

The learner reports Phase 2.4C runtime-verified complete: header totals 1900 → 2650 → 2800 → 2400 → 0, successful relevant COMMIT ENTITIES calls (sy-subrc 0), and `PASS: header totals 1900/2650/2800/2400/0; cleanup complete.` The Phase 2.4C persisted-parent lookup did not support uncommitted-item deletion; Phase 2.6 replaces it with the runtime-verified root `removeItem` path for active and draft instances. Phase 2.5A validateSupplier is also runtime-verified: blank Supplier create/update are rejected during save with `Supplier is required.`, negative commits return nonzero sy-subrc, persistence remains unchanged, and the valid regression flow passes. No independent SAP execution by the assistant is claimed.

## Historical phase checkpoints

Paragraphs below preserve earlier checkpoints, newest first; the Phase 7 closure section at the top is the current state. Earlier prepared/not-deployed wording describes its dated checkpoint, not the present repository or runtime.

**PHASE 7.4a AND 7.4b — CAP RETRY ELIGIBILITY, TRANSPORT CERTAINTY AND RETRY-AFTER — ARE IMPLEMENTED AND LOCALLY VERIFIED, NOT DEPLOYED. Phase 7.4 is not complete.** The Phase 7.4a four-attempt budget, durable jittered due times, inclusive 15-minute window, derived diagnostics, legacy-null behavior and atomic history remain unchanged. Phase 7.4b adds optional unanswered certainty: exact `ENOTFOUND`, `ECONNREFUSED` and five explicit TLS certificate-verification codes become `NOT_SENT` → `PENDING` / `TRANSIENT`; timeouts, aborts, resets, missing codes and unknown values conservatively remain `MAY_APPLY` → `UNKNOWN` / `NO_ANSWER`. Answered HTTP classification is unchanged. The HTTP adapter preserves the complete raw `Retry-After` only through 128 characters and discards overlong values; the pure policy parses nonnegative delta-seconds or canonical IMF-fixdate, ignores malformed/negative/empty/overflowing values, and selects the later of that due and normal jittered policy only for answered `429`, `502` and `503`. The winning `cds.tx` still commits parent timing and one history row together; a CAS loser and dry-run write nothing. Local evidence: typecheck clean; **224 tests in 42 suites, 0 failures**; production build clean. No SAP, Integration Suite, Cloud Foundry, HANA runtime, deployment or network action occurred. **7.4c SAP parity, 7.4d Integration Suite/timeouts and 7.4e deployed acceptance remain.**

**PHASE 7.3 — ATTEMPT HISTORY — IS COMPLETE AND RUNTIME-VERIFIED.** Executed 2026-09-21. **This is the first Phase 7 subphase that changed executable source**, on both legs: a `SupplierResponseDeliveryAttempts` composition child on the CAP inbound path, and one `attempt_count` column on `ZJP_PO_DLV` surfaced as `AttemptCount`. The frozen 7.1 design was built exactly as written — no field added, none removed, `completedAt` and `transportDirection` still excluded, the parent's `attempts` / `lastAttemptAt` / `lastError` / `lastCorrelationId` retained as the fast path. **Phase 7.4a has since been implemented locally; Phase 7.4 overall and Phase 7 as a whole are not complete.**

**The two attempt counters are independent and must never be conflated.** `ZJP_PO_DLV-AttemptCount` counts **outbound** SAP → Cloud Integration → CAP transport attempts and is written only by `ZJP_CL_DISPATCH_COORDINATOR`; `SupplierResponseDeliveries.attempts` and its history child count **inbound** CAP → Cloud Integration → SAP supplier-response attempts and are written only by `record()`. The 7.3 fixture ends at `AttemptCount 2` **and** CAP `attempts 2`, and those two 2s count entirely different events. `AttemptCount` increments once per request that actually reached `transport->post`, whatever came back; a skipped claim, an `EMPTY_SNAPSHOT`, a live-lease skip and a state reset move it by nothing, and `claim()` never writes it.

**The fresh fixture is `PO00000160`** (`PurchaseOrderUUID 37FC3FA8EB2D1FD1ADC2A13257140EFD`, `DeliveryUUID 37FC3FA8EB2D1FD1ADC3229323ECD083`, supplier `RTTEST001`, `EUR 1500.00`, revision `1`), ingested as portal order `6576c154-647c-4460-a539-e17b86c7555c` with exactly one `DeliveryReceipt`. The full 32-hex `PortalOrderUUID` survived the UUID case boundary, so the `PO00000121`/`PO00000122` truncation defect did **not** recur.

**The outbound record includes the part nobody planned, and it is not rewritten.** The first real outbound attempt met a **stopped CAP application**: the Cloud Foundry router answered `HTTP 404` *"Requested route … does not exist"*, an answered deterministic refusal, so the coordinator correctly recorded `FAILED` / `AttemptCount 1` and drove the header to `ERROR`. **That is an environmental failure, not a coordinator defect** — `cf` showed requested state `stopped`, instances `0/1`, and after `cf start` the app was `started`, `1/1 running`, ready. Because production `retryDelivery` does not exist, the one-off `ZJP_CL_DISPATCH_RETRY_RUNNER` reset **only** `DispatchState FAILED` → `PENDING` through EML and performed exactly one coordinator run on the **same** `DeliveryUUID`, which returned `HTTP 201`. **So the fixture's final outbound `AttemptCount` is `2`, not `1`**: two genuine transport attempts, of which only the second reached CAP.

**Inbound attempt A produced the first history row.** It reused the Phase 7.2 composite unchanged: one real send, the real transport eventually `answered=true` / `204`, the wrapper returning `answered=false` to the state machine first. **This is a *deterministic state-machine-facing unanswered observation*, not a genuine socket timeout, `AbortSignal` timeout, caller disconnect or lost HTTP response** — genuine timeout stays **Phase 7.9**. Correlation **A** `0d492a23-8360-4691-81ff-c1f4b662b66e`; parent `PENDING` → `UNKNOWN`, `attempts 0` → `1`; row #1 `af8dfe6a-…` at `attemptNumber 1`, `durationMs 25`, `outcome UNKNOWN`, `httpStatus null`, `errorCategory NO_ANSWER`. A read-only SAP Data Preview then showed the response **already applied** — `SENT` / `DELIVERED` / `ACCEPTED`, `LastResponseVersion 1` — while CAP durably held `UNKNOWN`: the ideal reconciliation case. **Correlation A did not overwrite `ZJP_PO_H-LastCorrelationId`**, which still held the outbound value `37FC3FA8EB2D1FD1ADC391CFA138F189`.

**Inbound attempt B was the real production reconciliation**, `scripts/reconcile-supplier-response.js --response-id 5bcb706a-b8ce-46ae-be18-aa9fb692e583`, with no wrapper, no manual `UNKNOWN` → `PENDING` reset and no raw persistence change. Fresh correlation **B** `4217a307-efec-41ae-915e-ddc480d10ed9` (**B ≠ A**), real result `answered=true` / `204` → `DELIVERED` / `NONE`. The parent reached `DELIVERED`, `attempts 2`, `lastError` cleared to `null`; **row #1 was untouched** and row #2 `7237b9b2-…` recorded `attemptNumber 2`, `durationMs 1612`, `204`, `NONE`. Final counts: one delivery row for the order, `HISTORY_FOR_THIS_RESPONSE 2`, `HISTORY_TOTAL 2`, `PENDING_TOTAL 0`, census `DELIVERED 5` / `UNKNOWN 1`. **No duplicate delivery row and no row #3.** The remaining `UNKNOWN` is the unrelated `PO00000121` diagnostic, left untouched.

**SAP supplied the at-most-once proof a second time, and this time the decisive field did not move at all.** After attempt B, `LastResponseId` and `LastResponseVersion 1` were unchanged and **`LastChangedAt` was byte-for-byte identical** — `20.260.922.004.448,5202090` before and after — so RAP recognised the identical replay and did not write the business object again. `ZJP_PO_DLV` stayed `DELIVERED` / **`AttemptCount 2`**: **the inbound reconciliation did not increment the outbound counter.**

**There is no historical backfill, deliberately, and it is evidenced rather than asserted.** After the Phase 7.3 deployment (operation `015b1895-b5f8-11f1-be3d-eeee0a98cb8e`) the Phase 7.2 fixture `a1bd862a-573f-461e-b337-5e39aa595a61` still reads `DELIVERED` at `attempts 2` with **zero** history rows — one read proving both that the child persistence is queryable and that 7.3 invented no past. Historical `ZJP_PO_DLV` rows likewise remain `AttemptCount 0`.

**What 7.3 does not prove, stated so no reader over-reads it.** **No real concurrent HANA race was manufactured** — the losing-compare-and-set invariant (a loser writes zero child rows) is proven by automated test against the in-memory database, and concurrency hardening remains **Phase 7.5**. **Production `retryDelivery` is still unimplemented**: `ZJP_CL_DISPATCH_RETRY_RUNNER` is a one-off, fixture-pinned recovery harness, not that capability, and it is to be **deleted** when 7.6 builds the real thing. **No SAP outbound attempt-history table or child entity exists** — 7.3 added `attempt_count` and nothing else. See the [Phase 7 guide](docs/phase-7-operational-resilience.md).

**PHASE 7.2 IS COMPLETE AND RUNTIME-VERIFIED. No executable source changed to close it.** Executed 2026-09-21 on fixture `PO00000123` (`portalOrderId ed267da7-83e2-4a38-8cb3-dfe095f7a5fc`, SAP `37FC3FA8-EB2D-1FD1-ACF8-86475DCE323D`), an `ACCEPTED` for `2026-12-15` under `responseId a1bd862a-573f-461e-b337-5e39aa595a61`. **Phase 7.3 has since completed; see the newer checkpoint above.**

**What it proves, in the exact words the design froze:** *runtime-proven `UNKNOWN` and reconciliation via a deterministic injected state-machine-facing unanswered observation over a real CAP → Cloud Integration → SAP send.* The composite transport called the deployed `HttpResponseTransport` **exactly once** (`sends=1`), retained that promise, returned `answered: false` to the delivery state machine, and let the real HTTP client go on waiting. The row became `UNKNOWN` at `attempts 1` under correlation **A** `d57bf9b0-9f22-46b8-818e-87234115c75e`; the retained send then settled **`answered=true`, `status=204`**; and a re-read proved the row was **still** `UNKNOWN` — the eventual success cannot move the durable state, because the only writer is `record()`. `PIH_CI_TIMEOUT_MS` was unset for the task process alone and the send used the normal 30 000 ms deadline: **the retired shortened-timeout strategy played no part in this proof.**

**All nine acceptance criteria passed.** Reconciliation replayed the same `responseId` under a fresh correlation **B** `e4159f7c-1007-49b5-93b3-0fb9a6febefa` (**B ≠ A**), reaching `DELIVERED` at `attempts 2`. SAP supplies the at-most-once evidence: `LastResponseId` unchanged, **`LastResponseVersion` stayed exactly `1`**, and `L0 = 2026-09-19T00:51:08.288116Z` → `L1 = 2026-09-21T16:09:00.120564Z` → `L2 = L1`, so `LastChangedAt` moved **once** and the replay did not apply the response a second time. Final CAP: `DELIVERED`, `attempts 2`, **`PENDING_TOTAL 0`**.

**Cloud Integration evidence is Tier 1, and its limit is stated rather than glossed.** Exactly one `PIH_SupplierResponse_v1` message existed in the window — `MessageGuid AGqxVqAxtbJ2F-jLgG_R2m19ILXd`, `COMPLETED`, `LogLevel INFO`. **Trace was not enabled and was not required.** The message's CPI `CorrelationId` is **not** the application `X-Correlation-ID` and no equality is claimed; custom correlation indexing is Phase 7.7 work.

**What 7.2 did not prove.** No genuine socket timeout, `AbortSignal` timeout, caller disconnect, client abandonment or lost HTTP response, and nothing about Cloud Integration's behaviour after a vanished caller. Those stay in **7.9**, so the Phase 6 induced-`UNKNOWN` gap is closed on its **lifecycle half only**.

**An abandoned first attempt on `PO00000121` is recorded as a diagnostic, not as an acceptance run.** The injection worked — `UNKNOWN`, `attempts 1` — but the retained real send settled `answered=true`, **`status=400`**, so the mandatory real-send gate returned `FAIL_HTTP_400`. **Reconciliation was correctly not performed and SAP was not manually repaired**; a `400` is a deterministic refusal, not an `UNKNOWN` ambiguity. `/IWFND/ERROR_LOG` gave the reason — *"Response belongs to a different portal order."* — and the root cause is **legacy delivery-intent identity corruption in `ZJP_PO_DLV`**, where `PORTAL_ORDER_UUID` for `PO00000121` and `PO00000122` is truncated at the first lowercase hex letter. The coordinator's current source already documents the `CL_SYSTEM_UUID=>CONVERT_UUID_C36_STATIC` behaviour behind it and already guards the boundary with an upper-case round-trip check, so **no fix was introduced in 7.2** — the defect is in rows persisted before that guard. **That `UNKNOWN` row is deliberately left untouched**, which is why the census reads `DELIVERED 4, UNKNOWN 1`. It does not affect the isolated `PO00000123` proof, which began and ended at `PENDING_TOTAL 0`. See the [Phase 7 guide](docs/phase-7-operational-resilience.md).

**PHASE 7.1 IS FROZEN DESIGN. No executable source changed.** Phase 7 — error handling, monitoring and operational resilience — hardens the Phase 6 flows in place and introduces no new product: Event Mesh stays Phase 9 and API Management stays Phase 10. Subphase 7.1 froze the two-direction fault matrix with an explicit evidence level on every row, and the inspection behind it found four things worth recording as status rather than as design.

**At the Phase 7.1 checkpoint, a no-answer outcome was not one condition and both implementations treated it as one.** Both legs classified every unanswered transport result as `UNKNOWN`, the state reserved for genuine ambiguity. A DNS failure, a refused connection and a proven TLS handshake failure are conditions in which **the receiver could not have committed**; they belong in `PENDING`. Phase 7.4b has since corrected the locally implemented CAP leg by preserving a narrow `error.cause.code` allowlist as `NOT_SENT`; SAP parity remains 7.4c. ADR-040 records why `PENDING` carries this rather than a fifth state.

**The documented timeout budgets do not match the running system, and the mismatch manufactures ambiguity.** API_CONTRACTS proposes CAP receiver 15 s, CI processing 25 s and coordinator request 35 s, and requires that inner calls get shorter deadlines than their callers. In fact the CAP receiver timeout does not exist, `transactionTimeout` is 30 s on both deployed iFlows, the coordinator leaves `i_timeout` at its API default by explicit decision, and the CAP sender's real 30 s appears in no table. The consequential one: **every Cloud Integration receiver call is allowed `httpRequestTimeout` = 60 000 ms while the CAP sender that triggers it waits only 30 s**, so the caller abandons an exchange the inner call may legitimately still be completing. That inverts the stated rule and is how a correctly functioning system produces `UNKNOWN` rows. Six inconsistencies are enumerated TI-1 to TI-6; no constant was changed.

**The inbound CSRF failure is classified `UNKNOWN` and is provably not ambiguous.** Read from the committed export: the integration process is one linear sequence with no gateway and **no Exception Subprocess**, and `Apply Supplier Response` is reachable only downstream of `Fetch SAP CSRF Token`, whose message flow keeps `throwExceptionOnFailure = true`. A failed token fetch therefore terminates the flow and `applySupplierResponse` is **never invoked** — SAP cannot have applied the response. What Cloud Integration returns externally in that case is an **inference** (`500`, unhandled) and has never been observed, which is exactly why it is recorded as an open question rather than as a finding. The correction cannot be made in CAP, which cannot distinguish that `500` from a SAP-applied-then-failed `500`; it needs a second Cloud Integration change beyond the approved custom-header work, and that ruling is pending.

**An outbound `UNKNOWN` has no operator recovery path at all.** `find_eligible` returns only `PENDING` and expired-lease `IN_FLIGHT` rows, `claim` refuses `FAILED` and `UNKNOWN` as `state_final`, and the `retryDelivery` those comments defer to was never built. The inbound leg received `reconcile-supplier-response` in 6.5g and the outbound leg was left without the equivalent — the largest operational gap carried out of Phase 6, now tracked as **OI-15** and assigned to Phase 7.6 rather than deferred again. See the [Phase 7 guide](docs/phase-7-operational-resilience.md).

**PHASE 6 IS COMPLETE AND RUNTIME-VERIFIED.** The architecture is bidirectional: SAP submits an order, Cloud Integration mediates it into CAP, a supplier decides, and the decision travels back through Cloud Integration into the RAP business object. **Postman is off the business path in both directions, and CAP has no direct dependency on SAP in either.**

| Subphase | | Evidence |
| --- | --- | --- |
| **6.1–6.4** | outbound leg and SAP cutover | runtime-verified |
| **6.5a** | inbound contract and action design | design freeze |
| **6.5b-1 / 6.5b-2** | RAP `applySupplierResponse`; restricted OData V4 Web API | EML-verified; published, `$metadata` verified |
| **6.5c / 6.5d** | authentication and CSRF; `PIH_SupplierResponse_v1` | runtime-verified |
| **6.5e** | the CAP sender | runtime-verified — `DELIVERED`, `attempts 1` |
| **6.5f** | real `ACCEPTED`, date update and `REJECTED` end to end | **all three runtime-verified** |
| **6.5g** | `UNKNOWN` reconciliation | implemented, **deployed**, guard **runtime-verified**; transitions **test-proven only** |

**Acceptance — the first real delivery.** `responseId 1cb091be-2db9-4fca-adaa-d9c443725970` reached SAP `37fc3fa8-eb2d-1fd1-ad92-f2fc9e60f34d`: `Status SENT`, `SupplierResponse ACCEPTED`, `EstimatedDeliveryDate 2026-11-15`, `LastResponseVersion 1`, `SupplierRespondedAt 2026-09-20T16:59:23Z`, `LastChangedAt 2026-09-20T17:06:29.954186Z`. The outbox row ended `DELIVERED` with `attempts 1`, `lastError null`, correlation `426779ed-e8e3-401b-8cf3-59025f17a327`.

**Idempotency, on the production path.** An identical replay returned `HTTP 204` and **`LastChangedAt` did not move**, which is the strong form of the claim: RAP recognised the response and returned *without writing*, rather than rewriting the same values.

**A conflicting replay, and the defect it exposed.** The same `responseId` with the date changed to `2026-11-16` was refused by RAP with `HTTP 400 SABP_BEHV/100`, but Cloud Integration converted that deterministic refusal into an external `500` — which the sender classifies `UNKNOWN`, the label reserved for genuine ambiguity. **The 6.5e audit predicted this from the export before it happened.** The fix is narrow: `throwExceptionOnFailure` is now `false` on the action receiver alone (`SAP_RAP_Action` / `MessageFlow_719`, `POST`), while the CSRF-fetch receiver (`MessageFlow_705`, `GET`) keeps it `true`, because a token failure has no business answer to pass through; `retryOnException` stays `false` on both. After redeployment the same conflict returned `400` → `FAILED`, and SAP's state was unchanged throughout.

**A date update.** Portal order `9efeb598-…` moved to `2026-11-20` at `ResponseVersion 2` under `responseId 11108278-d0b8-4db6-b52a-9d57ab7adc2f`, delivered `HTTP 204` (correlation `d0c5cf89-1fc1-442b-af94-0b13b843e9ec`). SAP: `SupplierRespondedAt 2026-09-20T18:46:50Z`, `LastChangedAt 2026-09-20T18:50:25.924892Z`. **`Status` stayed `SENT` across both versions**, exactly as 6.5a froze it, and the version-1 outbox row stayed `DELIVERED` and untouched.

**A rejection.** Portal order `92a4d059-…` (`PO00000127`) → SAP `37fc3fa8-eb2d-1fd1-ad92-8973260a322e`, reason *"Supplier cannot fulfill the requested delivery"*, `responseId 9acb2cde-a62f-4c86-9cab-2b5d29274b6d`. The flush reported `delivered 1` and exit 0. SAP ended **`Status REJECTED`**, `SupplierResponse REJECTED`, `EstimatedDeliveryDate null`, `SupplierRespondedAt 2026-09-20T19:09:10Z`, `LastChangedAt 2026-09-20T19:11:09.837479Z`. **This is the asymmetry 6.5a designed**: an acceptance leaves `Status` at `SENT` and speaks only through `SupplierResponse`; a rejection is terminal and moves `Status`.

**`UNKNOWN` reconciliation, with its evidence levels kept apart.** The implementation is complete and **deployed to Cloud Foundry** — built with `mbt build -p=cf` and deployed with `cf deploy`, so the running droplet carries the reconciliation command, the updated sender and the guarded compare-and-set bookkeeping. Its **state guard is runtime-verified** in that deployed environment: task `pih-reconcile-delivered` against the `DELIVERED` row `1cb091be-…` answered *"is DELIVERED, not UNKNOWN"* and exited `2`.

**Three things are deliberately NOT claimed.** No real `UNKNOWN` transport condition was induced in production, so the `UNKNOWN → DELIVERED` and `UNKNOWN → FAILED` transitions carry **automated-test evidence only**. The compare-and-set race is likewise **test-proven only** — no concurrent writer was staged against the deployed database. Both are evidence about the code rather than about the deployed transport, and the distinction is kept.

**What Phase 6 deliberately did not do.** Retry and resilience remain unexercised on both legs — no timeout, transient `5xx` or redelivery has been tested, and returning an intent or a row to `PENDING` is classification rather than recovery. `retryDelivery` does not exist, and neither sender is scheduled; both are explicit commands, as ARCHITECTURE.md intended for this stage.

**No credential material is in this repository.** The exported iFlow carries the alias `PIH_SAP_BASIC` and nothing else, `parameters.prop` is empty, and no password, secret, token or cookie value appears in any tracked artifact.

**The first deployed dry-run failed, and was fixed.** `cf run-task ... --dry-run` exited 2 with *"Query was not inferred and includes `*` in the columns"* from `@cap-js/hana`, before any HTTP request. Root cause: the standalone command connected to the database **before** publishing the model, and `cds.connect.to("db")` captures `cds.model` at that instant and never re-reads it — so the service held a model with no definitions and could not resolve the columns. Local SQLite hid it because `cds.deploy` repairs `db.model` as a side effect and only runs on the in-memory profile. Fixed in `srv/lib/cds-bootstrap.ts`, which publishes `cds.compile.for.nodejs(await cds.load("*"))` **before** connecting, matching the pattern this app's earlier successful HANA tasks used. The redeployed task then ran clean, and 6.5f went on to make the first real delivery through it.

**What 6.5e's local evidence proved, and what 6.5f added.** `npm run typecheck`, `npm test` (114 tests, 0 failures) and `npx cds build --production` all pass, and the tests drive the sender against a fake transport over rows a real supplier decision committed. That proves the payload, the classification table, the persistence and the replay behaviour. It proved **nothing** about the deployed iFlow, the HANA container or the credential; that gap is what 6.5f closed for `ACCEPTED` by running the real path. Local Node execution remains a weaker kind of evidence than SAP runtime evidence and is still labelled as such throughout.

**6.5d's runtime evidence, recorded.** An idempotent replay through `PIH_SupplierResponse_v1` returned **HTTP 204**, and the direct `GET` that followed proved no mutation: `Status SENT`, `SupplierResponse ACCEPTED`, `EstimatedDeliveryDate 2026-11-15`, `SupplierRespondedAt 2026-09-20T03:08:45Z`, `LastResponseId` and `LastResponseVersion 2` all unchanged, with `LastChangedAt` and `LocalLastChangedAt` untouched — which is the whole claim RAP idempotency makes, now proven over HTTP rather than only by EML. The deployed route is `POST /http/pih/v1/supplier-responses`, and the flow performs CAP-style JSON → validation and mapping → Cloud Connector → `GET` the OData instance and fetch a CSRF token → **preserve the SAP session cookie** → `POST` the bound action. CAP → CI authentication was proven manually with HTTP Basic against the CI HTTPS sender, whose User Role is `ESBMessaging.send`, using a Process Integration Runtime service key. **No credential, token, cookie or service-key content is recorded in this repository, and none may be.**

---

*The record below is the 6.5a design checkpoint and describes the state before 6.5b. It is kept as written.*

**Phase 6.5 is ACTIVE at DESIGN ONLY. Phase 6.4 remains CLOSED and runtime-verified, and nothing in the outbound path changed.** Phase 6.5a froze the inbound supplier-response contract and the RAP action design; **no SAP object, no iFlow and no CAP code has been created or modified**, and every object named below is **DESIGN / NOT YET CREATED**.

**Frozen lifecycle.** A supplier **acceptance** leaves `Status` at **`SENT`** and sets `SupplierResponse = 'ACCEPTED'` — **no `CONFIRMED` status is introduced**, because `SENT` already carries the successful outbound commercial state and `SupplierResponse` is the inbound field the model has reserved since Phase 1. A supplier **rejection** is terminal and does move `Status` to **`REJECTED`**, with `RejectionOrigin = 'SUPPLIER'` and the inbound reason, which is exactly what `RejectionOrigin` has always been for; **the buyer's `reject` action is untouched and is not reinterpreted**. A later `ACCEPTED` at a higher `ResponseVersion` is CAP's date-update path and replaces only `EstimatedDeliveryDate`.

**One action, `applySupplierResponse`, with parameter entity `ZJP_A_ApplySupplierResponse`** — no second action for date updates, since the version rules already order them and a second action would need the same guards, the same receipt and the same conflict rules while adding a way for the two to disagree. `PurchaseOrderUUID` locates the order and is the only lookup key; `DeliveryUUID`, `PortalOrderUUID`, `Supplier` and `OrderRevision` are **guards**. **`PortalOrderUUID` is persisted on the delivery intent, not on the header**, so validating it means reading `ZJP_PO_DLV` as well as the order — the one finding in 6.5a that changes the handler's shape. `LastResponseId` and `LastResponseVersion` are the idempotency receipt: an identical response is `ALREADY_APPLIED` with no mutation, a conflicting one is `409`, and a stale `ResponseVersion` is `409` that never overwrites newer state.

**Authentication and CSRF are deliberately NOT frozen.** CI-to-SAP inbound authentication has never been exercised in this project — the outbound leg proved SAP-to-CI, which says nothing about the reverse — CSRF behaviour on an OData V4 write is unproven, and CAP has **no outbound capability at all** today. Candidates are documented; none is selected, and none may be marked selected without runtime or configuration evidence. **6.5b** builds the RAP surface and proves it through EML before any HTTP; **6.5c** isolates the endpoint, authentication and CSRF; **6.5d** the iFlow; **6.5e** the CAP sender; **6.5f** real end-to-end evidence.

**Phase 6.4 is runtime-verified and the SAP cutover is done. Phase 6 is NOT complete** — 6.5, the inbound supplier response, has not started. The production outbound path is now:

```text
RAP sendToSupplier → durable DeliveryIntent + immutable PayloadSnapshot
  → post-commit coordinator (transaction A, COMMIT)
  → destination ZJP_CI_ORDER_DELIVERY → POST /http/pih/v1/order-deliveries
      → Integration Suite PIH_OrderDelivery_v1 → CAP Supplier Portal
  → receipt → recordDeliveryResult (transaction B, COMMIT)
```

**Integration Suite now owns mapping on the active production path.** The coordinator posts the persisted `PayloadSnapshot` **verbatim** — never rebuilt from the order, never deserialized and re-serialized — so the bytes whose SHA-256 was frozen with the approval are the bytes that go on the wire. `ZJP_CL_DLV_SNAPSHOT_READER` and `ZJP_CL_CAP_ORDER_MAPPER` are **unchanged, undeleted and still tested**, but are **no longer in the active coordinator network path**; they are the Phase 5 fallback and the mapping-parity reference, exercised directly by harness test D. `ZJP_CAP_BASE` and `ZJP_CL_HTTP_TRANSPORT_TEST` likewise survive as the direct-to-CAP reference. **No `Idempotency-Key` is sent**: delivery identity is the `deliveryId` in the body, which is what CAP deduplicates on.

**Classification is unchanged and was confirmed rather than adjusted:** `201` and a `200` replay → `DELIVERED`/`SENT`; `409` (with 400, 401, 403, 413) → `FAILED`/`ERROR`; unanswered → `UNKNOWN`/`ERROR`; `429`, `502`, `503` → `PENDING` with the order untouched; `500` → `UNKNOWN`. **`classify()` was not modified to make anything pass.**

**Runtime evidence.** 6.4a proved the destination, its OAuth 2.0 client credentials and the ABAP-supplied path in isolation, before any coordinator change. 6.4c returned a real **`201`** with `DELIVERED`/`SENT`, `PortalOrderUUID` and correlation persisted and the lease cleared, then replayed **the exact persisted snapshot under the same `deliveryId`** for a real **`200`** whose receipt named the **same delivery and same `portalOrderId`** with no duplicate and the intent untouched. 6.4d sent the same `deliveryId` with one changed mapped field **altered only in a local test copy** and got a real **`409 DELIVERY_PAYLOAD_CONFLICT`** with the stored order unchanged; the coordinator's own handling of `409`, unanswered and an answered `502` is covered deterministically by the fake, including `502` → `PENDING` with the lease cleared and the order still `APPROVED`.

**What this does not prove, stated because the distinction is easy to lose:** retry and resilience remain **unexercised**. The receiver still runs on platform-default retry and nothing has tested a timeout, a transient `5xx` or a redelivery. Returning an intent to `PENDING` is **classification, not recovery** — nothing has been shown to pick it up again, and `retryDelivery` does not exist. CI's controlled technical-error contract and platform retry behaviour are separate things and only the first is evidenced. **Phase 6.5, inbound `PIH_SupplierResponse_v1`, is NOT STARTED and is next.**

**Phase 6.3 is SAP Integration Suite runtime-verified: Cloud Integration now calls the portal, and Phase 5 is still untouched.** `PIH_OrderDelivery_v1` gained a *Prepare CAP Request* Content Modifier, a **Request Reply** step calling the `CAP_Supplier_Portal` HTTP receiver over **OAuth 2.0 client credentials** (Security Material alias `PIH_CAP_OAUTH`, no secret recorded anywhere), a *Prepare Delivery Receipt Response* Content Modifier, and an **Exception Subprocess** *Handle Technical Exception*. Four runs on delivery `244b218a-…`: **6.3a** create returned `201` with CAP's receipt; **6.3b** identical replay returned `200` with the **same `portalOrderId` and `receivedAt`**, which is what proves no duplicate portal order; **6.3c** a replay with changed content returned CAP's real `409 DELIVERY_PAYLOAD_CONFLICT` body intact; **6.3d** a deliberately unresolvable receiver host produced the controlled `502 INTEGRATION_TECHNICAL_ERROR`, after which the real receiver was restored and the restore verified by re-running the original payload to `200`.

**The load-bearing setting is `Throw Exception on Failure = OFF`.** With it enabled, CPI turned CAP's `409` into a failed MPL and a caller-facing generic `500`, destroying the precise conflict message. With it off, downstream HTTP answers stay real responses from the portal and only *technical* failures — DNS, connectivity, TLS, timeout, OAuth, iFlow processing — reach the Exception Subprocess. **A downstream `409` is not an integration error and a DNS failure is not a business outcome**, and collapsing both into one `500` would have made the coordinator's receipt classification unimplementable. The `502` body exposes a stable code, a `retryable` flag and the caller's correlation ID, and deliberately **not** `exception.message` or a stack trace.

**Two open items closed, one of them against its own premise.** CI sets `Content-Type` and `X-Correlation-ID` only and **never sends `Idempotency-Key`**, yet deduplication worked — because `deliveryId` is the key of the `Orders` entity in the body, which `integration-service.cds` itself calls the *"`Idempotency-Key` equivalent"*. Delivery identity travels in the payload, so no header ownership transfers at cutover. The receipt-shape item closed as recommended: CI reshapes nothing and returns CAP's receipt verbatim, so the coordinator's runtime-verified `read_receipt` still applies. **Still absent: any SAP change.** No ABAP and no CAP source changed in 6.1, 6.2 or 6.3; every run was driven by a test client. SAP continues to post directly to CAP over `ZJP_CAP_BASE`, **Phase 6.4 cutover has not started**, and Phase 5 remains the verified fallback.

**Phase 6.2 is SAP Integration Suite runtime-verified, and Phase 5 is still untouched.** Cloud Integration now maps the source delivery contract to the CAP order contract: `PIH_OrderDelivery_v1` gained a **Graphical Message Mapping** and a *Prepare Mapping Response* step, and reached **semantic parity** with the Phase 5 ABAP mapper on a **one-item and a two-item** payload. **The target contract is an OpenAPI 3.0.3 document rather than the XSD originally proposed**, and both conversion risks the plan had flagged were answered by a **declaration plus a mapping-side mechanism**, not by the target document alone. `revision` and `lineNumber` are declared `integer`, but the deployed run still emitted `"1"` and `"10"` until the Message Mapping was set to use the basic data types defined by the target JSON/OpenAPI schema (*Basic Data Type Handling*); only then did they emerge as JSON **numbers**. `lines` is declared an `array` with `minItems: 1`, and the repeated node is produced by an explicit `items` → `lines` structure mapping. **The two-item run is what actually retires the array risk** — one item alone cannot distinguish an array from a collapsed object. `EA` becomes `PCE` through a standard **`FixValues`** function, the header currency is replicated to `amount.currency` directly and to every line through **`useOneAsMany`** with **`removeContexts`**, and `supplierName`, `companyCode`, `purchasingOrganization` and `purchasingGroup` are excluded. **No Groovy was required**, which is ADR-009 upheld on the one rule most likely to tempt a script. A further consequence: with an OpenAPI target the mapping emits JSON directly, so the flow has **no XML-to-JSON converter step at all**, a deliberate deviation from the ARCHITECTURE sketch. **Parity is semantic, not byte-level, and is recorded as such**: the runtime response orders its members differently from the ABAP mapper, so byte parity is not merely unproven, it is known to be false. The two-item fixtures in the repository are the **executed** scenario and the **verbatim deployed response** — `1500.00 + 500.00 = 2000.00` — not a derivation. **Getting into the flow needed one adapter change.** With **CSRF protection enabled** on the HTTPS Sender, an authenticated service-key POST was rejected with `HTTP 403 Forbidden` and produced **no Message Processing Log**, which located the rejection at the sender/endpoint boundary rather than inside the iFlow. Disabling CSRF protection let the same authenticated POST through to `HTTP 200 OK` and the mapped contract, with `X-Correlation-Id` echoed. **Authentication was never broken**, and `CSRF Protected = disabled` is recorded as the **current Phase 6.2 runtime configuration, not a declared production posture** — the final inbound posture is an open decision. **Still absent: any CAP call and any SAP change** — SAP continues to post directly to CAP over `ZJP_CAP_BASE`, and `ZJP_CL_CAP_ORDER_MAPPER` remains the parity oracle until cutover in 6.4. The exported iFlow is now in the repository and the `mappings/` files are extracted from it rather than hand-derived. **OI-06b is narrowed**, not closed.

**Phase 6.1 is SAP Integration Suite runtime-verified, and Phase 5 is untouched.** Cloud Integration now has a deployed entry boundary for the source delivery contract: `PIH_OrderDelivery_v1` in the dedicated **`PIH Integration`** subaccount, published at `/http/pih/v1/order-deliveries`, doing HTTPS sender — correlation capture — JSON-to-XML with root `OrderDelivery` — source XSD validation — controlled response. **A real POST returned the `VALIDATED` body with the caller’s own `X-Correlation-ID` echoed back**, Monitor status **Completed** at 477 ms; the same request with `"schemaVersion": "2.0"` was rejected by the schema with *“Value "2.0" contravenes the enumeration facet "1.0" of the type SchemaVersionType”*, Monitor status **Failed** at 490 ms, and the happy-path response was never reached. **The negative run is the more valuable of the two**: it proves the schema is enforced and that a rejection is visible in monitoring rather than quietly successful. **A dedicated subaccount was necessary** — the original trial tenant refused deployment with *“Requested runtime location (: cloudintegration) is not supported for tenant: 0badc38dtrial”*, and the same exported artifact then deployed cleanly in the new one, which proves it was a **tenant provisioning problem and not an iFlow defect**. **Deliberately absent:** any mapping, any CAP call, any SAP change. The `VALIDATED` body is a Phase 6.1 stand-in and not a contract. **Phase 5 remains the working baseline and the rollback**: SAP still posts directly to CAP over `ZJP_CAP_BASE`, and no ABAP or CAP object was touched. **OI-06 is split** — OI-06a closed by this evidence, OI-06b still open for mapping, target validation and the receiver. **Phase 6.2, mapping parity, was designed and not yet built as at this checkpoint**, with `ZJP_CL_CAP_ORDER_MAPPER` kept as the runtime-verified parity oracle; it has since been built and runtime-verified.

**Phase 5.2g is SAP runtime-verified. The post-commit coordinator exists and works end to end: SAP claims a persisted intent under a lease, commits, posts the immutable snapshot to the deployed CAP portal, and records the outcome back onto both business objects in a second transaction. The full A-J matrix passed, combining the network-free A-D/F-J run with the final real-HTTP E run. Phase 5.2a through 5.2f stay VERIFIED.** The post-commit coordinator now exists in source across three transaction boundaries. **Transaction A** claims one eligible persisted intent under a lease — `DispatchState` to `IN_FLIGHT`, `LeaseOwner` to the run identity, `LeaseExpiresAt` to now plus an injected `lease_seconds` — and **commits before any network call**, which is the whole reason the outbox exists. **The network step** reconstructs the delivery from the **persisted** `PayloadSnapshot` through the new `ZJP_CL_DLV_SNAPSHOT_READER`, maps it through the verified `ZJP_CL_CAP_ORDER_MAPPER`, mints a fresh correlation UUID for the attempt and posts through the verified `ZJP_CL_OUTBOUND_TRANSPORT` seam — it never rebuilds the delivery from the current PurchaseOrder and never touches the snapshot or its hash. **Transaction B** writes the classified outcome onto the intent and drives the header through the new internal `recordDeliveryResult` action, both in one LUW, then commits. **The real E run returned HTTP 201**: `DispatchState` `DELIVERED`, business `Status` `APPROVED` -> `SENT`, `IntegrationStatus` `DELIVERED`, `DeliveryId` unchanged, the receipt’s `PortalOrderUUID` stored, the correlation id persisted on both rows, the error fields initial, and the lease released to initial — with `PayloadSnapshot`, `PayloadHash`, `ApprovedBy` and `ApprovedAt` all untouched. **Classification follows section 6.4 on the transport’s own fields, never on `answered` alone**: 201 and 200 are both `DELIVERED`, 409 stays a meaningful collision and is `FAILED`, an unanswered call is `UNKNOWN` and replays the same `deliveryId`, and 429/502/503 return the intent to `PENDING`. **Success is `APPROVED` to `SENT`**; a definite failure or an ambiguous timeout is `ERROR`, per the domain model, and a `SENT` order cannot be downgraded by a late failure. **Three probes closed in the same pass:** **P14** is answered — `CL_SYSTEM_UUID=>CREATE_UUID_X16_STATIC( )` returns `SYSUUID_X16` directly, used for the run identity and for one correlation UUID per attempt; **P16** is answered — `CL_ABAP_TSTMP=>ADD` on a short timestamp, whose fractional-second loss is accepted for a lease; and **P17 is superseded** by the permanent reader, because the SAP method list showed `IF_XCO_CP_JSON_DATA` exposes `WRITE_TO` and no member accessors. **Three target-specific facts were learned by failing first and are now permanent:** XCO’s `WRITE_TO` is **terminal** and cannot be chained, so the order is `FROM_STRING` -> `APPLY` -> `WRITE_TO`; `CONVERT_UUID_C36_STATIC` takes `EXPORTING uuid` / `IMPORTING uuid_x16`; and that conversion **silently corrupts lowercase input**, which is why the CAP receipt is uppercased and then verified by a round trip before it is stored. **Two environment findings came with it:** a Cloud Foundry 404 meant the app was stopped, not that the destination was wrong, and the deployed portal needs the synthetic supplier **`RTTEST001`** for the real test, because production CAP HANA carries no local fixture data. **Deliberately still absent:** any automatic retry of a `FAILED` intent, which only `sendToSupplier` may return to `PENDING`; `retryDelivery` and its bounded budget; and projection exposure of either `sendToSupplier` or `recordDeliveryResult`.

**Phase 5.2f is SAP runtime-verified. `sendToSupplier` exists and works: SAP now creates a durable delivery intent inside the order's own LUW, with no network call anywhere in the path. Phase 5.2a through 5.2e stay VERIFIED; Phase 5.2g was next and not started as at that checkpoint; it has since been implemented and SAP runtime-verified.** Eight branches passed, covering every row of the locked contract in Phase 5.1 section 7.4. **A** — an eligible `APPROVED` active order produced exactly **one** intent with `DispatchState = PENDING`, left business `Status` at `APPROVED`, set `IntegrationStatus = PENDING` and `DeliveryId` to the managed `DeliveryUUID`, persisted the deterministic source snapshot, and stored a **SHA-256 that the test recomputed over the exact persisted string and matched**. Approval evidence came from the header's pre-send `LastChangedBy` / `LastChangedAt`, not from `sy-uname` and not from a fresh timestamp. **B** — a `PENDING` replay was an idempotent no-op: same intent, same `DeliveryId`, no second row. **C** — a `FAILED` retry reused the same `DeliveryId` and returned the state to `PENDING` with snapshot, hash and approval evidence preserved. **D** — an **active** order whose `Status` was not `APPROVED` was refused and created nothing. **E** — a **real RAP technical draft**, `%is_draft = 01`, was refused by the draft guard with *"Not allowed on a draft instance."* and created nothing; E and D are deliberately different tests, because a technical draft is not a business `DRAFT` status. **F** — an `IN_FLIGHT` intent was a pure no-op: same identity, state unchanged, snapshot, hash, approval evidence, **both lease fields** and the header all untouched. **G** — a `DELIVERED` intent produced an **informational** outcome with `FAILED` empty, the intent unchanged and the business `Status` still `APPROVED` — **no `SENT` transition**, which stays Phase 5.2g's. **H** — an `UNKNOWN` intent followed `FAILED`'s retry semantics exactly. **Four things the run settled that had been flagged as risks rather than assumed.** `ZJP_CL_ORDER_DELIVERY_BUILDER` **can** be called from inside the PurchaseOrder behavior handler on this target — the feared re-entrant EML problem did not occur, and that was the single largest implementation risk. A **read-only `SELECT` on `ZJP_PO_DLV` works inside a behavior handler**, which is how a previous transaction's intent is found before anything is created. `CL_ABAP_MESSAGE_DIGEST=>CALCULATE_HASH_FOR_CHAR` works in the composed flow, not only in isolation. And `if_abap_behv_message=>severity-information` can be **read back** through `%msg->m_severity`, which no code in this project had ever done. **Blocker B4 is RESOLVED FOR DEV with multi-environment configuration deferred**: `PIH_ABAP_DEV` is declared once as a private constant in `lhc_PurchaseOrder`, so `sendToSupplier` has a single controlled producer while the builder keeps its verified parameter for tests — but a transport to QAS or PRD would carry the DEV identity, so a real configuration mechanism must replace it before any rollout beyond DEV. **Deliberately still absent:** the coordinator, any HTTP call from `sendToSupplier`, the `SENT` transition, lease acquisition or expiry, retry scheduling, and projection exposure — `sendToSupplier` is **not** reachable through `ZJP_C_PurchaseOrder`, because an action is proven through EML before it is offered to a UI. **P7 and P14 remain OPEN**, neither blocking; P14 is the coordinator's.

**Phase 5.2e is SAP runtime-verified. SAP now posts to the deployed CAP portal over real HTTP through the verified seam. Probe P15 is ANSWERED. Phase 5.2a through 5.2d stay VERIFIED; Phase 5.2f is next and is NOT STARTED, and 5.2g is NOT STARTED.** The harness ran to `PASS: Phase 5.2e real HTTP transport verified.` **P15 first, because the adapter could not be written without it.** P1 had recorded which HTTP classes work but **not one method signature**, and the original probe's source is **not in this repository** — so a new base destination **`ZJP_CAP_BASE`** was created with the proven host, port, TLS and OAuth configuration and **no application path prefix**, and a console probe ran `CREATE_BY_DESTINATION` → `CREATE_BY_HTTP_DESTINATION` → `GET_HTTP_REQUEST` → `SET_URI_PATH( '/health/ping' )` → `EXECUTE` GET → `GET_STATUS` / `GET_TEXT` for **HTTP 200 `OK`**. It also closed P1's timeout question: `EXECUTE` takes `I_TIMEOUT TYPE I DEFAULT 0`. **Path ownership is decided and evidenced**: the destination owns host, port, TLS and OAuth; `TY_REQUEST-PATH` owns the application route, applied through `SET_URI_PATH`. `ZJP_CAP` and `ZJP_CAP_INGEST` are **not** used by the production adapter. **Runtime evidence.** CREATED returned `answered = X`, `201` and the receipt with `portalOrderId 5c07ecdc-7210-4a87-9e54-dc183730794b`, `receivedAt 2026-09-18T14:53:01.940Z`. REPLAYED returned `200` with **the same `portalOrderId` and `receivedAt`** — the stored original receipt, which is what makes a replay safe. CONFLICT returned `409` carrying `DELIVERY_PAYLOAD_CONFLICT`, the receiver confirming the stored order was unchanged, and — the assertion that matters — `answered = X`, because a 409 is a protocol answer and not a transport failure. The unresolvable destination `ZJP_NO_SUCH_DEST` returned `answered = false`, status `0`, initial body and safe diagnostic text. **The adapter is exactly an adapter**: it forwards headers generically, sends the body unchanged, holds no SQL, no EML, no `COMMIT` and no `ROLLBACK`, knows nothing of PurchaseOrder or DeliveryIntent, interprets no CAP business error, and generates no correlation UUID — the seam is unchanged and needs no new field. Timeout stays at the API default, because how long a delivery may hang is a business decision. **The earlier HTTP 500 was infrastructure state, not transport logic, and the diagnosis is the lesson.** The same unchanged ABAP failed twice with 500, and CAP logged only `Pool resource could not be acquired within 1s` at `HANAService.begin`. That fitted a real fact — `@cap-js/hana` overrides `acquireTimeoutMillis` to **1000 ms in production** against a 10000 ms base — and it was still the wrong cause. Deeper Cloud Foundry logs exposed `HANA Database instance is stopped`, SQL code **1890**, SQL state **HY000**, and HANA Cloud Central showed `pih-hana` as **Stopped**. Starting that database and waiting for `RUNNING` made the **unchanged** test pass. Three things hid it: the service instance and broker reported healthy, which describes the **service** and not the **database runtime** beneath it; the HDI binding was intact and the app was `1/1 running`; and **`/health/ping` returned 200 throughout, because it opens no database connection** — a liveness probe that never touches the database cannot report that the database is unreachable. **Nothing was changed to fix it**: not the transport, not the CAP payload, not the HDI binding, not the HANA service. The `acquireTimeoutMillis` override written during diagnosis has been **reverted**, because an experiment that did not turn out to be the fix has no business surviving as production configuration; `cds env requires.db --profile production` resolves back to `kind hana`, `min 0`, `max 10`, `acquireTimeoutMillis 1000`, and 89 tests still pass. **P13 is now ANSWERED and SAP runtime-verified**, after this checkpoint: `CL_ABAP_MESSAGE_DIGEST=>CALCULATE_HASH_FOR_CHAR` with `IF_ALGORITHM = 'SHA256'` compiled under **ABAP for Cloud Development** and reproduced the published `abc` test vector exactly — `BA7816BF8F01CFEA414140DE5DAE2223B00361A396177A9CB410FF61F20015AD`, 64 characters, uppercase hex, no Base64 conversion needed for `PayloadHash CHAR(64)`. **P7 and P14 remain OPEN**, and neither blocks Phase 5.2f. P14 is why the 5.2e harness used a fixed delivery identity, so a rerun returns 200 rather than 201 on its first call. **Still absent:** `sendToSupplier`, the coordinator, `DeliveryId` on the header, any `IntegrationStatus` transition beyond `NOT_REQUESTED`, and revision-increment behaviour.

**Phase 5.2d is SAP runtime-verified. The outbound builder and the CAP mapper exist and work. Probes P5, P6 and P10 are ANSWERED and runtime-verified. Phase 5.2a, 5.2b and 5.2c stay VERIFIED; Phase 5.2e is next and is NOT STARTED.** The harness ran to `PASS: Phase 5.2d outbound builder and CAP mapper verified.` with no `STOP`. **Three probes were answered first, and two of the answers shaped the design rather than merely unblocking it.** **P5**: both `CL_SYSTEM_UUID` and `XCO_CP_UUID` are released and the smaller was chosen — `CL_SYSTEM_UUID=>CONVERT_UUID_X16_STATIC`. **P6**: `|{ value NUMBER = RAW DECIMALS = n }|` is fixed-scale and locale-independent; because those values travel as JSON **strings**, the canonical snapshot keeps them **numeric** and the mapper formats them, putting scale on the wire side of the seam. **P10**: `XCO_CP_JSON=>DATA->BUILDER( )` … `GET_DATA( )->TO_STRING( )`, with member names passed **explicitly** to `ADD_MEMBER` rather than left to automatic underscore-to-camelCase, so the case-sensitive wire contract is visible in the source and cannot drift when an ABAP field is renamed. **Builder evidence, from a real active order.** It read `PO00000002` / supplier `SUP003` / `EUR` / `1500.0` and one item — `00010`, `MAT001`, quantity `2.0`, `EA`, net price `750.0`, total `1500.0` — copying the header, the revision and every item, and the row counts before and after were identical, so it **wrote nothing**: no `COMMIT`, no `ROLLBACK`, no `DeliveryIntent` row, no `PurchaseOrder` update. The `DeliveryUUID` and `SourceSystem` inputs came through untouched. **That order carried `OrderRevision = 0`, and it is the most useful number in the run.** It is a row persisted before Phase 5.2a, which P11 established keeps its original values and was never back-filled — so the snapshot carrying `0` through unchanged is **stronger** evidence than a `1` would have been, because `1` is exactly what a builder that quietly recalculated the revision would also produce. A `0` can only have been copied. **Mapper evidence.** It produced the golden CAP payload **byte for byte**, and twice from the same snapshot, proving determinism across two separately constructed instances. Inside that one comparison: `deliveryId`, `source.orderId` and `lines[].sourceItemId` as canonical C36 text; `revision` and `lineNumber` as JSON **numbers**; `NUMC 00010` → `10`; `EA` → `PCE`; the header currency propagated onto the line; and the three wire scales — `1500.00`, `2.000`, `750.0000`. Path `/rest/integration/v1/Orders`, `Content-Type: application/json`, and `Idempotency-Key` equal to the C36 `deliveryId`. **`X-Correlation-ID` is deliberately absent**, being a fresh UUID per transport *attempt* and therefore the transport's to mint. **The rejection path is verified too**: unit `BOX` returned `success = false`, `UNSUPPORTED_UNIT`, *"Unit BOX has no mapping to a CAP unit."* and an **initial** request — so a refused delivery cannot be mistaken for a sendable one. That is the contract's *"unknown units fail validation; they are not guessed"* holding at runtime. **The mapper's fixture carries `revision = 1` while the builder's order carried `0`, and the two are independent paths by design** — the builder test proves real RAP read behaviour, the mapper test proves deterministic wire transformation. **No network of any kind** was involved: no CAP request, no destination, no OAuth, no HTTP client. **One compiler correction on the way**, in the harness only: the golden payload could not be a `CONSTANTS` `VALUE`, because a text literal caps at 255 characters and a constant's value must be a single literal, so `&&` cannot rescue it; it became a concatenating method, checked to reproduce the 524-character payload byte for byte. **Two boundaries held.** `deliveryId` is an **input** — 5.2d creates no `DeliveryIntent` and generates no UUID — and `sourceSystem` is an **input** too, so blocker **B4** stays OPEN without production code deciding its configuration mechanism by accident. **P7 and P13 remain OPEN**, neither blocking: the outbound body has no timestamp field, and nothing computes `payload_hash` until 5.2f. **Still absent:** the real HTTP transport, `sendToSupplier`, the coordinator, `DeliveryId` on the header, any `IntegrationStatus` transition beyond `NOT_REQUESTED`, and revision-increment behaviour.

**Phase 5.2c is SAP runtime-verified. The outbound transport seam and its deterministic fake exist and work. Phase 5.2a and 5.2b stay VERIFIED; Phase 5.2d is next and is NOT STARTED.** Three objects activated cleanly — `ZJP_IF_OUTBOUND_TRANSPORT`, the seam; `ZJP_CL_TRANSPORT_FAKE`, a deterministic fake implementing it; and `ZJP_CL_TRANSPORT_TEST`, a dedicated console harness — and the harness ran to `PASS: Phase 5.2c outbound transport abstraction verified.` with no `STOP` and no short dump. **The seam is demonstrated, not merely declared.** Every scenario was driven through a `REF TO ZJP_IF_OUTBOUND_TRANSPORT`, with the concrete class named only at construction, so the run proves a consumer can dispatch through the interface to an implementation it does not name — which is the whole point of the boundary, and what will let `ZJP_CL_OUTBOUND_TRANSPORT` replace the fake in 5.2e without touching a caller. **All four deterministic outcomes passed.** CREATED returned `answered = X`, status `201` and the fixture receipt `{"deliveryId":"00000000-0000-0000-0000-000000000001","sourceOrderId":"…0002","portalOrderId":"…0003","status":"RECEIVED","receivedAt":"2026-01-01T00:00:00Z"}`. REPLAYED returned `answered = X`, status `200` and a body **byte-identical** to the CREATED receipt, which is the property that makes a replay safe and the reason 6.4 handles 200 and 201 alike; because the two bodies came from two separately constructed fakes, the determinism is a property of the class rather than of one instance's history. CONFLICT returned `answered = X`, status `409` and a body carrying `DELIVERY_PAYLOAD_CONFLICT` — **answered on purpose**: a 409 is a business or protocol answer, not a technical transport failure, and collapsing the two is exactly what this interface exists to prevent. UNANSWERED returned `answered = false`, status `0`, an initial body and `Fake transport: the receiver did not answer.` — the distinction that *"a timeout never proves non-delivery"* depends on. **None of it needed an exception**, which was the design bet of ADR-032 point 4 and section 6.3, and it is now runtime evidence rather than an argument. **The run used no network of any kind** — no CAP request, no SM59 destination, no OAuth, no HTTP client — and no SQL, no EML, no `COMMIT` and no `ROLLBACK`; there is nothing to commit, since the fake touches no database. **One compiler and runtime finding worth keeping:** the CONFLICT check uses the ABAP **`CS`** containment operator, the **first use of any string-containment operator in this repository**. It had no precedent to copy, so it was written as core language rather than as a released API and flagged as unproven on this target; it **compiled and executed correctly**. **The test-object decision is verified too.** `ZJP_CL_TRANSPORT_TEST` is the dedicated Phase 5.2c harness; `ZJP_CL_PO_EML_TEST` remains the RAP lifecycle regression suite and was **not touched**, because 5.2c changes no RAP behavior; `ZJP_CL_PO_DISPATCH_TEST` remains reserved for the Phase 5.2g coordinator and still does not exist. **Still absent, and not claimed:** the builder and the mapper (5.2d), the real HTTP transport (5.2e), `sendToSupplier` (5.2f), the coordinator (5.2g), `DeliveryId`, any `IntegrationStatus` transition beyond `NOT_REQUESTED`, and revision-increment behaviour. The fake also does **not** echo the request into its receipt and does **not** record the request it was given, so what is verified is outcome *classification*, not body correlation — a 5.2g coordinator test will likely need a capturing fake. **P13 remains OPEN**, as do P5, P6, P7 and P10, which now gate 5.2d directly.

**Phase 5.2b is SAP runtime-verified. The DeliveryIntent persistence and RAP business object exist, blocker B7 is RESOLVED, and probes P8 and P9 are ANSWERED. Phase 5.2a stays VERIFIED; Phase 5.2c is NOT STARTED.** The suite finished with `PASS: Phase 5.2b DeliveryIntent persistence verified.` and reached `=== END LIFECYCLE SUMMARY ===` with no `STOP` and no short dump, so the PurchaseOrder regression did not regress. **The stack.** `ZJP_PO_DLV` — the 16 fields of Phase 5.1 §4.2 unchanged, `payload_snapshot` kept as `abap.string` rather than falling back to `char`; the unique Table Index **`ZJP_PO_DLV~REV`**; `ZJP_I_DeliveryIntent`; `ZJP_I_DELIVERYINTENT`, managed, `strict ( 2 )`, **no draft**; `ZBP_I_DELIVERYINTENT` as a skeleton implementing only `get_instance_authorizations`, which is all `authorization master ( instance )` obliges; and a `ZJP_CL_PO_EML_TEST` extension rather than a new test class, per the object inventory. **Automated runtime evidence.** An intent is created through EML with a `DeliveryUUID` generated by managed numbering — the run's example was `37FC3FA8EB2D1FD1ACE0E74B1139368A`, read out of `MAPPED`, which is exactly how `sendToSupplier` will learn the wire `deliveryId`. Before the commit the buffer held the real `PurchaseOrderUUID`, `OrderRevision = 1`, the probe `PayloadSnapshot`, a `PayloadHash` of 64 zeros, `DispatchState = PENDING`, and initial `LastCorrelationId`, `PortalOrderUUID`, `LeaseOwner` and `LeaseExpiresAt`. `COMMIT ENTITIES` returned `sy-subrc = 0` with empty `FAILED` and `REPORTED`, the persisted `ZJP_PO_DLV` row read back with all of those values intact, and exactly **one** intent existed for that order revision. **Three compiler corrections, each taken on evidence rather than reasoned around.** A **`total etag` requires `with draft`** on this target, so copying `lock master total etag LastChangedAt` from the draft-enabled `ZJP_I_PurchaseOrder` was refused; the non-draft BO uses plain `lock master` with `etag master LocalLastChangedAt`, and draft was **not** added to satisfy the syntax. ABAP's **30-character method-name limit** rejected `TEST_DELIVERY_INTENT_PERSISTENCE`, renamed to `TEST_DLV_INTENT_PERSIST`. And `field ( readonly, numbering : managed ) DeliveryUUID` makes a caller-supplied duplicate key a **compile-time** error — *"The field `DELIVERYUUID` ... cannot be modified"* — which is a stronger guarantee than the runtime test it replaced, so the test was deleted rather than the BDEF loosened. **The unique index is verified manually, and that is a deliberate testing decision.** Two operational findings drove it. First, **a DDIC-active index is not a database index**: after activation SE11 showed `REV` as unique while SE14 reported *"Não existe no banco de dados"*, and in that window the duplicate-revision test **persisted two rows** — the definition was never wrong, the physical index simply did not exist. The duplicates were cleaned through EML, one row per order revision kept, and SE14 used to create the index, which then reported *"Existe no banco de dados"*. Second, **a physical unique-index violation during managed RAP save short-dumps**: a second intent for the same order and revision with a *different* generated `DeliveryUUID` terminated in `CX_SY_OPEN_SQL_DB` → `CX_CSP_ACT_INTERNAL` → `RAISE_SHORTDUMP` inside `CL_CSP_ACT_SAVE_TO_DB`, the database reason naming a duplicate primary or unique secondary key; the differing keys prove the collision was on the **secondary business index**. SE16N afterwards showed **no duplicate row persisted**. A dump aborts a run and leaves every later assertion unexecuted, so the suite asserts the **positive** form instead — exactly one intent per order revision — and prints a note pointing at the manual evidence. Neither escape was taken: no direct SQL write to force the duplicate, and no catching or suppressing of the dump. **The invariant is one DeliveryIntent per `(CLIENT, PURCHASE_ORDER_UUID, ORDER_REVISION)`, ever** — deliberately stronger than the original "one non-terminal intent per revision", because a retry must reuse the same delivery identity and a corrected snapshot must eventually use a **higher `OrderRevision`**. **A requirement this creates for a later slice:** because the constraint dumps, it cannot serve as control flow, so `sendToSupplier` must check for and **reuse** an existing intent before a duplicate ever reaches the database. That is recorded, not implemented. **Still absent:** `sendToSupplier`, the transport interface and its fake, the real HTTP transport, the builder, the mapper, the coordinator, `DeliveryId`, any `IntegrationStatus` transition beyond `NOT_REQUESTED`, and revision-increment behaviour. **P13 remains OPEN** — nothing computes `payload_hash`, which is why the probe row carries 64 zeros. One `ZJP_PO_DLV` row is left behind on purpose as evidence.

**Phase 5.2a is SAP runtime-verified. Blockers B1 and B2 are RESOLVED, and the PurchaseOrder header now carries the two values a real delivery needs.** This is the first real Phase 5.2 implementation slice, and it is deliberately small: **two existing classes changed, no object created.** **What changed.** `submit` writes `OrderRevision = 1` in the same local-mode `UPDATE FIELDS` clause that already wrote `Status` and `PurchaseOrderNumber`, so the three facts a successful submit establishes commit or roll back together; and the existing `initializeStatus` determination writes `IntegrationStatus = 'NOT_REQUESTED'` in the same clause that writes `Status = 'DRAFT'`, so a newly created root can never exist with one initialized and the other not. `IntegrationStatus` was put in `initializeStatus` rather than a second determination because both values answer the same question — what a newly created root starts as — and two handlers on the same `on modify { create; }` trigger would duplicate the read with no ordering guarantee between their writes. Both fields were already declared, mapped and `readonly` in the BDEF, so **no behavior definition, view, table, projection, service definition or binding changed**, and the activation confirms the repository's reading of `readonly`: external callers cannot write these fields, a handler can through `MODIFY ... IN LOCAL MODE`, exactly as `submit` has always written `Status`. **Activated:** `ZBP_I_PURCHASEORDER` and `ZJP_CL_PO_EML_TEST`. **Runtime evidence, from the full EML suite.** `PASS: new order before submit - OrderRevision 0, IntegrationStatus NOT_REQUESTED.` — a new order is `DRAFT`, revision `0` and therefore deliberately undeliverable, since CAP refuses `source.revision` below 1. `PASS: submit set the buffered OrderRevision to 1; IntegrationStatus stayed NOT_REQUESTED.` — asserted in the RAP buffer before the commit, which proves the **action** wrote it rather than a determination or a save-time side effect. `PASS: after successful submit - OrderRevision 1, IntegrationStatus NOT_REQUESTED.` with the `PO########` format retained. `PASS: after refused re-submit - OrderRevision 1, IntegrationStatus NOT_REQUESTED.` — a refused second submit neither re-wrote nor incremented the revision, and kept the original number. `PASS: refused submit without items - OrderRevision 0, IntegrationStatus NOT_REQUESTED.` — the decisive negative case: a failing submit exits before the update, so failure can never assign revision `1` and leaves the order undeliverable. The **whole pre-existing lifecycle suite ran through its final summary with no `STOP`**, so nothing regressed. **The final summary showed a lifecycle distinction worth recording**: orders that passed through submit and later became `APPROVED`, `REJECTED` or `CANCELLED` all carry `OrderRevision = 1`, while an order cancelled while still pre-submit retained `0` — so the revision tracks *"was this ever submitted"* and not the business status, which is exactly the frozen-at-submission semantics the domain model specifies. **Legacy rows are intentionally untouched.** Rows persisted before 5.2a keep their blank `IntegrationStatus` and are documented as pre-Phase-5 data. Two mechanisms guarantee it: the determination is `on modify { create; }` so it never fires for existing rows, and its guard `DELETE purchase_orders WHERE Status IS NOT INITIAL` drops any instance that already has a status. That guard was deliberately **not** widened to `IntegrationStatus`, because legacy rows have a filled `Status` and a blank integration value, so guarding on the blank one would have classified them as uninitialized and reset their `Status` to `DRAFT`. **What this does not do.** There is **no revision increment and no revision-2 behaviour** — 5.2a establishes only the initial frozen revision the current lifecycle requires. **`DeliveryIntent` does not exist**, **`sendToSupplier` does not exist**, the **`DeliveryId` lifecycle is not implemented**, **`IntegrationStatus` does not advance beyond `NOT_REQUESTED`**, and there is **no HTTP dispatch from the real RAP business object**. The earlier `ZJP_CL_HTTP_PROBE` remains only technical transport evidence and is not a repository object. The ADR-022 `APPROVED` → `CANCELLED` delivery guard is still untestable: a handler can now write `IntegrationStatus`, but the only value it writes is `NOT_REQUESTED`, which is precisely the no-delivery-requested state, and `DeliveryId` is still written by nothing. **The next slice is Phase 5.2b**, and it is gated on probes **P8** and **P9**.

**SAP S/4 has called the deployed CAP application and completed a full ingestion, replay and conflict sequence. Phase 5.1 blocker B3 is RESOLVED; Phase 5.2 is still not started.** This is a **technical outbound runtime probe**, and the distinction matters: it proves `SAP ABAP → HTTP destination → TLS → OAuth2/XSUAA → CAP IntegrationService → HANA ingestion`, and it proves nothing about the business outbound flow. **Configuration.** Destinations **`ZJP_CAP`** (`/health/ping`) and **`ZJP_CAP_INGEST`** (`/rest/integration/v1/Orders`); OAuth2 client profile **`ZJP_CAP_OAUTH`** with configuration **`ZJP_CAP_XSUAA`**, grant type client credentials, client authentication HTTP Basic / standard HTTP, resource authentication through the `Authorization` header; the XSUAA token endpoint is reachable from SAP, `OA2C_GENERIC_ACCESS` obtained a token, and the token carries `<xsappname>.IntegrationClient` and `uaa.resource`. **TLS trust had to be established first**: the initial handshake failed with **`SSSLERR_PEER_CERT_UNTRUSTED`**, and importing **DigiCert TLS RSA4096 Root G5** into the selected **ANONYM** SSL client PSE resolved it — a fact about *this* Cloud Foundry endpoint's chain, not a general requirement to carry into other environments. **The released ABAP API question is answered by execution**, and the answer was not what this repository guessed: the working chain is `CL_OUTBOUND_PROVIDER_HTTP=>CREATE_BY_DESTINATION` → `IF_HTTP_DESTINATION` → `CL_WEB_HTTP_CLIENT_MANAGER=>CREATE_BY_HTTP_DESTINATION` → `IF_WEB_HTTP_CLIENT`, whereas probe P1 had named `cl_http_destination_provider` and its `create_by_url` / `create_by_comm_arrangement` / `create_by_cloud_destination` factories. **Connectivity evidence.** Before OAuth the `ZJP_CAP` connection test returned **HTTP 401**, which was the decisive result rather than a setback — only CAP can produce a 401, so DNS, network path, TLS and the running application were all proven by a failure. After OAuth the same test returned **HTTP 200 / OK**, and ABAP code read `/health/ping` for **200** with `status UP`, `component cap-supplier-portal`. **Ingestion evidence.** A POST of `{}` through `ZJP_CAP_INGEST` returned **HTTP 400 `SCHEMA_VERSION_UNSUPPORTED`** — the portal's own envelope, so the request cleared OAuth/XSUAA and entered the `IntegrationService` handler. A valid hand-built delivery (`source.system = PIH_ABAP_PROBE`, `supplierCode = RTTEST001`, EUR, PCE, one line, quantity `2.000`, unit price `25.0000`, amount `50.00`) returned **HTTP 201**, status `RECEIVED`, `portalOrderId` `e6f96663-ed98-478f-9692-036f78a92682`, `receivedAt` `2026-09-17T23:47:58.476Z`. An **exact second execution returned HTTP 200 with the same `portalOrderId` and the same `receivedAt`** — idempotent replay proven from SAP, not just from the developer machine. A third execution reusing the same `deliveryId` with changed but internally valid content (quantity `3.000`, lineAmount `75.00`, total `75.00`) returned **HTTP 409 `DELIVERY_PAYLOAD_CONFLICT`**, and CAP reported the stored order unchanged. **This resolves blocker B3** — whose own closure condition was that an outbound call from S/4 actually reach the deployed endpoint — and answers probes **P1, P2/P3, P4 and P12**; **P5–P11 remain open** and are now the critical path, because they are all about building a payload rather than sending one. **What it explicitly does not prove.** The payload was hand-built. The real RAP purchase order is **not** mapped into the DTO, `OrderRevision` is not populated, the `DeliveryId` and `IntegrationStatus` lifecycles are not implemented, **`sendToSupplier` does not exist**, the HTTP outcome is not persisted back into RAP, the section 7 transaction boundary is not implemented, and there is no retry or reconciliation. **No ABAP object from the Phase 5.2 inventory was created**, and no RAP behavior changed. No client secret or access token is recorded in this repository. *As a checkpoint record this paragraph stands, with one part superseded: Phase 5.2a has since started Phase 5.2 and changed RAP behavior — `OrderRevision` is now populated at submit and `IntegrationStatus` at create — so B1 and B2 are resolved. Still no inventory object exists. See the paragraph above.*

**The CAP ingestion path is runtime-verified against real SAP HANA. OI-14 item 4 now splits in two: HANA runtime verification — ingestion path is VERIFIED, and HANA runtime verification — supplier decision/update path is OPEN.** This is the first time the project has exercised HANA rather than read its generated DDL. **The environment.** CAP deployed on SAP BTP Cloud Foundry with real XSUAA authentication and the real HDI container `pih-hdi`; the deployed application resolves the **production** profile to **`HANAService`**, which is the first direct evidence that the HANA wiring is live rather than merely built. **The initial HANA state answered a question and proved another.** Suppliers, Orders, OrderItems and DeliveryReceipts all existed, were queryable and held **0 rows** — so the HDI deployment worked, **the production fixture exclusion of item 3 is now runtime-proven** rather than build-proven, and no active supplier existed, which the ingestion handler requires before it writes anything. **One synthetic prerequisite was created** through a CAP-aware Cloud Foundry task over the application's own `pih-hdi` binding — not raw SQL, not a fixture CSV, not a code change and not a new endpoint: `supplierCode` **`RTTEST001`**, active, sized to the real CDS fields. Deliveries were sent under `sourceSystem` **`PIH_RUNTIME_TEST`** so the test data stays identifiable. **The valid ingestion.** An authenticated `POST /rest/integration/v1/Orders` with a fresh `client_credentials` token returned **201** with the contract receipt and status **RECEIVED**, and HANA was then queried directly rather than the 201 being trusted: exactly **one Order**, **one OrderItem** and **one DeliveryReceipt**, the supplier association resolving through an expanded read, `sourceSystem`/`sourceOrderId`/`sourceRevision` as sent, server-owned `status RECEIVED` and `responseVersion 0`, and **decimal scale and Timestamp precision intact** across the round trip — `37.50`, `3.000`, `12.5000` returned with scale preserved and no float drift, and `receivedAt` keeping millisecond precision. The stored `requestHash` matched the hash computed locally before the request was sent, so the dedup key is cross-validated and not merely present. **The HANA catalog holds no `IntegrationService.Orders` table** — six tables, the five `pih.portal` entities plus CAP's framework `cds.outbox.Messages` — which **runtime-confirms the `@cds.persistence.skip` design of ADR-034**, previously argued only from the compiler. **Idempotency and conflict protection hold in the cloud.** An exact replay returned **200** with the same `portalOrderId` and the **original** `receivedAt`, so it was answered from the stored receipt; a **byte-different but business-equivalent** payload — keys reordered, decimals written with fewer trailing zeros — also returned **200**, confirming that the replay hash is taken over a normalized projection rather than raw bytes; and a changed payload reusing the same `deliveryId` returned **409 `DELIVERY_PAYLOAD_CONFLICT`**. Row counts stayed **1 Order / 1 Item / 1 Receipt** throughout, and the final query was byte-identical to the post-create one **including `modifiedAt`**, which proves no UPDATE touched the row either. **No HANA-specific runtime defect was found.** `CDS_OUTBOX_MESSAGES` in the container is a CAP framework table, not a defect. **What stays open is the reason item 4 is not closed outright: every write exercised here was an INSERT.** No UPDATE was ever issued against HANA, so the `SupplierService` accept/reject path, the `responseVersion` increment, the `@cds.on.update: $now` behaviour of `modifiedAt` and `SupplierResponseDeliveries` persistence are all unverified on HANA, as are the interactive `SupplierPortalUser` identity and the `supplier` → `req.user.attr.supplier` attribute mapping. **Test data and cleanup debt.** The synthetic supplier, order, item and receipt **remain intentionally in Trial `dev`** as clearly labelled runtime-test data: `DeliveryReceipts.order` is an association rather than a composition, so deleting the order would orphan the receipt and the referential effect was not established, and deleting the receipt would destroy the idempotency record and make that `deliveryId` re-ingestable. `runtime-test-key` on the real XSUAA instance and the unbound `pih-xsuaa-probe` both remain cleanup debt. **No credential, secret, token or service-key content is recorded in this repository.**

**The CAP Supplier Portal is deployed to SAP BTP Cloud Foundry and authenticated runtime access is verified. OI-14 item 2 is now resolved; item 4 remains open because no business row has touched HANA.** `cap-supplier-portal-srv` is **started, 1/1 running** on Trial `us10-003` space `dev`, served at `https://0badc38dtrial-dev-cap-supplier-portal-srv.cfapps.us10-003.hana.ondemand.com`. `cap-supplier-portal-db-deployer` deployed the HANA artifacts into the **pre-existing `pih-hdi`** container, and the MTA's `org.cloudfoundry.existing-service` resource held: **no duplicate HANA Cloud database and no duplicate HDI container** were created, and `pih-hdi` is bound to both the application and the deployer. A **real XSUAA instance `cap-supplier-portal-auth`** was created from the tracked `xs-security.json` and bound to the application; `pih-xsuaa-probe` remains in the space, **bound to nothing**, and is cleanup debt rather than an application dependency. **The first deployment staged cleanly and then crashed at startup** with `sh: 1: cds-tsx: not found`, exit 127: `npm start` ran `cds-tsx serve`, and `cds-tsx` is declared only by the `@sap/cds-dk` devDependency, so a production install does not contain it — its shebang is `#!/usr/bin/env tsx`, needing a second devDependency even if it were. The fix was one word, `"start": "cds-serve"`, which `@sap/cds` provides as a production dependency and which is sufficient because `cds build --production` precompiles every handler into `gen/srv` as JavaScript with **zero `.ts` files**. After rebuilding and redeploying the application starts and stays 1/1. **Authenticated runtime evidence against the real deployment.** Anonymous requests are refused everywhere — `/health/ping`, the integration route and the supplier routes all answer **401**, because CAP with XSUAA restricts all services by default. A real `client_credentials` token from `cap-supplier-portal-auth` carries `uaa.resource` and `<xsappname>.IntegrationClient` and **does not carry `SupplierPortalUser`**. With that token, `GET /health/ping` returns **200**; `POST /rest/integration/v1/Orders` with `{}` returns **400 `SCHEMA_VERSION_UNSUPPORTED`** in the portal's own error envelope with a correlation id — which is the decisive result, because it is not a framework rejection but the ingestion handler compiled and executing in the cloud, so the request cleared XSUAA, cleared the `IntegrationClient` check and entered the handler; and the same token on the supplier surface returns **403** with `User 'system' is lacking required roles: [SupplierPortalUser]`, so the two surfaces are isolated in the deployed application and not merely in configuration. Nothing was persisted, because `schemaVersion` is the first check in `normalizeDelivery`. **This closes the deployed-enforcement uncertainty that OI-14 item 2 carried.** Four things remain open: **no valid order has been ingested into HANA**, so **item 4 stays fully open** and no HANA read, write, decimal or SQL behaviour is verified; the **`supplier` → `req.user.attr.supplier` mapping is unverified**, since a client-credentials token has no user and cannot test it; **SAP S/4 is not configured** for OAuth2 client credentials or to call the endpoint; and the deployed build still contains the mock user block, inert under `[development]`. A `runtime-test-key` service key exists on the real XSUAA instance for the upcoming SAP outbound work. **No credential, client id, secret, token or service-key content is recorded in this repository.** *As a checkpoint record this paragraph stands; the first of the four open points has since been addressed — a valid order has been ingested into HANA and read back, so item 4 is no longer fully open but split into a verified ingestion path and an open supplier decision/update path. See the paragraph above. The other three remain open.*

**Production authentication is prepared and the SAP machine-to-machine authority is runtime-proven at the token level. OI-14 item 2 is partially resolved and stays open; item 4 remains open; CAP is still not deployed.** Mocked authentication can no longer resolve in production: the `.cdsrc.json` block moved under an explicit `[development]` key, so `cds env requires.auth --profile production` returns `kind: xsuaa` with **no `users` key**, where before it returned `kind: mocked` including CAP's accept-any-user `"*": true` default. Two named roles replace the `authenticated-user` pseudo-role — **`SupplierPortalUser`** on `SupplierService`, **`IntegrationClient`** on `IntegrationService`, which had been reachable with no credentials at all. The pseudo-role was the wrong tool twice over: it generates no XSUAA scope, and a client-credentials token *is* an authenticated user, so it would have granted the integration client the supplier surface. `xs-security.json` is generated from the annotations by `cds compile --to xsuaa`. **The probe answered a question reasoning could not.** A temporary XSUAA instance on the `application` plan in Trial `us10-003` space `dev` showed that a scope plus a role-template is **not sufficient** for the application's own technical client: the `client_credentials` token carried only `uaa.resource`. Adding top-level **`authorities: ["$XSAPPNAME.IntegrationClient"]`** — the syntax used by two shipped cds-dk templates, checked before it was written — produced a token carrying the resolved `IntegrationClient` scope, with **`SupplierPortalUser` absent**, so isolation holds in a technical token. `grant-as-authority-to-apps` was **not** used: it applies per-scope to a *foreign* XSUAA application, and none exists in this architecture. `SupplierPortalUser` is deliberately kept out of `authorities`, because that key grants to the app's own client and including it would defeat the separation. **Four things remain unproven, which is why item 2 is not closed:** no deployed CAP has accepted a real token against `@requires: 'IntegrationClient'`; the `supplier` → `req.user.attr.supplier` mapping is untested because a technical token carries no user; SAP S/4 is not configured to fetch or send such a token; and `xsappname` is deliberately absent from the tracked descriptor, so the identity the real deployment presents is still undetermined — `cf create-service` refused the descriptor without one, which the deployment descriptor must supply. Declarative `@restrict` row filtering was attempted and **reverted on evidence**: it answered 403 where the contract documents a scoped 404. Evidence is local execution plus a token-level cloud probe — **89 tests in 20 suites, 0 failures**, clean typecheck, warning-free production build, HANA artifacts unchanged. No secret, client id, service key or token is recorded in this repository.

**OI-14 item 3 is resolved: no synthetic fixture data reaches the production HANA build. Items 2 and 4 remain open, and no HANA runtime behaviour is verified.** The three fixture CSVs moved from `db/data/` to `test/data/` — **no content change, all three pure renames**, and no configuration written. The mechanism is CAP's own, verified in the installed toolchain rather than assumed: `@sap/cds/lib/env/cds-requires.js` declares seed folders per profile, giving `[development]` the folders `db/data`, `db/csv` and `test/data` but `[production]` only `db/data` and `db/csv`, so **`test/data` is intentionally development-only** and a file placed there cannot reach a production build. The old location was the whole defect — `db/data` is in both lists, so fixtures were deployed by construction. Alternatives were checked and none is supported: the CAP defaults carry no seed-data skip flag, and the HANA build plugin's `CSV_FILE_TARGET` only renames the target folder between `data` and `csv` without excluding anything. **Verified:** `.hdbtabledata` **3 → 0** and fixture `.csv` in the build **3 → 0**, while `.hdbtable` remains **6**, `.hdbindex` **7** and `.hdbview` **2** — the whole-`gen/` tree diff removed exactly those six files and nothing else, leaving `.hdiconfig`, `.hdinamespace`, `undeploy.json`, the HDI deployer `package.json`, all five `pih.portal` tables, `cds.outbox.Messages` and both `SupplierService` views untouched. **Local development is unchanged**, confirmed two ways rather than assumed: `npm start` logs `init from test\data\pih.portal-Suppliers.csv`, `…-Orders.csv` and `…-OrderItems.csv` followed by a successful in-memory deploy, and a live read returned `SUP001`'s `PO00001001` and `PO00001002` and `SUP002`'s `PO00001003` — the same fixture data as before. `npm test` passed **87 tests in 20 suites with 0 failures** with no test expectation altered, `npm run typecheck` reported no diagnostics, and `cds build --production` passed with **no errors and no warnings**. One stale comment in `test/supplier-service.test.ts` that named `db/data` was corrected to `test/data`; it is a comment, not an assertion. **This is build-output evidence, not HANA evidence:** nothing has been deployed or bound, so item 2 (mocked authentication carried into the production build) and item 4 (HANA portability and runtime behaviour unverified) are **open and untouched**, as are Phase 5.1 blocker B3 and every other open item.

**OI-14 item 1 is resolved: the unnecessary persistence artifact for `IntegrationService.Orders` is gone. Items 2, 3 and 4 remain open, and no HANA runtime behaviour is verified.** `IntegrationService.Orders` now carries `@cds.persistence.skip`, applied only after the annotation was **verified against the installed tooling** rather than assumed: `@sap/cds-compiler` 7.1.1 implements it in `lib/transform/db/cdsPersistence.js`, where the skip predicate sets `$ignore` on the artifact, and grepping the OData/EDM transform for the annotation returns nothing — so it suppresses the database artifact and cannot reach the service contract. Two safety checks were run first, because the compiler's only documented failure mode for the annotation is a view or join-relevant association navigating *into* a skipped entity: nothing associates to `IntegrationService.Orders` and it is not a projection, so there was no collateral to lose. **Results, all verified.** The entity **remains part of the service contract**, `POST /rest/integration/v1/Orders` behaves exactly as before, the handler still receives the same nested payload and still persists only `pih.portal` entities, and the generated **OData metadata is byte-identical** before and after at 4530 bytes with an empty `diff`. `IntegrationService.Orders.hdbtable` is **no longer generated**, and it is the *only* difference in the HANA artifact list: **7 `.hdbtable` → 6**, while all **7 `.hdbindex`**, both `.hdbview`, all 3 `.hdbtabledata`, the five `pih.portal` tables, `cds.outbox.Messages`, `.hdiconfig`, `.hdinamespace`, `undeploy.json` and the HDI deployer `package.json` are untouched. `npm test` passed **87 tests in 20 suites with 0 failures**, the ingestion suite alone **28 tests with 0 failures**, `npm run typecheck` reported no diagnostics, and `cds build --production` passed **with no errors and no warnings**. One thing was caught mid-task and fixed: the explanation was first written as a `/** */` doc comment, which made the compiler warn that it overwrote the entity's own doc comment — a doc comment becomes a model description, so that would have altered metadata under cover of a comment. It is a plain `//` comment now, and the entity's doc comment was confirmed to round-trip verbatim. **This changes nothing about HANA at runtime**: still not deployed, not bound, no schema created and no query executed, so OI-14 items 2 (mocked authentication in the production build), 3 (fixture CSVs in the deployment output) and 4 (HANA portability and runtime behaviour unverified) are **open and untouched**, as are Phase 5.1 blocker B3 and every other open item. See ADR-034. *As a checkpoint record this paragraph stands; item 3 has since been resolved by moving the fixtures to `test/data`, so only items 2 and 4 remain open — see the paragraph above; item 2 has since become partially resolved, with production mocked authentication eliminated.*

**CAP HANA production support is configured and locally verified. Nothing is deployed, nothing is bound, and no HANA runtime behaviour is verified.** This is the first CAP cloud-deployment preparation step and it adds no business capability: no entity, service, contract, handler, fixture, authorization rule or HTTP route changed. The database is now chosen **by CAP profile**. `cds env requires.db` resolves to `impl @cap-js/sqlite`, `kind sqlite`, `url :memory:` — byte-identical to the previous baseline, pool settings included — and `cds env requires.db --profile production` resolves to `impl @cap-js/hana`, `kind hana`, with no `:memory:` credentials. **`cds add hana` alone was not sufficient**, which is the substantive finding: it added only the `@cap-js/hana` dependency, and the Phase 4.1 `.cdsrc.json` pinned `requires.db` to SQLite `:memory:` *unconditionally*, so that pin applied to production too and kept it on SQLite while the build had already switched to emitting HANA artifacts — a combination that would have deployed HDI tables and then connected to an in-memory SQLite. The fix was to **delete** the pin rather than hand-write HANA configuration, because the plugins already declare it: `@cap-js/sqlite` contributes `requires.db = "sql"` plus `kinds.sql.[development]`, and `@cap-js/hana` contributes `kinds.sql.[production]`, so with no project-level `db` key the two compose as designed. `cds build --production` now emits a `db` build task `for: 'hana'` and writes `gen/db` as an HDI deployer module — `@sap/hdi-deploy` with the pure-JS `hdb` driver — holding 7 `.hdbtable`, 7 `.hdbindex`, 2 `.hdbview`, the fixture `.hdbtabledata`, `.hdiconfig`, `.hdinamespace` and `undeploy.json`; on the previous baseline the same command produced **no database build task at all**. The 7 indexes are the 7 `@assert.unique` rules rendered as `UNIQUE INVERTED INDEX`, and decimals render as real `DECIMAL(19,2)`, `DECIMAL(19,4)` and `DECIMAL(13,3)` columns rather than SQLite's `REAL_DECIMAL` — which advances but does not close the recorded doubt about decimal storage, because that DDL has been **read and not run**. One **pre-existing defect was found and fixed**: `cds build --production` failed with `TS2210` on the clean baseline as well, before any HANA change, because the `cds-typer` build plugin looks for a file named `tsconfig.cdsbuild.json` and without it falls back to `tsc --outDir gen/srv`, which needs a `rootDir` to emit against the `package.json` `imports` map. Nobody had hit it because `cds build` had never been run in this project. Adding that file makes the build exit 0 and touches neither typecheck nor tests, which both use `tsconfig.json`. **Evidence, all local execution:** `npm test` 87 tests in 20 suites, 0 fail; `npm run typecheck` no diagnostics; `cds build --production` exit 0. **`pih-hana`** (service `hana-cloud`, plan `hana-free`) and **`pih-hdi`** (service `hana`, plan `hdi-shared`) exist in BTP Cloud Foundry space `dev`, but the application is **not bound to `pih-hdi` and not deployed**, and no XSUAA, MTA, approuter or destination configuration was added. Four findings are open and are recorded as **OI-14**: a physical persistence artifact for `IntegrationService.Orders`, mocked authentication carried into the production build, fixture CSVs in the deployment output, and unverified HANA runtime behaviour. None was fixed here. *As a checkpoint record this paragraph stands, and several of its figures have since been superseded: item 1 was resolved by `@cds.persistence.skip`, taking the build from 7 `.hdbtable` to 6, and item 3 by moving the fixtures to `test/data`, taking it from 3 `.hdbtabledata` to none. Only items 2 and 4 remain open — see the paragraphs above; item 2 has since become partially resolved, with production mocked authentication eliminated.* Because the two HANA services exist, the BTP-deployment option compared for Phase 5 connectivity is no longer blocked by an unknown account, but **Phase 5.1 blocker B3 stays open**: with nothing deployed there is still no CAP URL an SAP system could call. See the [CAP README](cap-supplier-portal/README.md#production-build-hana). *Superseded: CAP has since been deployed and S/4 has called it, so **B3 is resolved**.*

**Phase 5 is open at 5.1, which is design and evidence review only. Nothing was implemented, nothing was activated in ADT, and no HTTP request was made in either direction.** Phase 5.1 prepared the SAP side for direct SAP → CAP communication and stopped short of building it: no ABAP object was created or changed, no CAP file was modified, nothing was deployed, no tunnel was configured and no credential was created. **It is not runtime-verified, and it cannot be until the probes below are answered.** Four things were established by reading the actual sources rather than by assuming. First, **the RAP side needs no new header field**: all nine integration fields — `OrderRevision`, `IntegrationStatus`, `DeliveryId`, `LastErrorCode`, `LastErrorMessage`, `LastErrorAt`, `LastCorrelationId`, `LastResponseId`, `LastResponseVersion` — already exist in `ZJP_PO_H`, are mapped, are `readonly`, and are written by **nothing**; the behavior pool's eight `UPDATE FIELDS` clauses cover only `TotalAmount`, `Status`, `PurchaseOrderNumber`, `RejectionOrigin` and `RejectionReason`, and the only mention of any integration field in the pool is a Phase 2 comment explaining why the cancellation guard was impossible. Second, that has a consequence that **blocks Phase 5 outright**: `OrderRevision` therefore holds `0` on every row, while CAP's `normalizeDelivery` requires `source.revision` to be an integer **≥ 1** and answers `400 INVALID_SOURCE_IDENTITY` otherwise, so a delivery built from today's persistence fails before anything interesting runs. That is documented debt becoming a blocker rather than a regression, and the recommended fix is to write `OrderRevision = 1` in `submit` as its own 5.2 step with an updated `ZJP_CL_PO_EML_TEST`, **not** to default `0` to `1` inside a mapper. `IntegrationStatus` is blank rather than `NOT_REQUESTED` for the same reason, which 5.2 must decide explicitly. Third, **the receipt's `portalOrderId` had nowhere to live**: the Phase 4.6 handoff assigns it to SAP, and neither `ZJP_PO_H` nor the domain model's `PurchaseOrder` has a column for it, so it is designed onto the new `DeliveryIntent` — a receipt is the outcome of one delivery, not a property of an order that may be delivered again at a later revision. Fourth, **SAP cannot call the laptop**, which is not a code problem: CAP listens on `http://localhost:4004` and `localhost` inside the SAP system means the SAP application server. The two options the project already permits — a deliberately configured HTTPS development tunnel, and CAP deployed to a reachable hosted runtime such as BTP Cloud Foundry — were compared, and the comparison has since been **settled in favour of deployment**: the preferred Phase 5 connectivity path is now **CAP deployed to BTP Cloud Foundry**, with the public tunnel kept only as a **fallback and debugging aid**. That became the better option once the entitlement question was answered — **BTP Trial Cloud Foundry is available in region `us10-003`, space `dev` is usable, and `pih-hana` and `pih-hdi` exist** — and it also avoids publishing an ingestion endpoint that has no authentication at all from the developer laptop, which is Phase 4.6 debt item 2 arriving on schedule. **Choosing the path is not walking it: CAP is still not deployed and not bound, so blocker B3 remains open — there is still no reachable CAP HTTPS URL for SAP to call.** *Superseded: the path has since been walked end to end — CAP deployed, destinations and OAuth2 configured, certificate trust fixed, and an outbound probe from S/4 completing a 201 / 200 replay / 409 sequence — so **B3 is resolved**.* The design work itself produced a `DeliveryIntent` as a separate managed RAP root BO with no draft (a composition child would be blocked by the ADR-021 invariant; a plain table would need the forbidden direct SQL), a fifteen-field minimum whose snapshot freezes the **source order delivery** rather than the mapped CAP order so that Phase 6's endpoint switch does not invalidate it, a two-hop mapping trace that keeps `EA` → `PCE` in a pure deterministic mapper, and a transport interface that returns an outcome whose `answered` flag separates "never replied" from "replied with a status" — because *"a timeout never proves non-delivery"*, and an exception would collapse the ambiguous case into the failed case, which is how a duplicate delivery gets created. **The released ABAP outbound HTTP API set is unknown**: a repository-wide search for `if_web_http`, `cl_web_http`, `cl_http_destination` and `IF_HTTP` returns no ABAP hit, so candidate classes are recorded as candidates and **twelve exact ADT probes** are listed instead of an implementation written from memory. Seven blockers need learner SAP evidence, led by the revision gap, the reachable-URL decision and the HTTP API probes. OI-05 is now in progress rather than untouched; OI-13, the `__OperationControl` UX gap, the permissive RAP authorization stub, the OI-03 remainder and all eight items of CAP debt remain **open and untouched**. See the [Phase 5.1 guide](abap-rap/docs/phase-5-1-outbound-foundation.md). **Phase 5.2 is not started.** *Superseded on two counts: the reachable-URL decision was carried out and S/4 has called the deployed endpoint, resolving B3; and Phase 5.2a has since closed the revision gap (B1) and the `IntegrationStatus` initial value (B2) while Phase 5.2b built the `DeliveryIntent` stack and closed B7, so Phase 5.2 is started and both slices are runtime-verified. The remaining blockers and the HTTP API probes are recorded above.*

**Phase 4 is complete. Phase 4.6 closed it with a consolidated verification, and added no new capability.** From a clean `node_modules`, `npm ci`, `npm run typecheck`, `npx cds compile db --to sql` and `npm test` were all re-run: **87 tests pass in 20 suites**, 0 fail, 5 tables and 7 unique constraints compile, and the application serves `HealthService`, `IntegrationService` and `SupplierService` plus the static UI. A live smoke test re-verified every Phase 4 capability together — ingestion 201/200/409 with `UNKNOWN_SUPPLIER`, `SOURCE_ORDER_ALREADY_INGESTED`, `TOTAL_AMOUNT_MISMATCH` and `UNSUPPORTED_UNIT`; supplier isolation with a scoped 404 on another supplier's order and on a cross-supplier decision; accept to `ACCEPTED`/v1, a date update to v2, a reject to `REJECTED`, a `responseId` replay returning the original decision, and a stale version refused with `STALE_RESPONSE_VERSION`; and the UI assets with `limit=1` paging and `INVALID_PAGINATION` on a malformed value. **Contract reconciliation found no implementation defect**: every documented point — the ingestion route and its nested DTO, receipt semantics, `deliveryId` idempotency by database constraint, source-order uniqueness as a separate rule, the supplier routes and bound-action wire shape, attribute-derived supplier identity, scoped-404 isolation, `responseVersion`, `responseId` replay, the pending response, pagination and the error envelope — matches the running code. Five items of **documentation drift** were corrected: printed test entries were called "suites" in the 4.3 and 4.4 guides when `health.test.ts` contributes a standalone top-level test (87 tests, 20 suites, 21 printed entries); the 4.3 guide described `SupplierService` as not existing; its "Phase 4.4 remains unimplemented" 404 evidence is now framed as a checkpoint and preserved rather than altered; the 4.4 guide's "no pagination" limitation is marked resolved by 4.5; and its supplier error-code count is noted as **14** after 4.5 added `INVALID_PAGINATION`. The portal now exposes 21 application-defined codes on ingestion and 14 on the supplier service, and **no persistence entity is writable through any service**. Guarantees verified: replay by `deliveryId` and by `responseId`, refusal of a reused identity with changed content, refusal of a stale version, and decision-plus-pending-response atomicity demonstrated by forcing the second write to fail. **Not guaranteed and not claimed: no concurrent race was ever exercised on either service** — the Phase 4.3 delivery race branch recorded zero hits under instrumentation, so the constraints are proven to fire but the concurrent paths stay reasoned. Security limits are unchanged and stated plainly: mocked authentication, an unauthenticated integration endpoint, local HTTP only, and no roles. OI-04 stays **closed** on the Phase 4.3 and 4.4 runtime evidence, re-verified here. OI-13, the `__OperationControl` UX gap and the permissive RAP authorization stub remain **open and untouched**, and `sendToSupplier` remains Phase 5. Eight items of CAP-specific debt travel forward, led by the unraced concurrency, the unauthenticated ingestion endpoint and unverified HANA portability. The Phase 5 handoff is recorded with the seven identifiers it must carry — `deliveryId`, `sourceSystem`, `sourceOrderId`, `sourceRevision`, `portalOrderId`, `responseId`, `responseVersion` — and there are no cross-database foreign keys. See the [Phase 4.6 closure](cap-supplier-portal/docs/phase-4-6-closure.md). **Phase 5 is not started.**

**Phase 4.5 — the minimal supplier UI — is locally runtime-verified. Phase 4 as a whole remains in progress.** A supplier can now do the whole flow in a browser. The UI is plain HTML, CSS and ES modules served from `app/` by CAP's static handler: **no framework, no build step, and no dependency added**. Presentation logic lives in [order-view.mjs](cap-supplier-portal/app/lib/order-view.mjs) as pure functions with no DOM or fetch, and the test suite imports the same file the browser loads, so what is tested is what runs. **It uses `SupplierService` and nothing else** — a test asserts structurally against the shipped script that the only base path is `/rest/supplier/v1`, that `/rest/integration/v1` and `/odata/` appear nowhere, that `PATCH`, `PUT` and `DELETE` appear nowhere, and that `supplierCode` is never written into a request. Controls follow the lifecycle: `RECEIVED` offers accept and reject, `ACCEPTED` offers only a date revision, `REJECTED` offers nothing — a mirror of the backend rules that duplicates none of them, since a wrongly drawn button is still refused by the service. Every command carries a **fresh `responseId`** and the **loaded `responseVersion`**; the user never types a version, and after every success the order is re-read from the service rather than patched from the response body. `responseDeliveryStatus: PENDING` is shown verbatim, so the screen never implies SAP has been told. **Pagination was finally defined and implemented**, closing an item deferred twice: `?limit=` (default 20, server maximum 100, clamped rather than refused) and `?offset=`, explicitly not `$top`/`$skip` because the contract rules out assuming OData syntax on REST — neither had any effect before this subphase. The result envelope is deliberately **a bare array with no total count**, because wrapping it would change a read contract Phase 4.4 verified; the client pages until it gets a short page. Items are not paged, since ingestion already bounds an order to 100 lines and a default of 20 would silently truncate a detail view. A malformed `limit` is 400 `INVALID_PAGINATION`. **Evidence, all local execution rather than SAP:** from a clean `npm ci`, typecheck reported no diagnostics and `npm test` passed **87 tests in 20 suites**, including every Phase 4.1–4.4 test; all eleven browser scenarios were driven by hand, SUP001 saw only its two orders and SUP002 only its one, an accept moved `PO00001001` to `ACCEPTED`/v1 and a date revision to v2, a reject moved `PO00001003` to `REJECTED`/v1 with its reason, and tampering with another supplier's order id returned **404 on the detail, the items and the accept route alike**. Supplier identity remains the Phase 4.4 mocked mechanism and the page says so in a banner: **local mock sign-in, not production authentication**. One bug was caught only by looking at the rendered page — an author `display: flex` rule beat the user agent's `[hidden]`, leaving every toggled panel visible. See the [Phase 4.5 guide](cap-supplier-portal/docs/phase-4-5-supplier-ui.md). Phase 4.6 is closure; nothing is sent to SAP before Phase 5.

**Phase 4.4 — the supplier-facing service and supplier decisions — is locally runtime-verified. Phase 4 as a whole remains in progress.** `SupplierService` is served at `/rest/supplier/v1`, separate from `IntegrationService` so the two can carry different authentication and permissions. Reads are `@readonly` projections with explicit element lists — no FK column, no SAP correlation field — narrowed by a `before READ` hook that adds the caller's scope to the query itself, so one rule covers the collection, a key read, `$expand` and navigation. **Supplier identity comes from the authenticated user's attributes and never from the payload**, as the architecture requires; locally that is CAP mocked auth mapping `supplier1` to `SUP001` and `supplier2` to `SUP002`, which is **local supplier isolation, not production authentication** — XSUAA and IAS remain Phase 8. Knowing another supplier's order id is not enough: a foreign id returns a scoped **404** with no fields disclosed, chosen over 403 because a 403 would confirm the row exists. The three decisions are bound actions at `POST /Orders/{portalOrderId}/accept|reject|updateEstimatedDeliveryDate`, exactly the documented paths: `accept` needs `RECEIVED` and treats an omitted date as unknown, `reject` needs `RECEIVED` and a non-blank reason and takes no date parameter at all, and `updateEstimatedDeliveryDate` needs `ACCEPTED`, keeps the decision and increments the version. A date may not precede the request's UTC date, but **replay detection runs first**, so a replayed command whose date has since passed still succeeds. Every command carries a stable `responseId` and a required `expectedResponseVersion`: an identical replay returns the original decision with 200, the same id with different content is 409, and a stale precondition is 409 rather than a silent overwrite. **The decision and its pending outbound response commit in one transaction** — `SupplierResponseDeliveries` was pulled forward from Phase 5 for the same reason `DeliveryReceipts` was in 4.3, carrying only identity, version, payload and state; every row is `PENDING` and **nothing is sent to SAP**, which is Phase 5. **Evidence, all local Node.js execution rather than SAP:** from a clean `npm ci`, typecheck reported no diagnostics and `npm test` passed **63 tests in 14 suites**, including every Phase 4.1–4.3 test; eight manual HTTP scenarios were executed, and the Phase 4.3 endpoint still answered 201/200/409/400 on the same live server. **The atomicity claim is demonstrated, not asserted**: a planted response row takes the `(order, version)` slot the next decision needs, the handler's order update succeeds, its response insert violates the documented uniqueness rule, and the order is verified to remain `RECEIVED` at version 0. One limit is stated rather than solved: two genuinely simultaneous decisions on one order were never raced, so the version precondition is verified sequentially and the concurrent path is reasoned. A Phase 4.3 lesson was applied in advance — action parameters carry no `not null`, because CAP's `ASSERT_MANDATORY` runs before any handler and would have made the contract's own error codes unreachable. See the [Phase 4.4 guide](cap-supplier-portal/docs/phase-4-4-supplier-service.md). OI-04 is now closed by runtime evidence. The supplier UI is Phase 4.5.

**Phase 4.3 — the integration-facing order ingestion API — is locally runtime-verified. Phase 4 as a whole remains in progress.** `POST /rest/integration/v1/Orders` is live on `IntegrationService`, a service kept separate from the Phase 4.4 supplier service so the two can carry different permissions. It exposes **no persistence**: `IntegrationService.Orders` is a service-local contract entity with `@insertonly`, not a projection on `pih.portal.Orders`, so there is no generic CRUD path into the database and `GET` returns 405. The request body is the nested *Mapped CAP order* from [API_CONTRACTS.md](API_CONTRACTS.md) used verbatim — `source{}`, `amount{}`, `lines[].product{}`, decimals as strings — and a custom CREATE handler is the only thing that converts it into the flat `pih.portal` shape. 20 application-defined integration-contract validation codes are reachable over HTTP and are enforced with exact `BigInt` decimal arithmetic rather than floating point, covering schema version, source identity, supplier resolution, EUR-only currency, PCE-only units, line-number and item-id uniqueness, positive quantity, non-negative price with zero explicitly valid, per-line currency consistency, `round(quantity × unitPrice, 2)` against `lineAmount`, and the header total against the sum of the lines. **None of them re-decides a SAP rule**; RAP still owns the commercial lifecycle. A twenty-first code, `INVALID_PAYLOAD`, is an internal `normalizeDelivery` guard that no request body can reach — CAP refuses a non-object JSON scalar with its own `ASSERT_DATA_TYPE` before the handler runs — so it is retained and covered by a direct test, but is deliberately not counted in the HTTP error-code set. Server-owned fields are set by the handler and absent from the contract, so a caller cannot supply `status`, `responseVersion` or either timestamp. **Idempotency rests on a database constraint, not a check in code**: `DeliveryReceipts(sourceSystem, deliveryId)` is `UNIQUE`, an exact replay returns the *original* stored receipt with 200, and a reused `deliveryId` with different content is 409 with the stored order untouched. The hash is taken over a normalized projection, so a replay that reorders every JSON member and writes `1500.0` instead of `1500.00` is still recognised as the same delivery. Delivery uniqueness and source-order uniqueness stay distinct: a *new* `deliveryId` carrying an already-ingested source order and revision is 409 `SOURCE_ORDER_ALREADY_INGESTED` naming the original delivery, while the same source order at a new revision is accepted. `DeliveryReceipts` was **pulled forward from Phase 5 deliberately**, because the replay and conflict rules are unanswerable without a stored hash and receipt; `DeliveryIntent`, `SupplierResponseDelivery` and attempt history were not pulled forward with it. One schema change: `Orders.supplier` became `not null`, justified because ingestion is the only writer and it refuses an unresolvable supplier with 400. **Evidence, all local Node.js execution rather than SAP:** from a clean `npm ci`, typecheck reported no diagnostics and `npm test` passed **37 tests in 8 suites**, including every Phase 4.1 and 4.2 test unchanged; the four contract scenarios were also exercised by hand, and `/rest/supplier/v1/Orders`, its `accept`/`reject` routes and `/odata/v4/Orders` all return 404, so Phase 4.4 is verifiably absent. Two CAP behaviours had to be corrected to keep the documented contract: structured elements are flattened unless `cds.odata.structs` is set, and a `UUID`-typed key is auto-generated when omitted, which silently made the mandatory `deliveryId` optional until a test caught it. **One limit is stated rather than solved:** the handler's concurrent-race branch was never entered — instrumented runs recorded zero hits, because a single local SQLite connection serializes the two requests — so the invariant is verified and that code path is reasoned. See the [Phase 4.3 guide](cap-supplier-portal/docs/phase-4-3-integration-ingestion.md). OI-04 is now partly closed; the supplier decision API, supplier isolation and the supplier UI remain Phases 4.4 and 4.5.

**Phase 4.2 — the CAP persistence and domain model — is locally runtime-verified. Phase 4 as a whole remains in progress.** [db/schema.cds](cap-supplier-portal/db/schema.cds) defines namespace `pih.portal` with `Suppliers`, `Orders` and `OrderItems`, implementing the CAP persistence table already specified in [docs/architecture/domain-model.md](docs/architecture/domain-model.md) rather than a new design and **never** a copy of `ZJP_PO_H`/`ZJP_PO_I`: the portal says `externalOrderNumber`, `productCode`, `lineNumber`, `unitPrice`, `lineAmount` and `uom` `PCE`, and omits company code, purchasing organization, purchasing group and the transmitted supplier name because the contract says the portal does not need them. `Orders` keeps two identities on purpose — its own `ID` plus the full `sourceSystem`/`sourceOrderId`/`sourceRevision` triple — since there are no cross-database foreign keys in this project. Relationship kinds were chosen rather than defaulted: `Orders.items` is a **Composition** because a line dies with its order, while `Orders.supplier` and `Suppliers.orders` are **Associations** because a supplier outlives every order it receives; a test deletes an order and asserts both halves of that. `cuid` is used for the UUID key; `managed` is deliberately not, because it would rename the contract's `receivedAt` to `createdAt` and add `createdBy`/`modifiedBy` columns nothing can populate before Phase 4.4 — its `@cds.on.insert: $now` mechanism is used without its naming. `@assert.unique` produced real database `UNIQUE` constraints in the generated DDL, including the `Orders(sourceSystem, sourceOrderId, sourceRevision)` rule the Phase 4.3 deduplication depends on. `@cap-js/cds-typer` now generates `#cds-models` types, wired into `pretypecheck`/`pretest` because the output stays git-ignored. **Evidence, all local Node.js execution rather than SAP:** from a clean `npm ci`, `npm run typecheck` reported no diagnostics and `npm test` passed 9 tests in 3 suites, covering model deployment, the composition/association distinction, all three entities queried, both navigations, the cascade, and the Phase 4.1 health endpoint unchanged. The server loads 3 model files, initializes all three CSV fixtures and still serves `HealthService` alone — `/rest/supplier/v1/Orders` and `/odata/v4/Orders` both return 404, which is the Phase 4.2 boundary verified rather than asserted. One Phase 4.1 setting had to change: `tsconfig.json` moved to `module`/`moduleResolution` `node16` and dropped its `#cds-models/*` `paths` entry, because that entry is read by `tsc` **and** by `tsx`, and cds-typer's `.d.ts`/`.js` split gives those two consumers no common target; `node16` resolves the subpath through the `imports` field in `package.json` instead. See the [Phase 4.2 guide](cap-supplier-portal/docs/phase-4-2-domain-model.md). Nothing validates, ingests or decides anything yet: `IntegrationService`, `SupplierService`, the ingestion DTO, deduplication, receipts, supplier authorization and the accept/reject/date commands are Phases 4.3 and 4.4, and `DeliveryReceipts` and `SupplierResponseDelivery` remain Phase 5. OI-04 stays open.

**Phase 4 is started, and Phase 4.1 — the CAP project foundation and local runtime — is locally runtime-verified.** [cap-supplier-portal](cap-supplier-portal/) is now a real CAP 10 project on Node.js 22 with TypeScript. `npm ci`, `npm run typecheck`, `npm test` and `npm start` were all executed and all succeeded, and `GET http://localhost:4004/health/ping` returned HTTP 200 with `{"status":"UP","component":"cap-supplier-portal","phase":"4.1"}` from a handler the start-up log confirms was resolved as `srv\health-service.ts`. **This is local Node.js execution evidence and a weaker claim than the SAP runtime evidence behind Phases 1–3**: nothing in Phase 4 runs in or against an SAP system, and Phase 4 makes no contact with SAP at all. The foundation deliberately carries no business model and no business API — `npx cds compile srv --to sql` produces no output, because no entity exists — so that a failure in a later subphase is attributable to that subphase rather than to the toolchain beneath it. Phase 4 is subdivided as **4.1** foundation, complete; **4.2** persistence and domain model; **4.3** integration-facing ingestion with `deliveryId` deduplication and the receipt; **4.4** supplier-facing read and decision API with row-level supplier isolation; **4.5** minimal supplier UI; **4.6** closure. Ingestion and the supplier surface are separated because the architecture requires those two services to stay separable with distinct permissions. Two CAP 10 facts were established by running it rather than by reading: CAP resolves `.ts` handlers only when `CDS_TYPESCRIPT` is set, and `cds.test` now lives in the separate `@cap-js/cds-test` package. **Phase 4 as a whole is not complete.** ADR-004 still governs the boundary — SAP RAP owns the purchase-order lifecycle and CAP owns supplier-facing persistence and supplier decisions, never a second implementation of the SAP rules. See the [Phase 4.1 guide](cap-supplier-portal/docs/phase-4-1-cap-foundation.md). OI-04 stays open until Phases 4.3 and 4.4.

**Phase 3.2 UI annotations are SAP runtime-verified complete, which completes Phase 3.** Two `#CUSTOMER` metadata extensions, [zjp_c_purchaseorder.ddlx](abap-rap/cds/zjp_c_purchaseorder.ddlx) and [zjp_c_purchaseorderitem.ddlx](abap-rap/cds/zjp_c_purchaseorderitem.ddlx), drive a working Fiori Elements List Report and Object Page over the Phase 3.1 OData V4 service. Verified: `@Metadata.layer: #CUSTOMER` — ADT offered `#CORE`, `#CUSTOMER`, `#INDUSTRY`, `#LOCALIZATION`, `#PARTNER`; `@UI.headerInfo`; `@UI.lineItem` on both entities; `@UI.selectionField`; `@UI.identification`; and `@UI.facet` with `#IDENTIFICATION_REFERENCE`, `#LINEITEM_REFERENCE` and `targetElement: '_Items'`. The List Report shows Purchase Order, Supplier, Status and Total Amount beside a Filter Bar of Editing Status, PurchaseOrderNumber, Supplier and Status, where Editing Status is framework-provided by draft handling; **Go** loaded real data, `PO00000002` through `PO00000010`, with blank numbers on pre-2.7E and draft-cancelled rows exactly as the allocation rule predicts. The Object Page shows header `PO00000004` / `SUP010`, a General Information facet of ten fields, and an Items table rendering Item `10`, Material `MAT001`, Quantity `2 EA`, Net Price `750.00 EUR`, Item Total `1,500.00 EUR`. **One compiler finding is preserved as a negative result: `@UI.facet` placed at top level beside `@UI.headerInfo` is invalid on this target** — ADT returned 11 "used at wrong position (wrong scope)" errors — and it must sit inside the `annotate entity … with { }` body. The two entity-level annotations do not share a placement rule here. **Units and currency need no annotation**: the Phase 1 `@Semantics` markers propagate through the projections into OData and Fiori renders `2 EA` and `750.00 EUR` on their own, which settles the decision not to add `UnitOfMeasure` or `Currency` as separate item columns. Activating an extension refreshes `$metadata` with no binding republish, which matters because publishing on this Customizing client requires `/IWFND/V4_ADMIN`. See the [Phase 3.2 guide](abap-rap/docs/phase-3-2-ui-annotations.md). Four limits remain open and unchanged: `__OperationControl` advertises bound actions the lifecycle would reject, a UX gap rather than a rule defect since the handlers still enforce every transition; dynamic feature control stays out of scope; authorization remains the permissive study stub; and OI-13 stale-ETag and concurrency behavior remains untested.

**Phase 3.1 OData V4 service exposure is SAP runtime-verified complete.** The RAP purchase order BO is now reachable from outside the system. An ADT-generated projection behavior definition on `ZJP_C_PurchaseOrder` declares `projection; strict ( 2 ); use draft;` and re-exposes root create/update/delete, the five business actions `submit`, `approve`, `reject`, `cancel` and `removeItem`, the five framework draft actions, and the `_Items` association with draft; the child block exposes `update` only, so the base `internal delete` keeps item removal behind `removeItem`. Service definition `ZJP_UI_PURCHASEORDER` exposes `ZJP_C_PurchaseOrder` as `PurchaseOrders` and `ZJP_C_PurchaseOrderItem` as `PurchaseOrderItems`; service binding `ZJP_UI_PURCHASEORDER_O4` is OData V4 – UI, service version `0001`. The published service root and the full buyer-service contract are recorded in [API_CONTRACTS.md](API_CONTRACTS.md), which no longer describes that surface as proposed. Client 100 is a Customizing client, so local publishing from ADT was refused and the service group was published through `/IWFND/V4_ADMIN`. **No Phase 2 source changed**: the base behavior definition, behavior pool, views, abstract entities and both console test classes are untouched. The learner's HTTP evidence covers the service document, `$metadata`, both entity sets, draft metadata, all ten bound and draft actions with their parameters, `$expand` navigation, `approve` and `reject` over HTTP including the exact Phase 2 rejection text `Only submitted orders can be decided.`, the full Edit/PATCH/Activate and Edit/PATCH/Discard draft cycles, creation through to submission where `PO00000010` appeared at `submit` and not at `Activate`, and a `SUBMITTED` order whose technical draft inherited both the status and the number and refused the first content change with `Only draft orders can be changed.` Two items are recorded as unproven rather than solved: `__OperationControl` advertises actions the lifecycle would reject, which is a UX gap and not a rule defect because the handlers still enforce every transition; and `OptimisticConcurrency` appears in the metadata while stale-ETag and multi-user behavior remain untested under OI-13. See the [Phase 3.1 guide](abap-rap/docs/phase-3-1-odata-service-exposure.md). [Phase 3.2](abap-rap/docs/phase-3-2-ui-annotations.md) then added the Fiori Elements surface on top of this service.

**Phase 2 is complete. Phase 2.7E `PurchaseOrderNumber` allocation is SAP runtime-verified, and the same run confirmed Phase 2.7D-2.** `submit` now draws exactly one number from the configured number range object `ZJP_PO`, interval `01`, through `CL_NUMBERRANGE_RUNTIME=>NUMBER_GET`, formats it as `PO` plus eight zero-padded digits, and writes it together with `Status` in the same local-mode update. **Allocation happens on a successful `DRAFT` → `SUBMITTED` only** — not on create or save, not through a determination, and not through RAP late numbering. This settles OI-02 inside Phase 2 as an implementation rather than a deferment, and supersedes two earlier, mutually inconsistent statements in the documentation. A `DRAFT` order carries no number; an order cancelled directly from `DRAFT` never receives one; every state after `SUBMITTED` keeps exactly the number drawn at submission. The number range object is the sole uniqueness authority: never `MAX + 1`, never reset or reused, gaps accepted, and no database unique index added at this step. The target returns `NRLEVEL` as twenty digits against an eight-digit interval, so the handler verifies the leading twelve digits are zeros and fails the action rather than truncating a wider value into a wrong number. Only `CX_NR_OBJECT_NOT_FOUND` and `CX_NUMBER_RANGES` are caught. No behavior definition, CDS or table change was needed. The regression captures whatever number each run actually draws and compares later lifecycle states against that captured value; it asserts no literal, because the interval level is environmental and gaps are valid. The learner's run confirms the format, the first integrated number `PO00000002`, preservation across refused re-submission, refused commercial mutations, `approve`, `reject` and both cancel paths, a `DRAFT` → `CANCELLED` order that correctly received none, and a technical draft of a submitted order carrying `PO00000009` alongside `SUBMITTED` and total 1500. Zero `STOP` markers in either console class. See the [Phase 2.7E guide](abap-rap/docs/phase-2-7e-purchase-order-number.md).

**Phase 2.7D-2 root `DELETE` narrowing is SAP runtime-verified by the same run.** `delete ( precheck )` restricts physical deletion to business `Status` `DRAFT`, with the transient `INITIAL` carve-out; `SUBMITTED`, `APPROVED`, `REJECTED` and `CANCELLED` are refused with `Only draft orders can be deleted.` Technical draft `Discard` is unaffected. Terminal fixtures are now deliberate residue: roughly nine header rows and ten item rows survive each full run, every UUID is printed for manual identification, and four positive `DRAFT` deletion paths still prove that deletion works where it is allowed. See the [Phase 2.7D-2 section](abap-rap/docs/phase-2-7d-1-cancel-action.md#phase-27d-2-root-delete-narrowed-to-draft-runtime-verified).

**Phase 2.7D-1 the `cancel` lifecycle action is SAP runtime-verified complete.** One root action was added: `cancel` moves business `Status` to `CANCELLED` from `DRAFT`, `SUBMITTED` or `APPROVED`. It is root-bound, parameterless, active-instance only, and writes `Status` and nothing else — no `RejectionOrigin`, no `RejectionReason`, no `PurchaseOrderNumber`, no integration field, no totals recalculation. Eligibility is a positive allow-list, so `REJECTED`, `CANCELLED` and the transient `INITIAL` are all refused without the `INITIAL` carve-out the Phase 2.7C prechecks need; `REJECTED` and `CANCELLED` stay terminal, and rework means a new order. A technical draft is refused before any read. **`CANCELLED` is commercially immutable through the existing Phase 2.7C invariant with no new code**: no `CANCELLED`-specific branch was added to any precheck or to `removeItem`. `APPROVED` → `CANCELLED` carries **no delivery-request guard** in Phase 2, deliberately: `IntegrationStatus` and `DeliveryId` are readonly and no handler can write them, so the condition is unrepresentable as true and a guard would be an untestable tautology — it belongs with `DeliveryIntent` and `sendToSupplier` in Phase 5. The learner's run confirms all three transitions with persistence and `TotalAmount` 1500, `PurchaseOrderNumber` still initial, `REJECTED` refusing cancel while keeping `RejectionOrigin = APPROVER`, a `CANCELLED` order refusing re-cancel, `submit`, `approve`, `reject` and every commercial change, and a `SUBMITTED` technical draft refusing cancel. See the [Phase 2.7D-1 guide](abap-rap/docs/phase-2-7d-1-cancel-action.md). Root `DELETE` was **intentionally unchanged** at this checkpoint; Phase 2.7D-2 later narrowed it to `DRAFT`. `sendToSupplier` is not started, `PurchaseOrderNumber` was unallocated at this checkpoint and is allocated from Phase 2.7E onwards, and authorization is still the permissive study stub, so `cancel` names a transition and not an access control.

**Phase 2.7C approver decisions are SAP runtime-verified complete.** Two root actions were added: `approve` moves business `Status` from `SUBMITTED` to `APPROVED`, and `reject` moves it to `REJECTED` while setting `RejectionOrigin = 'APPROVER'` and storing the mandatory `RejectionReason` supplied through the new abstract entity `ZJP_A_Reject`. Both are active-instance only and refuse technical drafts. The same subphase generalized the Phase 2.7B rule into a lifecycle invariant: **commercial content is editable only while business `Status` is `DRAFT`**, so every persisted state after `DRAFT` — `SUBMITTED`, `APPROVED`, `REJECTED` and any state added later — is immutable for root updates, item updates, create-by-association and `removeItem`. `Status` `INITIAL` passes the prechecks strictly as a transient creation-compatibility case before `initializeStatus` establishes `DRAFT`; it is not an editable business state. The learner's run confirms both transitions with persistence, `RejectionOrigin = APPROVER`, the reason stored verbatim, rejection of every re-decision and of every commercial mutation on `APPROVED` and `REJECTED`, a refused reason-less reject that wrote nothing, and a technical draft of a submitted order refusing updates plus both decisions. See the [Phase 2.7C guide](abap-rap/docs/phase-2-7c-approve-reject.md). Authorization is still the permissive study stub, so no approver-role enforcement exists. At that checkpoint `cancel` and `sendToSupplier` were both unimplemented; `cancel` was added by [Phase 2.7D-1](abap-rap/docs/phase-2-7d-1-cancel-action.md). `PurchaseOrderNumber` was unallocated at this checkpoint, and root `DELETE` remained allowed at any status.

**Phase 2.7B post-submission commercial immutability is SAP runtime-verified complete.** Once business `Status` reaches `SUBMITTED`, root commercial updates, item commercial updates, create-by-association and `removeItem` are all rejected, for active and technical draft instances alike. Enforcement is server-side through RAP prechecks declared as operation options — `update ( precheck )` on root and item, `create ( precheck )` on `_Items` — scoped by `%control` so that only user-writable commercial fields are guarded and the framework's own `Status` and `TotalAmount` writes still pass. The learner's run confirms all six rejections with persistence unchanged, root `DELETE` still working on a submitted order, a `DRAFT` control that still recalculates correctly, and a draft taken from a submitted order that carries `SUBMITTED` and rejects both root and item changes. See the [Phase 2.7B guide](abap-rap/docs/phase-2-7b-post-submission-immutability.md). No validation-on-save backstop and no feature control were needed. `PurchaseOrderNumber` is still deferred, root `DELETE` of a submitted order remains allowed by design, and `Activate` over a `SUBMITTED` instance is untested.

**Phase 2.7A root action `submit` is SAP runtime-verified complete.** It moves business `Status` from `DRAFT` to `SUBMITTED` on active instances only, requires at least one current item, and duplicates no Supplier/Quantity/NetPrice logic. The learner's run confirms the active transition with the buffered total unchanged at 1500, `sy-subrc` 0 on commit, `SUBMITTED` persisted in ZJP_PO_H and `PurchaseOrderNumber` still initial. All three rejections returned their exact texts and changed nothing: `Only orders in status DRAFT can be submitted.` for a re-submit, `Submit requires at least one item.` for an order with no items, and `Submit is not allowed on a draft instance.` for both a buffer-only and a saved technical draft, whose totals stayed 1500 and 1900. The Phase 2.5 and 2.6 regressions still pass. See the [Phase 2.7A guide](abap-rap/docs/phase-2-7a-submit-action.md). Phase 2.7B has since closed the editability gap; `PurchaseOrderNumber` allocation remains deferred.

**Phase 2.6 technical RAP draft and `removeItem` are SAP runtime-verified complete.** Target: SAP_BASIS 758 SP01, S4CORE 108 SP01, ADT Core 3.60.3 / Business Object Tools 1.209.0. The supported item-deletion path is the root-bound `removeItem` operation for both active and draft instances. It preserves the complete root `%tky`, verifies that the requested item belongs to that exact root/draft instance, deletes through managed internal EML, and recalculates the header from surviving items, including zero after the last item. Standard child DELETE remains internal. The ownership-negative runtime test returned FAILED for `removeItem` plus `Item does not belong to this Purchase Order.`, while both roots, all items and totals remained unchanged.

**Phase 2.5C validateNetPrice is SAP runtime-verified complete.** It rejects NetPrice < 0 on item create/price change while allowing zero. See the [Phase 2.5C guide](abap-rap/docs/phase-2-5c-net-price-validation.md). The [Phase 2.5B evidence](abap-rap/docs/phase-2-5b-quantity-validation.md) remains runtime-verified: both Quantity negative commits returned 4 with unchanged persistence and the full regression passed.

| Subphase | Status |
| --- | --- |
| 2.1 Behavior Definition | Complete for the current managed BO |
| 2.2 Behavior pool / instance authorization stub | Complete; permissive study policy only |
| 2.3 EML CRUD runtime verification | Complete; learner-supplied successful SAP console output |
| 2.4 Determinations | Complete: Status, item totals and scoped header aggregation runtime-verified |
| 2.5 Validations | 2.5A Supplier and 2.5B Quantity runtime-verified; 2.5C NetPrice runtime-verified |
| 2.6 Technical draft | Complete; technical draft and active/draft `removeItem` runtime-verified on SAP |
| 2.7 Business actions | Complete: 2.7A `submit`, 2.7B immutability, 2.7C approve/reject, 2.7D-1 `cancel`, 2.7D-2 root `DELETE` narrowing and 2.7E `PurchaseOrderNumber` allocation all runtime-verified. `sendToSupplier` is Phase 5 and does not belong to Phase 2 |
| 3.1 OData V4 exposure | Complete: projection behavior definition, service definition `ZJP_UI_PURCHASEORDER` and binding `ZJP_UI_PURCHASEORDER_O4` runtime-verified over HTTP |
| 3.2 UI annotations | Complete: root and item `#CUSTOMER` metadata extensions runtime-verified, rendering a Fiori Elements List Report and a two-facet Object Page |

## Next steps

Phase 2.5C compiler feedback: the console test's elementary PRICES table constructor rejected the integer literal 750 as incompatible with its row type. Both test prices now use explicit CONV zjp_po_i-net_price expressions. The learner's successful SAP run verifies the corrected test; BDEF and handlers were unchanged by this correction.

Phase 2.4C partial SAP evidence: create 1900, Quantity update 2650 and NetPrice update 2800 passed with persistence. The debugger confirmed different ME objects across delete precheck/determination: delete_context went from one row to empty (sy-subrc 4). The stateful bridge is withdrawn. The historical stateless correction read only immutable parent identities from ZJP_PO_I for previously committed active items, then reads current amounts and updates totals through local-mode EML. No direct SQL writes or shared state are used. Delete totals 2400 and 0 subsequently passed in SAP; Phase 2.4C is complete within the documented committed-item scope. Uncommitted-item deletion and technical draft were explicitly unsupported by that lookup. The verified known-root navigation supports the Phase 2.6 replacement, which is now runtime-verified for active, saved-draft and buffer-only draft deletion.

[Phase 2.4B runtime evidence](abap-rap/docs/phase-2-4b-runtime-evidence.md), 2026-09-13: the learner verified 2 × 750 = 1500 before commit, 3 × 750 = 2250 after Quantity change, and 3 × 800 = 2400 after NetPrice change. Each value persisted after commit and the console ended `PASS: item totals 1500/2250/2400; status, CRUD and cleanup.`

**Phase 2 is closed, Phase 3.1 and 3.2 are both SAP runtime-verified, and Phase 4 is complete through 4.6.** Every subphase from 2.1 to 2.7E, plus the OData V4 exposure and its Fiori Elements surface, is SAP runtime-verified within its documented scope. The whole CAP Supplier Portal — foundation, domain model, ingestion API, supplier service and supplier UI — is verified by local Node.js execution only, which is a separate and weaker kind of evidence. **Phase 5 is open and partly built. Phase 5.1's connectivity half is runtime-verified — S/4 has called the deployed CAP endpoint over HTTPS with OAuth2 — while its design half remains design. Phase 5.2a through 5.2f are all SAP runtime-verified. Phase 5.2g was next and not started as at that checkpoint; it has since been implemented and SAP runtime-verified.** 5.2a populated the two PurchaseOrder header values a delivery needs, 5.2b built the `DeliveryIntent` persistence and RAP business object with its unique Table Index, 5.2c built the transport seam and its deterministic fake, and 5.2d built the outbound builder and the CAP mapper, which now produce the exact payload the deployed portal accepts. What is still unbuilt is the far end of the path: the real HTTP transport class, `sendToSupplier` and the coordinator.

1. Preserve the Phase 2.1–3.2 runtime-verified baseline: the `removeItem` deletion boundary; the active-only `submit`, `approve`, `reject` and `cancel` transitions; the precheck-enforced lifecycle invariant; root `DELETE` restricted to `DRAFT`; `PurchaseOrderNumber` allocated on submit; the OData V4 service definition and binding; and both metadata extensions, including `@UI.facet` placed inside the `annotate entity … with { }` body.
2. Phase 5 is open at 5.1: direct RAP–CAP integration. SAP builds the mapped outbound order and posts it to `/rest/integration/v1/Orders`, replaying the same `deliveryId` on a timeout; a sender later reads `PENDING` rows from `SupplierResponseDeliveries` and submits them toward SAP. The seven identifiers it must carry and the CAP-side debt it inherits are recorded in the [Phase 4.6 closure](cap-supplier-portal/docs/phase-4-6-closure.md), and the SAP-side design, blockers and probes in the [Phase 5.1 guide](abap-rap/docs/phase-5-1-outbound-foundation.md). Phase 3 needs no further subphase: the OData service and its Fiori surface are done, and the two items still open there are OI-13 stale-ETag and concurrency testing, now reachable because `OptimisticConcurrency` is in `$metadata`, and the OI-03 remainder, which is the separate integration projection belonging to Phase 5.
3. The connectivity prerequisites for Phase 5.2 are **settled by runtime evidence**: probes **P1, P2/P3, P4 and P12** are answered, the outbound HTTP chain is proven, the destinations and OAuth2 configuration exist, and blocker **B3 is resolved**. **P5–P11 are now the critical path**, and they are all payload-construction questions rather than transport ones — UUID canonical formatting (P5), decimal formatting under a comma-decimal user (P6), UTC timestamp formatting (P7), a unique secondary index on the new table (P8), `abap.string` as a non-key column (P9), JSON serialization with exact camelCase (P10), and the actual persisted integration-field values (P11). The **`OrderRevision` gap (B1) is resolved** by Phase 5.2a, along with **B2**, so a submitted order now carries `source.revision = 1` and an explicit `IntegrationStatus`; the outbound probe had got past B1 only by hand-building a payload, and that workaround is no longer needed. **P11 is also answered**, by the Phase 5.2a lifecycle summary reading persisted `ZJP_PO_H` rows rather than only source — with no migration of legacy rows. A thirteenth probe, **P13**, was added for a released **SHA-256** API, a gap the original list did not cover at all: `payload_hash` is specified as sha256 hex and no probe asked how to compute it. Of the open probes, **only P8 and P9 gate the next slice**, because both decide the shape of the `ZJP_PO_DLV` table artifact, which cannot be altered after activation without a conversion; P5, P6, P7, P10 and P13 belong to the builder, mapper and intent-creation slices. Still do not author an ABAP HTTP call from memory — P1 is the standing example, since the class that worked was not among the candidates this repository named.
4. Phase 5.2 is open and sequenced. **5.2a through 5.2g are done and SAP runtime-verified** — the header value gaps B1 and B2; the `DeliveryIntent` persistence and RAP stack with its unique Table Index, closing B7 and answering P8 and P9; the transport seam and its deterministic fake; the outbound builder and the CAP mapper, answering P5, P6 and P10; the real HTTP transport over the path-less base destination `ZJP_CAP_BASE`, answering P15; and `sendToSupplier` with the durable delivery intent, answering P13 and resolving **B4 for DEV with multi-environment configuration deferred**. Every technical unknown that gated 5.2f was closed by a probe first, including two that were removed again afterwards: the DeliveryIntent LUW probe and the cross-BO EML probe. **5.2g is done and SAP runtime-verified**: the coordinator that claims a `PENDING` intent under a lease, calls the verified transport, interprets the receipt per section 6.4 and writes the outcome back — it owns the `SENT` transition and lease acquisition and expiry. **All three probes that gated it are closed**: **P14** answered with `CL_SYSTEM_UUID=>CREATE_UUID_X16_STATIC( )`, **P16** answered with `CL_ABAP_TSTMP=>ADD`, and **P17** superseded by the permanent `ZJP_CL_DLV_SNAPSHOT_READER` now that `IF_XCO_CP_JSON_DATA` is known to expose `WRITE_TO` and no member accessors. **Two architectural decisions were locked before implementation**: `lease_seconds` is an explicit coordinator input with no production default, and `recordDeliveryResult` moved from 5.x into 5.2g as an **internal, unprojected** operation, because every header outcome field is `field ( readonly )` and an external coordinator cannot write it by plain EML. **Retry scheduling and `retryDelivery` stay deferred.** Projection exposure of `sendToSupplier` through `ZJP_C_PurchaseOrder` is also still deliberately deferred: the action is proven through EML before it is offered to a UI.
5. OI-14 status. Items 1, 2 and 3 are **resolved** — item 2 by the deployed runtime evidence above. **Item 4 is now split in two: HANA runtime verification — ingestion path: VERIFIED; HANA runtime verification — supplier decision/update path: OPEN.** The ingestion half is closed by real evidence: one valid order ingested with a real token, persisted as one Order, one OrderItem and one DeliveryReceipt in `pih-hdi`, replayed for 200 twice and refused 409 on a changed payload, with no `IntegrationService.Orders` table in the HANA catalog. The open half is open for a precise reason — **every write exercised was an INSERT**, so the next substantive steps are the `SupplierService` accept/reject UPDATE against HANA, the `responseVersion` increment, the `modifiedAt` update behaviour, and `SupplierResponseDeliveries` persistence. After those come the interactive `SupplierPortalUser` identity and the `supplier` attribute mapping, then SAP's outbound OAuth2 client. Historical detail on items 1 and 3 — `@cds.persistence.skip` on `IntegrationService.Orders` removed the empty persistence artifact with the service contract and the OData metadata unchanged, and moving the fixture CSVs to CAP's development-only `test/data` folder took the build from three `.hdbtabledata` files to none while local seeding stayed identical. Item 2 is **partially resolved**: production no longer resolves to mocked authentication, the XSUAA model is prepared and the `IntegrationClient` authority is token-level proven, but a deployed CAP must still accept a real token against `@requires: 'IntegrationClient'`, the `supplier` attribute mapping needs an interactive token, SAP S/4 needs its OAuth client configured, and the real `xsappname` must come from the deployment descriptor. Item 4 is **split: HANA runtime verification — ingestion path: VERIFIED; HANA runtime verification — supplier decision/update path: OPEN.**
6. Expect deliberate test residue and number-range gaps. Each full regression run leaves roughly nine header rows and ten item rows in terminal states and consumes about eight numbers. Both are intended: terminal orders are not physically deletable, and a consumed number is never reset or reused.
7. Preserve the supplied version baseline: SAPK-75801INSAPBASIS, SAPK-10801INS4CORE, ADT Core 3.60.3, BO Tools 1.209.0, Eclipse 4.40.0.

## Architecture decisions

The full register is in [ARCHITECTURE.md](ARCHITECTURE.md#architecture-decision-register). Key decisions: custom BO instead of standard MM posting; managed RAP and UUID composition; draft separated from business status; post-commit dispatch with synchronous HTTP first; RAP commercial ownership and CAP supplier-response ownership; standard CI mapping; explicit mock labels and evidence-based portfolio claims.

Phase 1 refinements: ZJP_ naming replaces the provisional names; explicit DEC monetary storage carries CDS currency semantics; optional persistence values use ABAP initial values with later API conversion. The root/child buyer projection redirects both relationships and omits four bookkeeping fields. The subsequent EML run verifies managed writes and root audit maintenance. Concurrent locking and stale-ETag checks remain untested. Phase 2.6 technical draft and active/draft `removeItem` behavior have learner-supplied SAP runtime evidence. The Phase 2.7A `submit`, Phase 2.7B precheck immutability and Phase 2.7C approve/reject transitions are also runtime-verified.

Compatibility baseline: remove the rejected standalone currency-code marker from ZJP_I_PurchaseOrder while preserving the amount reference. Omit the explicit transactional_query provider contract from ZJP_C_PurchaseOrderItem because of its redirected-parent association; retain the root contract. Preserve these working declarations rather than forcing identical root/child syntax.

Current BDEF design: managed implementation in class ZBP_I_PURCHASEORDER unique, strict(2), full persistence mappings, root CRUD, child update/internal-delete with create-by-association, root lock master and child lock dependent, managed UUID keys and one local-instance ETag per entity. Root authorization master (instance) and child authorization dependent by _PurchaseOrder explicitly establish the required hierarchy. The behavior pool is required for instance authorization; standard managed CRUD still needs no handwritten persistence methods. Computed/status/audit/result fields are readonly; managed UUIDs and audit values have framework-driven population, while custom determinations derive status and totals. Phase 2.6 runtime-verifies root `removeItem` with authorization delegated to update, preserving the existing study-only authorization policy. Phase 2.7A adds the parameterless root action `submit` under the same update-authorization delegation; the target accepted it under strict(2) in a with-draft BO and runtime-verified its transition and rejections. Phase 2.7B adds `update ( precheck )` on root and item and `create ( precheck )` on the `_Items` association, with ADT-generated `FOR PRECHECK` handlers scoped by `%control`. Phase 2.7C adds the parameterless `approve` action and the `reject` action with parameter ZJP_A_Reject. Phase 2.7D-1 adds the parameterless `cancel` action under the same update-authorization delegation, with no BDEF change beyond that single line and no change to the existing prechecks or to the root `delete` declaration.

Compiler-driven lessons: (1) authorization master (none) was rejected in this target release; (2) strict(2) requires every entity to participate in the authorization hierarchy; (3) root now uses authorization master (instance); (4) child uses authorization dependent by _PurchaseOrder; (5) the current BO requires an implementation class declaration; (6) the real SAP compiler is authoritative over generic examples. The earlier expectation of activating this BDEF without a custom class is withdrawn. Exact diagnostics are recorded in the [BDEF guide](abap-rap/docs/phase-2-base-managed-bdef.md#compiler-feedback-and-correction).

Additional compatibility lesson: the target-generated authorization request/result structures omit `%assoc-_Items`. Both references were removed from the handler; only `%update` and `%delete` are handled. Available `%...` components depend on the target release and BDEF, so ADT compiler/autocomplete are authoritative. The composition create declaration is retained; runtime behavior is tested through EML rather than inferred from an absent result field.

## Open issues

| ID | Issue | Resolve by |
| --- | --- | --- |
| OI-01 | Exact target recorded: SAP_BASIS 758 SP0001 / S4CORE 108 SP0001; separate ATC evidence not recorded | Target compiler/runtime remains authoritative |
| OI-02 | Closed: number-range availability and number assignment resolved in Phase 2.7E — object `ZJP_PO`, interval `01`, allocated by `submit`, SAP runtime-verified. Draft behavior is evidenced across Phases 2.6–2.7E; no stale-ETag conflict case was ever executed, which is now tracked as OI-13 | Phase 2 |
| OI-03 | **Scheduled by Phase 6.5a.** The remaining half — the restricted integration projection, its service definition and an OData V4 Web API binding — is now designed and assigned to **Phase 6.5b**, where `applySupplierResponse` needs an active-instance integration surface that is not the draft-enabled buyer UI service. Nothing is built yet. Prior status follows. **Partly closed by Phase 3.1:** OData V4 metadata and all ten action signatures with their parameters are verified on the buyer UI service `ZJP_UI_PURCHASEORDER`. Still open — concurrency headers are unproven (see OI-13), and the separate integration projection is Phase 5 | Phase 3, finalized before phase 5 clients |
| OI-04 | **Closed by Phase 4.4, re-verified in Phase 4.6.** Both halves are implemented and locally runtime-verified: REST structured ingestion in Phase 4.3, and the supplier bound-action wire behavior in Phase 4.4, executed over HTTP rather than inferred from code, and exercised again in the Phase 4.6 closure smoke test. CAP's REST adapter routes a bound action as `POST /<service-path>/<EntitySet>/<key>/<actionName>`, with the parameters as a flat JSON body and the `returns` type as the JSON response; the instance is addressed by the URL key and never by a parameter, which matches the paths API_CONTRACTS.md specified | Closed in Phase 4.4 |
| OI-05 | **In progress, not closed.** Phase 5.1 turned this from an open question into a specified one: the outbox persistence is designed (`DeliveryIntent` as a separate managed RAP BO, fifteen fields, snapshot of the source delivery DTO) and the dispatch API is designed behind a transport interface. Still open — outbound reachability is unresolved because CAP listens on `localhost` and neither permitted option has been chosen; the released ABAP HTTP client API set is **unknown**, with no reference to any HTTP client class anywhere in the repository; and the inbound leg to the RAP Web API is untouched. Twelve ADT probes and seven blockers are listed in the [Phase 5.1 guide](abap-rap/docs/phase-5-1-outbound-foundation.md) | Phase 5 |
| OI-06a | **Closed by Phase 6.1 runtime evidence.** Integration Suite availability, the Cloud Integration runtime, the HTTPS sender adapter, JSON-to-XML conversion and XSD validation are directly proven on a real tenant: a deployed `PIH_OrderDelivery_v1` accepted a valid source snapshot (Completed, 477 ms) and rejected `schemaVersion 2.0` at the schema (Failed, 490 ms). It took a **dedicated `PIH Integration` subaccount** to get there — the original trial tenant refused the runtime location `cloudintegration`, which was a provisioning problem and not an iFlow defect | Closed in Phase 6.1 |
| OI-06b | **Narrowed again by Phase 6.4, and still not fully closed.** Closed by 6.4 runtime evidence: the **SAP cutover itself**, mapping ownership moving to Cloud Integration on the active path, the coordinator posting the `PayloadSnapshot` verbatim, and the **`502` classification question** — an answered `502` is `PENDING`, an unanswered call is `UNKNOWN`, and `classify()` needed no change. **Still open:** the **final inbound security posture including CSRF**, and **retry and resilience**, which remain on platform defaults and have never been exercised — returning an intent to `PENDING` is classification, not recovery. **Not addressed by 6.4 and still open elsewhere:** the inbound supplier response (6.5). Prior narrowing follows. **Narrowed again by Phase 6.3, and still not fully closed.** Newly resolved by 6.3 runtime evidence: the **receiver adapter**, the **CAP call itself** over OAuth 2.0 client credentials, **receipt handling** (CI returns CAP's receipt unchanged) and the **Exception Subprocess** (controlled `502` for technical failures, with downstream `201`/`200`/`409` preserved as real portal answers because `Throw Exception on Failure` is OFF). **Still open:** the **final inbound security posture including CSRF**; **retry and resilience**, which ran on platform defaults and was never exercised; and how the coordinator's Section 6.4 receipt classification should treat a CI `502`. **Not touched by 6.3 and explicitly still open elsewhere:** the SAP cutover (6.4) and the inbound supplier response (6.5). Prior narrowing from Phase 6.2 follows. **Narrowed by Phase 6.2, not closed.** Resolved: Graphical Message Mapping behaviour, the target contract format (an **OpenAPI 3.0.3** document, not an XSD), and both conversion questions — `revision` and `lineNumber` emerge as JSON **numbers** once *Basic Data Type Handling* honours the target schema, and a single-element `lines` stays an **array** given the target declaration plus the `items` → `lines` structure mapping; proven with a one-item and a two-item payload. **Still open:** the receiver adapter, the CAP call itself, receipt handling and the Exception Subprocess, none of which exists yet. Also open, and newly identified by 6.2 runtime evidence: the **final inbound security posture**. Phase 6.2 runs with **CSRF protection disabled** on the HTTPS Sender after an authenticated POST was rejected `403` before reaching the flow; that is the verified current configuration and not a production decision. Either the caller implements the CSRF token fetch-and-use flow or another supported machine-to-machine approach is chosen, at Phase 8 security or the 6.4 cutover | Phase 6.3, with the security posture at Phase 8 / 6.4 |
| OI-07 | Identity provider, XSUAA, technical-client trust and supplier attributes | Phase 8; host-required authentication earlier |
| OI-08 | Exact Event Mesh offering, RAP channel support and dead-letter configuration | Phase 9 |
| OI-09 | API Management access and proxy endpoints | Phase 10 |
| OI-10 | ABAP export/import tooling and package-to-folder serialization | Phase 1 setup, verified with first real export |
| OI-11 | Closed: Phase 1 activation plus Phase 2 EML root/item reads and table snapshots are evidenced | No additional model verification required for this checkpoint |
| OI-12 | Closed: corrected BDEF/pool and EML consumer executed successfully in SAP | See Phase 2.3 evidence |
| OI-13 | Stale-ETag conflict behavior and RAP multi-user editing remain untested; the saved-draft `LOCKED` observation is single-user evidence only. Phase 7.5 separately proved the deployed **CAP inbound drainer** race: one claim owner, one competing skip and one transport attempt/history entry. That does not prove a RAP stale-ETag conflict or two concurrent SAP coordinator runs | Phase 3 editing test remains open; Phase 7.5 inbound concurrency PASS |
| OI-14 | **CAP HANA deployment readiness — four findings from the HANA preparation step. Items 1, 2 and 3 are resolved; item 4 is split into a verified ingestion path and an open supplier decision/update path.** (1) **RESOLVED.** `IntegrationService.Orders` produced a **physical persistence artifact**: because it is an entity definition with its own elements rather than a projection, the compiler gave it a table, so the production build wrote `IntegrationService.Orders.hdbtable` — a permanently empty table, since the CREATE handler maps every delivery into `pih.portal`. It was **pre-existing and database-agnostic**; the same table appeared when the whole model compiled for SQLite, and the Phase 4.6 figure of "5 tables" came from `cds compile db --to sql`, which sees only the `db/` folder. `@cds.persistence.skip` was **verified against the installed compiler before being applied** — `@sap/cds-compiler` 7.1.1 handles it in `lib/transform/db/cdsPersistence.js`, which sets `$ignore` on the artifact, and a search of the OData/EDM transform for the annotation returns nothing, so it suppresses the database artifact and leaves the service contract untouched. Applied to the entity in `srv/integration-service.cds`; see ADR-034. Verified: `IntegrationService.Orders.hdbtable` is **no longer generated** and is the only difference in the artifact list, **7 `.hdbtable` → 6**, with all 7 `.hdbindex`, 2 `.hdbview`, 3 `.hdbtabledata` and every intended `pih.portal` artifact unchanged; the generated **OData metadata is byte-identical** before and after; `npm test` 87 tests in 20 suites with 0 failures, the ingestion suite 28 tests with 0 failures, a clean typecheck and `cds build --production` passing with no warnings. The entity remains in the service contract and `POST /rest/integration/v1/Orders` is unchanged. (2) **RESOLVED.** Previously the production build resolved to **mocked authentication**, because `.cdsrc.json` pinned `requires.auth` with no profile qualifier and CAP's default mock set includes an accept-any-user `"*": true` entry. That is gone: mocked auth now sits under an explicit `[development]` key, `cds env requires.auth --profile production` resolves to `kind: xsuaa` with **no `users` key at all**, and `@sap/xssec` is a dependency — all written by `cds add xsuaa` except the profile scoping. Two named application roles replace the `authenticated-user` pseudo-role, which generated no scope and would have let a technical token read supplier data: `SupplierPortalUser` on `SupplierService` and `IntegrationClient` on `IntegrationService`, an endpoint that until now accepted any caller. `xs-security.json` is generated from those annotations by `cds compile --to xsuaa` and carries both scopes, their role-templates, the `supplier` attribute and a `userattributes` template. **The machine-to-machine authority is runtime-proven at the token level** against a real XSUAA instance in Trial `us10-003` space `dev`: without top-level `authorities` a `client_credentials` token carried only `uaa.resource`, and with `authorities: ["$XSAPPNAME.IntegrationClient"]` it carried the resolved `IntegrationClient` scope while **`SupplierPortalUser` stayed absent**, so the two surfaces are isolated in a technical token. `SupplierPortalUser` is deliberately excluded from `authorities`, since that key grants a scope to the application's own client and listing it would hand every technical token the supplier surface. **Now verified in the deployed application, which is what closed this item:** a real `client_credentials` token satisfies `@requires: 'IntegrationClient'` on `POST /rest/integration/v1/Orders` and reaches the handler — the reply is the portal's own **400 `SCHEMA_VERSION_UNSUPPORTED`** with a correlation id, not a framework rejection — anonymous requests to every route answer **401**, and the same token on the supplier surface answers **403** with `User 'system' is lacking required roles: [SupplierPortalUser]`. **Carried forward rather than closed with it:** the `supplier` → `req.user.attr.supplier` mapping is still unverified, because a client-credentials token has no user and structurally cannot test it — that needs an interactive login with a role-collection assignment and belongs with Phase 8; SAP S/4 has not been configured to obtain or send a token; and the deployed build still *contains* the mock user block, inert under `[development]` but present. Declarative `@restrict` row filtering was tried and reverted — it returned 403 where the contract documents a scoped 404. (3) **RESOLVED.** Fixture CSVs were included in the HANA deployment output as three `.hdbtabledata` files, so deploying as built would have loaded the synthetic suppliers, orders and items into HANA. The cause was the folder, not the fixtures: `@sap/cds/lib/env/cds-requires.js` declares the seed folders **per profile** — `[development]` gets `db/data`, `db/csv` and `test/data`, while `[production]` gets only `db/data` and `db/csv` — so anything in `db/data` is by definition deployed, and **`test/data` is CAP's built-in development-only folder**. The three CSVs were moved from `db/data/` to `test/data/` with **no content change**, all three recorded by Git as pure renames. No configuration was written, because the profile split already exists in CAP; the alternatives were checked and rejected as unsupported — there is no seed-data skip flag in the CAP defaults, and the HANA build plugin's `CSV_FILE_TARGET` only chooses between `data` and `csv` as a target folder name and cannot exclude anything. Verified: `.hdbtabledata` **3 → 0** and fixture `.csv` in the build **3 → 0**, while `.hdbtable` stays **6**, `.hdbindex` **7** and `.hdbview` **2** — the whole-`gen/` diff removed exactly those six files and nothing else. Local development is unchanged: `npm start` logs `init from test\data\…` for all three CSVs and `SUP001` still sees `PO00001001` and `PO00001002` while `SUP002` sees `PO00001003`, with `npm test` at 87 tests in 20 suites and 0 failures, a clean typecheck and a warning-free production build. (4) **Split in two. HANA runtime verification — ingestion path: VERIFIED. HANA runtime verification — supplier decision/update path: OPEN.** The generated DDL is no longer merely read: the deployed application resolves the production profile to `HANAService`, and one valid order was ingested with a real XSUAA token and then verified directly in the `pih-hdi` container — **201 RECEIVED**, exactly one Order, one OrderItem and one DeliveryReceipt, the supplier association resolving, `sourceSystem`/`sourceOrderId`/`sourceRevision` as sent, and **decimal scale and Timestamp precision surviving the round trip**, which closes the long-standing doubt about decimal storage that reading `DECIMAL(19,2)` in the DDL could only advance. Idempotency holds in the cloud: an exact replay and a byte-different but business-equivalent replay both returned **200** with the same `portalOrderId` and the original `receivedAt`, a changed payload on the same `deliveryId` returned **409 `DELIVERY_PAYLOAD_CONFLICT`**, and counts stayed at 1/1/1 with the stored rows byte-identical including `modifiedAt`. The HANA catalog holds **no `IntegrationService.Orders` table**, runtime-confirming ADR-034; `CDS_OUTBOX_MESSAGES` is a CAP framework table. **No HANA-specific runtime defect was found.** The initial container was **empty on all four tables**, which runtime-proves item 3's fixture exclusion. **The open half is open because every write exercised was an INSERT**: no UPDATE has ever run against HANA, so the `SupplierService` accept/reject UPDATE, the `responseVersion` increment, the `modifiedAt` update behaviour and `SupplierResponseDeliveries` persistence are unverified, as are the interactive `SupplierPortalUser` identity and the `supplier` → `req.user.attr.supplier` mapping. One synthetic supplier (`RTTEST001`) and the order it received (`sourceSystem PIH_RUNTIME_TEST`) **remain intentionally in Trial `dev`** as labelled runtime-test data, because `DeliveryReceipts.order` is an association rather than a composition and the referential effect of deleting either row was not established. A fifth, lower-priority observation: `--profile hybrid` resolves `kind: hana` while retaining a stale `url: :memory:` inherited from the SQLite plugin's `[development]` declaration, which is harmless until a binding exists because `cds bind` supplies real credentials | Items 1, 2 and 3 resolved; item 4's ingestion path verified, its supplier decision/update path open until an UPDATE runs against HANA |
| OI-15 | **Recovery mechanism closed in Phase 7.6.** `retryDelivery` reuses the current UNKNOWN intent, its `DeliveryUUID` and durable snapshot; the targeted runtime run made one preparation and one coordinator dispatch, attempts 1 → 2, with no new intent. The answered CAP 400 `UNKNOWN_SUPPLIER` correctly persisted FAILED. UNKNOWN → DELIVERED remains an evidence limitation because no remaining UNKNOWN snapshot references an active CAP supplier, not a production-code defect | Phase 7.6 mechanism PASS; delivered-success branch unproven |
| OI-16 | **Phase 7.4 certainty contract implemented and ACTIVE source synchronized.** Proven pre-send failures can be `NOT_SENT`; ambiguous no-answer failures remain conservative `MAY_APPLY` / `UNKNOWN`. The ABAP HTTP client exception still cannot establish every pre-send case, and a timeout never proves non-delivery; no guessed certainty was introduced | Bounded target-API limitation retained |
| OI-17 | **Controlled pre-CSRF failure path implemented and exercised.** Only `CSRF_FETCH` is answered as retryable HTTP 503 `UPSTREAM_CSRF_UNAVAILABLE`; failures after successful CSRF remain ambiguous. The final restored SupplierResponse export was verified with its normal CSRF target and no OQ-2 override | Phase 7.9 accepted; closed |
| OI-18 | **Timeout hierarchy corrected and exported.** CAP waits 35 s; both iFlows use 25 s transaction timeouts; OrderDelivery's CAP receiver uses 15 s; SupplierResponse's SAP GET and POST each use 10 s. SAP `EXECUTE( I_TIMEOUT )` remains at its platform default because the target unit was not established; no invented 35-second value or `RETRY_EXECUTE` was added | Phase 7.4 accepted within the stated SAP API limit |

## Technical debt

The base BDEF, behavior pool, EML consumer, initializeStatus, item/header calculations and Supplier presence validation have successful SAP runtime evidence reported by the learner. Quantity greater-than-zero and nonnegative NetPrice validations are SAP runtime-verified by learner-supplied 2026-09-14 output. The old header aggregation is verified for committed active-item deletion only. Its runtime-verified replacement removes the active-table dependency and supports active, saved-draft and buffer-only draft items. Supplier name and **seven of the nine** integration fields remain initial. The other two were fixed by **Phase 5.2a, which is SAP runtime-verified**: Phase 5.1 had established what they cost at the boundary — `OrderRevision` held `0` on every row because no handler wrote it, and CAP's ingestion refuses `source.revision` below 1 with `400 INVALID_SOURCE_IDENTITY` — so `submit` now writes `OrderRevision = 1` and `initializeStatus` now writes `IntegrationStatus = 'NOT_REQUESTED'`. **Still initial and written by nothing:** `DeliveryId`, `LastErrorCode`, `LastErrorMessage`, `LastErrorAt`, `LastCorrelationId`, `LastResponseId` and `LastResponseVersion`. `IntegrationStatus` never advances beyond `NOT_REQUESTED`, and legacy rows persisted before 5.2a keep their blank value by design. The display number and supplier name are unchanged by 5.2a and remain as previously recorded. The update/delete authorization stub remains permissive; negative permission cases and standalone root-create/CDS read authorization are not implemented. No other business validation, DCL, display-number allocation, state-dependent editing restrictions or uniqueness enforcement exists yet.

Phases 2.7A, 2.7B, 2.7C, 2.7D-1, 2.7D-2 and 2.7E are all SAP runtime-verified. The commercial lifecycle is now closed at both ends: `submit`, `approve`, `reject` and `cancel` move business `Status` through `DRAFT` → `SUBMITTED` → `APPROVED`/`REJECTED`/`CANCELLED`, and content is editable only while `Status` is `DRAFT`. `Status` `INITIAL` passes the prechecks only as a transient creation-compatibility case, never as a business state. Root `DELETE` is restricted to `DRAFT` and `PurchaseOrderNumber` is allocated on submit, both runtime-verified. The following limits remain and must not be reported as solved. `APPROVED` → `CANCELLED` carries no delivery-request guard, because no Phase 2 capability can make that condition reachable; the guard belongs with `DeliveryIntent` and `sendToSupplier` in Phase 5 and must not be described as present. `sendToSupplier` is deliberately deferred to Phase 5 because `DeliveryIntent` and dispatch semantics are introduced there, and a status-only action would name a capability that does not exist. `PurchaseOrderNumber` is allocated on a successful `submit` from number range object `ZJP_PO` interval `01`, runtime-verified, which closed OI-02 inside Phase 2 with an implementation. Two consequences are intended rather than debt: an order cancelled directly from `DRAFT` never receives a number, and consumed numbers leave gaps because a number is drawn before the buffer update and is never reset or reused. Database-level uniqueness for `purchase_order_number` remains deliberately unenforced: there is no index and no unique constraint, and the number range object is the sole uniqueness authority. The two number-range exception paths and the wider-than-8-digits guard are compiler-accepted and reasoned but were never triggered by a run. Supplier-side rejection, which sets `RejectionOrigin = 'SUPPLIER'`, is separate future work. Authorization is still the permissive study stub: no approver-role enforcement exists, and "approve" and "cancel" name transitions, not access controls — any user may cancel any order. `Activate` over a closed-status technical draft is untested, instance feature control is not implemented, and no multi-user or concurrency coverage is claimed — the saved-draft `LOCKED` observation is target-specific, single-user evidence, not a lifecycle guarantee.

Phase 3.1 is SAP runtime-verified and added no business logic; the service layer re-exposes the Phase 2 BO and duplicates none of its rules. Phase 3.2 is SAP runtime-verified: both `.ddlx` files match the ADT active state exactly and render a Fiori Elements List Report plus a two-facet Object Page. One negative result is preserved rather than discarded — `@UI.facet` at top level is compiler-verified invalid on this target and must sit inside the `annotate entity … with { }` body. The Phase 1 `@Semantics` markers carry units and currency into Fiori unaided, so no `UnitOfMeasure` or `Currency` column is annotated anywhere.

Two items are recorded rather than solved. `__OperationControl` advertises bound actions even where the lifecycle would reject them — a UX gap, not a rule defect, since the handlers still return HTTP 400 with the Phase 2 message texts; dynamic feature control is the known remedy and must not be implemented unless explicitly scoped. `OptimisticConcurrency` is present in the metadata but no conflicting write was ever issued, so stale-ETag and multi-user behavior stay unproven under OI-13. The service definition `ZJP_UI_PURCHASEORDER` is now in source control as its activated ADT source, and the service root is recorded in [API_CONTRACTS.md](API_CONTRACTS.md). The binding `ZJP_UI_PURCHASEORDER_O4` has no plain-text form and is recorded by its settings; no artifact was invented for it.

Intentional scope limits remain EUR-only precision, synthetic master data, single-tenant supplier isolation, manual approval, no order revisions after submission, and no cancellation after delivery request. These are documented constraints, not hidden production capabilities.

Pending engineering work includes machine-readable API schemas, runtime version pinning, real service contract tests, dispatch/reconciliation implementation, operational retention settings, HANA verification, and deployment evidence. They belong to future phases rather than being reported as completed or bypassed.

HANA is now **configured, deployed, schema-deployed and exercised**: the production profile resolves to `HANAService`, `cap-supplier-portal-db-deployer` ran the artifacts into the existing `pih-hdi` container, the application is bound to it and running, and **one real order has been written to and read back from HANA**. The **ingestion path** is therefore verified — insert, deep insert of the composed items, the dedup receipt, the commit, the read-back and the idempotency behaviour all hold — and **decimal storage is no longer an open question**: `DECIMAL(19,2)`, `DECIMAL(19,4)` and `DECIMAL(13,3)` values round-tripped with their scale intact and no float drift, which closes the Phase 4.6 debt item about SQLite's `REAL_DECIMAL` for the values this path stores. What remains outstanding is narrower and specific: **no UPDATE has ever run against HANA**, so the supplier decision path — accept/reject, the `responseVersion` increment, the `@cds.on.update: $now` behaviour of `modifiedAt` — and `SupplierResponseDeliveries` persistence are unverified, and the broader "HANA portability is an intention, not a fact" item is now partly discharged rather than closed. No HANA-specific runtime defect was found. Security limits are no longer what they were: **production authentication is enforced in a deployed application**, mocked auth cannot resolve outside the development profile, the ingestion endpoint requires a dedicated `IntegrationClient` role instead of accepting any caller, and that enforcement is now verified against the live route — anonymous 401, authenticated technical token into the handler, 403 on the supplier surface. What is **still not** proven is the supplier attribute mapping and SAP's own OAuth client, so Phase 8 continues to own the interactive identity work. A `runtime-test-key` service key exists on the real XSUAA instance for the upcoming SAP outbound configuration; it is a standing credential and should be deleted once that work is done. `pih-xsuaa-probe` is now redundant — bound to nothing and superseded by the real instance — and is recorded as cleanup debt, not an application dependency.

The blocking findings are **OI-14**, of which **items 1 and 3 are now resolved** — the empty `IntegrationService.Orders` persistence artifact is gone, taking the HANA build from 7 tables to 6 with the service contract and OData metadata unchanged, and the fixture CSVs now live in CAP's development-only `test/data` folder, taking the build from three `.hdbtabledata` files to none with local seeding unchanged — while **item 2 is resolved and item 4 is split — its ingestion path verified, its supplier decision/update path open**. Item 4 is no longer a matter of what the build *emits* or what a token *carries*: one order has been ingested, persisted and read back from HANA with its receipt and its idempotency behaviour, and the HANA catalog confirmed the absence of the `IntegrationService.Orders` table that item 1 removed. The remaining half is bounded by one fact — every write exercised was an INSERT — so the supplier decision UPDATE path stays unproven. The one repository change made for the build rather than for HANA is `tsconfig.cdsbuild.json`, which fixes a pre-existing `cds build` failure and affects neither the typecheck nor the tests.

## Evidence policy

Phase 0 historical verification passed: 16 Markdown documents, 35 relative links/anchors and five JSON examples checked, with matching example identities and amounts. No implementation files existed at that milestone. The Phase 0 ZIP is an unchanged historical snapshot.

Phase 1 SAP evidence: the learner reports manually creating and activating all six objects in ADT/Eclipse, with two documented compatibility fixes. No independent SAP run, data-preview row count, CRUD, draft or EML result is claimed here.

The [GitHub repository](https://github.com/joaopedromribeiro/procurement-integration-hub) is published. Phase 2.6 is committed and pushed to main (ef5883d); local HEAD matched origin/main at that point. The Phase 2.7A `submit` sources and documentation are runtime-verified but not yet committed.

The learner-supplied EML output supersedes the earlier activation assumption: all three commits returned sy-subrc = 0, create/read/update/delete succeeded and final header/item row counts were zero. FAILED/REPORTED were inspected on the successful path; deliberate error, warning, authorization-denial and concurrency cases are not covered.

Historical correction checkpoint: 20 Markdown files, 68 relative file-link targets, 39 BDEF mappings and Git whitespace checks passed. No behavior pool existed at that historical point; it has since been added.

| Evidence level | Meaning | Current project |
| --- | --- | --- |
| Designed | Reviewed architecture, contracts or model | Phase 0 documents and Phase 1 refinements |
| Source prepared | Authored source with local structural review | Six table/CDS definitions, base BDEF, corrected behavior pool, ZJP_CL_PO_EML_TEST and ADT guides |
| Validated on SAP | Activated artifacts and actual runtime outcomes, provenance stated | Six Phase 1 objects; managed BO through Phase 2.7C, including active/draft `removeItem`, the `submit`, `approve` and `reject` transitions and the precheck-enforced lifecycle invariant for root, item, create-by-association and `removeItem`, verified by learner-supplied SAP output |
| Historical activation assumption | Superseded by execution evidence | The corrected BDEF/pool were exercised by the successful EML run |
| Published | Repository available on GitHub | GitHub publication reported; origin and main tracking verified locally; no deployed app claimed |

Historical minimal-class checks: 21 Markdown files, 75 relative file-link targets, 39 BDEF mappings and Git whitespace checks passed. These were local structural checks, not SAP execution.

Historical EML preparation checks passed: 22 Markdown files, 81 relative file-link targets, synchronized guide/source listing, 39 preserved BDEF mappings, absence of the unsupported component from handler source, no direct table writes or local-mode bypass in the console consumer, and Git whitespace checks. All six Phase 1 source hashes match the prior checkpoint. Those local checks are now supplemented by the successful learner-supplied SAP run.

Update this file after each phase with completed work, current phase, next steps, decisions, issues and debt. Record failed checks honestly and keep mocks separate from SAP execution evidence.

Historical runtime-evidence documentation update: 24 Markdown files and 96 relative file-link targets checked; the EML guide still matches the working source. The supplied output contains three successful commit results and the final PASS marker. All ten ABAP/CDS source hashes are unchanged, including all six Phase 1 objects. At that checkpoint the determination artifact was a plan only; implementation now follows.
